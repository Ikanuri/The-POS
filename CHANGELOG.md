# Changelog

Catatan teknis **1:1 dengan riwayat commit** (terbaru di atas). Setiap baris =
satu commit: `hash` — subjek commit. Ini catatan untuk developer/maintainer;
untuk ringkasan ramah-pengguna lihat [PATCHNOTES.md](PATCHNOTES.md).

> Dihasilkan dari `git log`. Saat menambah commit baru, tambahkan entri di
> bawah tanggal yang sesuai (paling atas).

## 2026-09-06 (sesi kedua puluh sembilan)

- `64a3585` — feat(kasir): redesain sheet "Pengaturan Struk" di layar Struk —
  ikon gear app bar sebelumnya membuka `PopupMenuButton` bawaan Flutter
  (1 item, terasa generik), diganti bottom sheet custom mengikuti pola
  visual sheet lain di app (handle bar + `Material` rounded-top 20, judul
  ber-ikon, `SwitchListTile` ber-ikon+deskripsi, accent terracotta). Isi
  tetap 1 opsi ("Tampilkan Laba"), perilaku (`_showProfit`, persist ke
  `receipt_show_profit`) tidak berubah — murni redesain tampilan. Test
  baru `test/receipt_settings_sheet_test.dart`.

## 2026-09-06 (sesi kedua puluh delapan)

- chore: bump `pubspec.yaml` ke `2.46.0+97` — merge dua sesi paralel yang
  kebetulan sama-sama bump ke `+96` sebelumnya (lihat docs/HANDOFF.md),
  BUILD dinaikkan sekali lagi supaya tidak ada dua rilis beda isi dgn
  nomor BUILD sama.
- `dce7935` — feat(kasir): fitur "kembalian sudah diambil" di Pra-Bayar —
  `CartPrabayarNotifier` dapat akumulator `changeTakenTotal` (persist ke
  SharedPreferences, key sama dgn entri, format lama tetap kompatibel),
  `poolAvailable = totalLocked - changeTakenTotal`. Baris "Kembalian" di
  footer keranjang (`cart_sheet.dart`) dapat checkbox — tap menambahkan
  nilai yg sedang tampil ke akumulator, langsung recompute jadi Rp 0/
  hilang. `buildPrabayarCheckout`/`_prabayarCoversTotal`
  (`payment_screen.dart`) dipindah dari `lockedSum` mentah ke pool —
  porsi yg sudah diambil dipotong dari entri Pra-Bayar paling baru
  dikunci (invariant Σ`TransactionPayments.amount` == `combinedPaid`
  tetap terjaga). `changeTakenTotal` ikut tersimpan/terpulihkan saat
  hold/resume pesanan (key `prabayarChangeTaken`), reset bersamaan
  `clear()` entri Pra-Bayar.

## 2026-09-06 (sesi kedua puluh tujuh)

- `6ab6e5e` — feat(produk): assign ke Kategori Harga langsung dari Edit
  Produk — ikon `Icons.sell_outlined` di tiap baris "Harga Lain
  (opsional)" (satuan produk utama & varian) membuka picker Kategori
  Harga + editor margin bidirectional (diekstrak jadi widget reusable
  `PriceCategoryMarginSheet`, `lib/core/widgets/price_category_margin_
  sheet.dart`, dipakai ulang di `kategori_harga_screen.dart`).
  `createVariant`/`updateVariant` (`app_database.dart`) altPrices param
  ganti jadi `AltPriceInput` (4 field kategori tambahan) supaya varian
  ikut bisa category-linked. Unassign membekukan harga live-computed
  terakhir jadi manual (bukan mengosongkan).

## 2026-09-06 (sesi kedua puluh enam)

- `ee313e8` — fix(kasir): chip "Harga dasar" di `ItemEntrySheet` (produk
  utama & varian) TIDAK lagi memakai harga Kategori Harga aktif —
  ditambah field `trueBasePrice` yg resolve TANPA `activeCategoryId`,
  terpisah dari `basePrice`/`price` (harga efektif, tetap dipakai apa
  adanya utk pre-fill & baseline `_priceOverridden`). Bug dilaporkan
  user: kasir override harga produk yg ada di kategori aktif, tap chip
  "Harga dasar" bermaksud kembali ke harga normal malah tetap dapat
  harga kategori lama. `repriceCartForCategoryChange` (toggle kategori
  di keranjang) diverifikasi SUDAH benar sejak awal, tidak diubah.

## 2026-09-06 (sesi kedua puluh lima)

- `72505bb` — fix(kasir,pengaturan): nominal Total keranjang dibungkus
  `FittedBox`+`maxLines:1` (mengecil otomatis, bukan wrap 2 baris — ruang
  makin sempit sejak ikon Pra-Bayar menempati baris tombol Bayar); layar
  "Tambah Produk" Kategori Harga sekarang menampilkan kode produk sbg
  subtitle.
- `a6e07ba` — fix(laci-meja): badge notif jumlah di ikon kategori/search/
  Kuota/Salin sekarang bergeser ke pojok kotak (offset 10,-10), tidak lagi
  numpuk di glyph ikon yang sejak revisi sebelumnya sudah di-tengah kotak.

## 2026-09-05 (sesi kedua puluh empat)

- `39011fd` — refactor(laci-meja): rapikan baris statistik pre-order &
  pindahkan Kategori Harga ke Produk. Hapus dropdown internal pemilih
  produk jaminan di chip statistik pre-order (`_PreorderStatsLine` DIHAPUS
  total) — fungsinya diwakili dropdown filter produk utama; jaminan
  sekarang teks biasa "Jaminan: N" ikut `effectiveProduct` (produk yang
  difilter, atau produk tunggal implisit), disembunyikan total kalau
  "Semua Produk" dipilih dgn >1 produk aktif. Logika filter+statistik pindah
  ke `_computePreorderStats` (dipakai bersama `build()` &
  `_buildPreorderList`, satu sumber kebenaran). Field cari tab Pre-order
  (& Titip/Pinjaman) jadi satu ikon 36x36 (persis `_CategoryIconBtn`),
  melebar via `Stack`+`AnimatedPositioned` (pola sama `_KasirTopbar`)
  menimpa tombol Kuota+Salin Laporan yang pindah ke baris atas
  (`_PreorderTopActionBtn` baru) — kedua tombol itu cuma tampil di tab
  Pre-order. qty total & jumlah entri pindah ke baris dropdown filter
  produk. Fix bug nyata: `Container` 36x36 ikon kategori/search/Kuota/Salin
  tanpa `alignment` bikin ikon numpuk di pojok kiri-atas — tambah
  `alignment: Alignment.center`. Menu "Kategori Harga" pindah dari
  `ListTile` Pengaturan ke `IconButton` AppBar layar Produk (route tetap
  `/pengaturan/kategori-harga`). Test baru: `laci_meja_top_row_actions_test.
  dart`, `kategori_harga_entry_point_test.dart`; `laci_meja_dashboard_
  redesign_test.dart`/`laci_meja_dashboard_grouping_test.dart`/
  `preorder_quota_line_test.dart` disesuaikan.

## 2026-09-05 (sesi kedua puluh tiga)

- `d77117e` — feat(kasir): toggle Kategori Harga aktif di keranjang (Fase
  C, final). `PriceService.resolvePrice` param `activeCategoryId` (menang
  di atas customerGroup/qty-tier/base, `PriceSource.category` baru,
  costPrice dari tier qty berlaku). `AppDatabase.getCategoryPriceFor` +
  refactor `_liveCategoryPrice` jadi satu sumber kebenaran (dipakai juga
  `getAltPrices`/`getPriceCategoryMembers`). `CartItem.priceFromCategoryId`
  (penanda internal, tidak pernah menyentuh `priceOverridden`).
  `cart_price_category_provider.dart` baru: `cartPriceCategoryProvider`
  (persist per-cartId), `canOverrideHargaProvider`,
  `repriceCartForCategoryChange` (re-price massal saat toggle
  berganti/mati/ganti kategori, manual override selalu menang). `CartSheet`:
  baris chip toggle "Normal"+kategori (gerbang izin `override_harga` +
  minimal 1 kategori terdaftar) & badge `sell_outlined` beda dari pensil
  override manual. `ItemEntrySheet`: harga awal item baru ikut kategori
  aktif. Hold/resume (`kasir_screen.dart`+`cart_sheet.dart`): kategori
  aktif ikut payload `priceCategory`, pulih saat resume (transfer QR
  SENGAJA tidak membawa lintas device). 22 test baru (semua
  revert-verify): resolvePrice prioritas+costPrice+live-compute,
  getCategoryPriceFor, repriceCartForCategoryChange (4 skenario),
  hold/resume, CartSheet toggle UI, ItemEntrySheet harga awal.

## 2026-09-05 (sesi kedua puluh dua)

- `2d5a8cb` — refactor(laci-meja): redesain dashboard Laci Meja jadi lebih
  compact (`laci_meja_dashboard_screen.dart`). 3 kartu ringkasan besar
  (`_SummaryCard`) diganti 3 ikon kotak kecil gaya `_TbBtn` kasir (36x36,
  radius 10, `Badge` count menempel di ikon, tap = pindah kategori aktif +
  update badge), kategori aktif ditandai latar+bingkai lebih pekat warna
  aksennya. Warna aksen baru di `app_theme.dart`: `pinjamanFg`/`pinjamanBg`
  (indigo/periwinkle) & `preorderFg`/`preorderBg` (turquoise/petrol);
  Titip/Ketinggalan reuse `laciFg`/`laciBg` lama. Satu field cari
  (`LaciMejaExpandableSearch`, dipromosikan jadi shared — sebelumnya
  privat tab Pre-order) dipakai bersama ketiga kategori, provider
  digeneralisasi (`_preorderSearchProvider` -> `_laciMejaSearchProvider`,
  dst) — teks TIDAK reset saat pindah kategori. Titip/Ketinggalan &
  Pinjaman (sebelumnya SAMA SEKALI tidak punya filter pencarian) sekarang
  ikut tersaring nama pelanggan/barang. Dropdown filter produk Pre-order
  tetap ada, dipindah ke baris tersendiri di bawah baris ikon+cari.
  Container besar pembungkus baris statistik Pre-order dihapus — tiap
  atribut (chip Produk/Jaminan, dropdown, tombol Kuota/Salin) sudah punya
  bingkai sendiri. Bug ditemukan+diperbaiki: search field yg pembungkusnya
  (`Expanded` vs polos) berubah tergantung status expanded membuat Flutter
  membuang state (termasuk teks yg sudah diketik) tiap ganti status —
  sekarang selalu dibungkus `Expanded`. Test baru
  `laci_meja_dashboard_redesign_test.dart` (9 kasus, revert-verified).
  Version 2.42.0+89 -> 2.43.0+90.

## 2026-09-05 (sesi kedua puluh satu)

- `5f45f4a` — feat(pengaturan): Fase B "Kategori Harga" — layar baru
  Pengaturan -> Kategori Harga (`kategori_harga_screen.dart`). List
  kategori (CRUD nama, reorder, hapus — pola sama `PaymentMethodsScreen`).
  Detail kategori: daftar produk anggota + tambah produk (cari produk ->
  pilih satuan) + editor margin bidirectional per produk (toggle Acuan
  Modal/Dasar — "Modal" disabled kalau costPrice<=0 — x toggle Jenis
  Rupiah/Persen; field Margin & Harga Jual saling live-update). Route
  `/pengaturan/kategori-harga` + entry menu Pengaturan. Test widget
  (`kategori_harga_screen_test.dart`, revert-verified). Version
  2.41.0+88 -> 2.42.0+89.
- `2e640c3` — test: bump assertion `PRAGMA user_version` terkini (v7-v39)
  ke 40, konsekuensi mekanis dari bump schemaVersion di commit
  sebelumnya (konvensi lama proyek).
- `143eacd` — feat(db): Fase B "Kategori Harga" — skema + kalkulasi.
  Migrasi aditif schemaVersion 39 -> 40: tabel baru `PriceCategories`
  (murni label pengelompokan, tanpa margin default) + 4 kolom nullable
  di `AltPrices` (`priceCategoryId`, `marginAnchor`, `marginType`,
  `marginValue`) — baris lama/ad-hoc tanpa kategori tidak berubah.
  Kalkulasi murni bidirectional `computeCategoryPrice`/
  `computeMarginValue` (`lib/core/utils/price_category_calc.dart`) —
  guard anchor "modal" dgn costPrice<=0 melempar `ArgumentError`.
  `AppDatabase.getAltPrices()` menghitung ULANG live harga baris
  berkategori dari harga dasar/modal produk TERKINI sebelum
  dikembalikan (chip "Harga Lain" `ItemEntrySheet` otomatis jadi
  dinamis, tanpa perlu diubah). CRUD `PriceCategories` +
  `getPriceCategoryMembers`/`setPriceCategoryMargin`/
  `removeProductFromPriceCategory`. Hapus kategori melepas keterkaitan
  (baris `AltPrices` jadi harga manual beku, tidak dihapus); lepas satu
  produk dari layar detail menghapus baris totalnya. Test: migrasi
  v39->v40, kalkulasi (19 kasus), CRUD+live-price+semantik hapus (9
  kasus) — semua di-revert-verify.

## 2026-09-05 (sesi kedua puluh)

- `f527c4e` — feat(kasir): Fase A "Kategori Harga" — dialog "Ubah Total" di
  layar bayar (`_editTotal`) diperluas jadi 2 mode: "Nominal" (perilaku
  lama, tidak berubah sama sekali) dan "Diskon %" (baru) — kasir ketik
  persentase, preview live nominal diskon mentah vs hasil SETELAH
  dibulatkan, pilih kelipatan pembulatan (100/500/1.000/5.000 via
  dropdown) & arah (Turun/Terdekat/Naik via `ChoiceChip`). Fungsi murni
  baru `applyPercentDiscount` (`discount_allocation.dart`) menghitung dari
  `_cartTotal` APA ADANYA (bukan `_total` yang mungkin sudah pernah
  di-override) supaya diskon % tidak menumpuk kalau dialog dibuka
  berkali-kali; hasil di-clamp ke `[0, cartTotal]`. Kelipatan & arah
  TERAKHIR yang dipakai kasir diingat per-device via `SharedPreferences`
  (`kasir_discount_round_multiple`/`kasir_discount_round_direction`).
  `allocateCartTotal`/`_confirm`/`_confirmAddItems` TIDAK disentuh sama
  sekali — hasil akhir mode manapun tetap masuk lewat `_totalOverride` di
  titik yang persis sama seperti sebelumnya. Ini Fase A dari rencana besar
  "Kategori Harga"; Fase B (tabel Kategori Harga + margin per-produk) &
  Fase C (toggle aktif di keranjang) menyusul terpisah.

## 2026-09-05 (sesi kesembilan belas)

- `fd6fa1d` — feat(laporan): transaksi void kini tampil di Laporan ->
  Transaksi (dulu difilter total oleh `watchTransactions`, padahal badge
  VOID + total dicoret di `_TxTile` sudah ada tapi tak pernah terpicu) —
  `watchTransactions()` dapat parameter `includeVoid` (default `false`,
  semua pemanggil lain tidak berubah). Tap nota (void/bukan) di Laporan ->
  Transaksi kini navigasi ke `ReceiptScreen` (struk asli lengkap), bukan
  sheet ringkasan tipis lama — dialog void bespoke `_confirmVoid` &
  `_tambahBayar` di `transaksi_tab.dart` jadi dead code, dihapus. Kolom
  baru `Transactions.voidedBy`/`voidReason` (schemaVersion 38 -> 39,
  migrasi nullable/aditif) dicatat `voidTransaction()` via parameter
  `reason` opsional baru; `showVoidTransactionDialog` (bersama) dapat
  field "Alasan (opsional)"; `ReceiptScreen` tampilkan siapa & alasan void
  di banner "Transaksi ini telah dibatalkan". Turut memperbaiki bug
  `TextEditingController` dipakai setelah dispose (dialog masih animasi
  keluar) yang ketahuan dari test UI baru. Test baru: `migration_v39_test`,
  `void_reason_test`, `transaksi_tab_void_test`, `void_dialog_reason_test`
  (semua revert-verified).

## 2026-09-05 (sesi kedelapan belas)

- `50b6cbe` — refactor(kasir): revisi tata letak UI Pra-Bayar — banner
  full-width lama dihapus, ringkasan "Pra-Bayar Rp X" (netral) + "Sisa Rp Z"
  (merah, `AppTheme.debtFg`)/"Kembalian Rp Y" (hijau, `AppTheme.changeFg`)
  dipindah jadi baris kecil di bawah "Total"; tombol Pra-Bayar dipindah dari
  header ke footer (sekunder, `IconButton.filledTonal`) di samping tombol
  Bayar. Logika Pra-Bayar tidak diubah, murni tata letak. Test
  `test/cart_sheet_prabayar_test.dart` diperbarui + ditambah (revert-verified).

## 2026-09-04 (sesi ketujuh belas)

- `076afe9` — feat(kasir): badge ringkasan Pra-Bayar ("Pra-Bayar Rp X") di
  `_HeldCard` (kartu antrian "Ditahan"), dibaca dari `HeldOrders.cartJson`
  key `prabayar` via `_parseHeldPayload` yang sudah ada — sebelumnya
  nominal terkunci tidak tampak sebelum kartu di-tap. Test baru
  `test/kasir_held_card_prabayar_badge_test.dart` (revert-verified).
- `d16c78c` — feat(kasir): fitur "Pra-Bayar" — kunci sebagian pembayaran di
  keranjang aktif sebelum checkout. Kasir bisa "Kunci" nominal + metode
  bayar dari keranjang yang MASIH aktif (berkali-kali, akumulatif, keranjang
  tetap 100% bisa diedit bebas selama belum checkout beneran) — kasus nyata:
  pelanggan pesan barang, bayar sebagian, lalu nambah/kurang barang lagi.
  `cart_prabayar_provider.dart` baru (`PrabayarEntry`, `cartPrabayarProvider`
  `StateNotifierProvider.family` per cartId, persist SharedPreferences pola
  sama `CartMetaNotifier`, prefix `cartprabayar_v1_` + `cleanupOrphanPrabayar`
  dipanggil `main.dart`). `cart_sheet.dart` — tombol "Pra-Bayar" (ikon
  `lock_clock_outlined`, gerbang `terima_pembayaran` via
  `needsPaymentGateProvider`, HANYA `kMainCartId`) buka `showDebtPaymentSheet`
  yang sudah ada (`remaining` = total keranjang - totalLocked, clamp 0);
  badge live "Pra-Bayar: Rp X terkunci · Sisa Rp Y" (atau "Kelebihan" kalau
  negatif) di atas footer total, tap → sheet daftar entri (hapus per-entri).
  Header sheet (7 IconButton sekarang) diubah dari `Row` statis jadi blok
  ikon scroll horizontal (`reverse: true`) supaya TIDAK overflow di HP sempit
  360dp (judul "Keranjang" tetap fixed di kiri) — fix
  `cart_sheet_header_overflow_test.dart` yang sempat regresi gara-gara ikon
  ke-7 ini.
  `payment_screen.dart` — logika UANG paling sensitif diekstrak jadi fungsi
  murni `buildPrabayarCheckout` (testable tanpa widget): `combinedPaid` =
  lockedSum (entri Pra-Bayar) + `paidAmountNow` (pilihan kasir di layar
  checkout); `status` = 'tempo' HANYA kalau `combinedPaid == 0` juga (selaras
  invariant `_reconcileTransactionTotals` yang sudah ada — tempo murni cuma
  valid kalau `paid == 0`), selain itu 'kurang_bayar'/'lunas' dari GABUNGAN
  vs total; `combinedChange` = kelebihan gabungan. Tiap entri Pra-Bayar jadi
  SATU baris `TransactionPayments` dgn `paidAt` = `lockedAt` ASLI (bukan
  waktu checkout), ditambah (kalau ada) satu baris "sekarang"; kembalian
  gabungan diatribusikan ke baris "sekarang" kalau ada, kalau tidak ke baris
  Pra-Bayar PALING TERAKHIR. Kalau `lockedSum >= total`: bottom bar berganti
  jadi tombol tunggal "Selesaikan Transaksi" (`_prabayarCoversTotal`),
  keypad/"Bayar Nanti" disembunyikan total — kasir tidak perlu isi apa pun
  lagi. `_prabayarEntries` SELALU kosong di mode Tambah Belanjaan
  (`_isAddMode`) — logika baru sama sekali tidak aktif di sana.
  `order_parser_service.dart` — `encodeHandoff`/`parse` bawa entri Pra-Bayar
  MENTAH (`ParsedPrabayarEntry`) lewat baris meta baru `Prabayar:` (JSON
  ringkas `[{a,m,n,t}]`) — service ini SENGAJA tidak tahu apa-apa soal
  gerbang izin (layering core/services tidak boleh bergantung Riverpod);
  keputusan adopsi/buang ada di pemanggil (`kasir_screen.dart._handleOrderCode`
  DAN `paste_order_sheet.dart._addToCart`, kedua titik yg sama dgn fix
  mismatch-harga sebelumnya) — device penerima TIDAK bergerbang → entri
  Pra-Bayar DIBUANG SEPENUHNYA (barang tetap masuk normal), BUKAN
  ditampilkan sebagian. Hold/resume (`kasir_screen.dart` `_holdCurrent`/
  `_autoHoldCurrentIfAny`/`_resumeHeld`/`_parseHeldPayload`, key baru
  `prabayar` di `HeldOrders.cartJson`) & clear keranjang (`cart_sheet.dart`
  `_holdCurrent`/`_confirmClear`) ikut menyertakan/membersihkan entri
  Pra-Bayar, sejalan dgn `cartProvider`/`cartMetaProvider`.
  Test baru: `payment_prabayar_checkout_test.dart` (komposisi murni +
  round-trip `AppDatabase` sungguhan — lockedSum < total, ==, >, interaksi
  tempo), `order_parser_prabayar_test.dart` (encode/parse + JSON rusak),
  `kasir_handoff_prabayar_test.dart` (adopsi vs buang sesuai gerbang
  penerima), `kasir_prabayar_hold_resume_test.dart` (hold+resume via UI),
  `cart_sheet_prabayar_test.dart` (gate visibility, badge live, hapus entri,
  ubah qty → Sisa ikut berubah). Semua kategori revert-verified (gagal
  sensible sebelum fix diterapkan, hijau lagi sesudahnya) — termasuk fix
  overflow header di atas, yang ditemukan justru DARI full test suite
  regresi saat menambah tombol ke-7.

## 2026-09-04 (sesi keenam belas)

- `b9d57c7` — feat(kasir): peringatan mismatch harga saat transfer
  transaksi antar device. Transfer Transaksi (`OrderParserService.
  encodeHandoff`/`.parse`, dipakai `cart_sheet.dart`/`kasir_screen.
  dart`/`paste_order_sheet.dart`) mempercayai harga pengirim mentah-
  mentah (`trustPrices`) tanpa pernah membandingkannya ke harga fresh
  lokal penerima — walau `parse()` sudah selalu resolve harga itu utk
  kebutuhan lain, hasilnya dulu dibuang. Sekarang, kalau pengirim DAN
  penerima sama-sama BERWENANG (tidak digerbang izin
  `terima_pembayaran`) tapi harga yang dibawa ternyata beda dari
  resolve fresh lokal saat ini, tampilkan badge peringatan
  informasional di baris keranjang penerima (`CartItem.
  priceMismatchLocal`, style sama pola `itemNote` tapi warna
  `scheme.error`) — harga yang dipakai/tersimpan TETAP harga pengirim,
  BUKAN auto-koreksi. Penerima tak berwenang di-skip (device itu toh
  ke-gate lagi di tombol Bayar). `ParsedOrderItem` dapat 2 field baru
  (`priceTrustedFromSender`, `currentResolvedPrice`); keputusan
  tampil/tidak dihitung di `kasir_screen.dart._handleOrderCode` DAN
  `paste_order_sheet.dart._addToCart` (sheet ini juga bisa memproses
  kode transfer yang ditempel manual, bukan cuma katalog pelanggan) —
  dua-duanya pakai `await needsPaymentGateProvider.future` (bukan baca
  sync `.valueOrNull`) krn provider `autoDispose` ini ke-reset saat
  build `KasirScreen` berganti total ke layar scanner kamera (bug yg
  kepergok lewat revert-verify test). State `priceMismatchLocal` murni
  ephemeral (keranjang aktif), tidak pernah ditulis ke tabel manapun.

## 2026-09-04 (sesi kelima belas)

- `2f3463c` — feat(kasir): tombol "Salin Kode Pesanan" di Struk untuk nota
  lama. AppBar layar Struk (`receipt_screen.dart`) dapat ikon baru
  (disembunyikan bila status `void`) yang membangun `List<CartItem>` dari
  `transaction_items` nota lama — qty di-NET-kan per `productUnitId`
  (baris retur negatif menetralkan; item sudah diretur penuh
  dihilangkan) — lalu memanggil `OrderParserService.encodeHandoff`
  (`trustPrices: false`, harga selalu di-resolve fresh saat ditempel
  ulang, bukan harga nota lama) dan menyalin hasilnya ke clipboard.
  Memudahkan kasir membuat ulang pesanan pelanggan langganan lewat
  "Tempel Pesanan" tanpa input manual satu-satu. Test widget baru
  `test/receipt_copy_order_code_test.dart`.

## 2026-09-04 (sesi keempat belas)

- `df5d32b` — feat(kasir): konverter "beli dengan nominal Rp" di field
  Jumlah pada `item_entry_sheet.dart`. Ikon kalkulator di sebelah label
  "Jumlah" (tampil hanya saat harga satuan aktif >0 & tidak terkunci
  pre-order) membuka dialog kecil: ketik nominal uang pelanggan, qty
  otomatis dihitung dari harga satuan aktif (termasuk tier grosir/Harga
  Lain terpilih), dibulatkan 3 desimal, diterapkan via tombol "Pakai".
  Test baru `test/item_entry_rupiah_converter_test.dart` (harness widget,
  revert-verified).

## 2026-09-03 (sesi ketiga belas)

- `cd4cd34` — fix(db): potong stok saat pre-order dipenuhi. Item 59
  (audit kritis sesi sebelumnya). Pre-order TIDAK PERNAH memotong stok
  di seluruh siklus hidupnya: checkout sengaja mengecualikan item
  pre-order dari pemotongan stok (barangnya belum ada), tapi
  `collectPreorderDeposit` maupun `fulfillPreorderQty`/
  `fulfillPreorderEntry` (tombol "Penuhi" dashboard Laci Meja) tidak
  pernah memanggil `_appendStock` — stok sistem tetap tidak berkurang
  walau barang sudah diserahkan ke pelanggan, inventori overstated
  permanen. Fix: stok dipotong SAAT barang diserahkan (fulfill), bukan
  saat DP dibayar — lebih akurat scr fisik. `fulfillPreorderQty`
  memotong persis `qtyFulfilled`; `fulfillPreorderEntry` memotong hanya
  SISA yang belum terpotong pemenuhan sebagian sebelumnya (dihitung dari
  `getLaciMejaTakenQty`), supaya tidak dobel-potong. Type `stock_ledger`
  baru `'preorder_fulfill'` (terpisah dari `'sale'`) utk kejelasan audit
  trail. Tidak ada guard stok negatif baru, konsisten dgn checkout
  normal yang juga tidak memblokir stok kurang. Pre-order yang sudah
  "Dipenuhi" SEBELUM fix ini stok sistemnya sudah kadung tidak
  terpotong — perlu opname manual sekali (lihat `docs/HANDOFF.md`/
  `PATCHNOTES.md`), tidak ada koreksi otomatis. Test baru
  `test/preorder_fulfill_stock_deduction_test.dart` (revert-verified).
- `8c10971` — docs: coret Item 28 dari `PLAN.md` (keputusan user). User
  menegaskan pendekatan QR+approval-gate yang diusulkan tidak praktis
  (menambah gerbang baru) — dianggap selesai/tidak perlu dikerjakan.

## 2026-09-03 (sesi kedua belas)

- `85c5c11` — fix(db): `voidPayment` reverse efek DP pre-order. Item 61
  (audit sesi sebelumnya). `voidPayment` pada baris pembayaran DP/jaminan
  pre-order (note persis `'DP/jaminan pre-order'`, dipakai
  `collectPreorderDeposit`) sebelumnya hanya membalik nominal
  `paid`/status nota via `_reconcileTransactionTotals` (uang aman scr
  nominal), TAPI tidak pernah membalik efek lain `collectPreorderDeposit`:
  `transactionItems.priceAtSale`/`subtotal` baris pre-order (dinaikkan
  dari Rp0 ke harga asli saat DP dibayar) tetap di harga asli, dan
  `preorderEntries.paid` tetap `true` — status "DP sudah dibayar" nyangkut
  walau pembayarannya sudah dibatalkan. Fix: guard tambahan — kalau baris
  yang dibatalkan adalah DP pre-order, reverse eksplisit (kebalikan
  persis): item balik ke Rp0, `preorderEntries.paid` balik `false`, total
  nota ikut turun; `updatedAt` `transactions`/`preorderEntries` dicap
  ulang. Tidak ada tautan `entryId` langsung dari pembayaran ke
  `preorder_entries` — dicocokkan lewat `transactionId` + `paid=true` +
  `updatedAt` terdekat dgn `paidAt` pembayaran (didokumentasikan sbg
  batasan yang diketahui utk kasus jarang >1 pre-order dgn DP terpisah
  dalam satu nota). Test revert-verified (`void_payment_preorder_deposit_
  reverse_test.dart`).
- `3ba1143` — fix(db): `voidTransaction` cascade batalkan entri Laci Meja
  tertaut. Item 60 (audit sesi sebelumnya). `voidTransaction` reverse
  stok/poin loyalty/status transaksi, tapi tidak pernah menyentuh 3 tabel
  Laci Meja (`PreorderEntries`/`LeftBehindItems`/`BorrowedItems`) walau
  baris di sana bisa merujuk transaksi yang di-void — dashboard/pengingat
  cart bar tetap menampilkan entri itu seolah masih berlaku. Fix per
  kategori: `PreorderEntries` reuse `cancelledAt`+`cancelPreorderEntry`
  existing (sudah cap `updatedAt`); `LeftBehindItems`/`BorrowedItems`
  SENGAJA TIDAK dapat kolom "batal" baru/migrasi schema (pendekatan
  paling sedikit mengubah struktur data) — status "batal" disimpulkan
  murni dari status `transactions` induk lewat JOIN di
  `getLaciMejaPending`/`watchLeftBehindItems`/`watchBorrowedItems`/
  `watchLaciMejaOpenCount` (mode default; mode riwayat penuh sengaja
  tidak difilter). Event audit `laci_meja_events` aksi='batal' tetap
  ditulis utk kedua kategori (pola sama pre-order). `voidTransaction`
  dapat parameter `locallyModified` baru (Item 40 pattern), di-thread
  dari 2 call site (`tx_history_sheet.dart`, `transaksi_tab.dart`).
  Investigasi sync (diminta eksplisit user) dilampirkan lengkap di pesan
  commit & `docs/HANDOFF.md`: (a) `voidTransaction` SUDAH mencap
  `updatedAt` transaksi (bukan bug baru); (b) event 'batal' baru justru
  DIKECUALIKAN dari `dumpSince` host->klien oleh filter `excludeVoidTx`
  yang SUDAH ADA sebelumnya (`73338c8`) — audit cukup di host, status
  dashboard klien tetap benar murni via sync status void (Item 62); (c)
  `LeftBehindItems`/`BorrowedItems` tidak dapat kolom baru jadi tidak ada
  `updated_at` baru yang perlu dicap di sana (desain sengaja, bukan
  gotcha kelupaan). Test sync end-to-end 2 `AppDatabase` host+klien
  revert-verified (`void_transaction_laci_meja_cascade_sync_test.dart`).
- `eec6890` — fix(kasir): filter produk di Riwayat Transaksi dipindah ke
  SQL `WHERE`. Item 55 (ditemukan saat audit filter pelanggan sesi
  sebelumnya). Bug pola PERSIS SAMA dgn fix filter pelanggan (`4a331d1`):
  hasil `findTxIdsWithProduct` dipakai MENYARING baris SETELAH query SQL
  utama (`ORDER BY created_at DESC LIMIT 1000/100`) sudah dipotong, bukan
  SEBELUM — transaksi yg mengandung produk dicari bisa tertimbun & tidak
  muncul sama sekali kalau rentang tanggal aktif punya >1000 transaksi
  total. Fix: `productTxIds` dimasukkan sbg `WHERE t.id IN (...)` SEBELUM
  `LIMIT`, pola identik JOIN `customers` yg dipakai fix filter pelanggan.
  Test baru meniru pola `tx_history_busy_month_search_limit_test.dart`
  utk filter produk, revert-verified
  (`tx_history_busy_month_product_filter_limit_test.dart`).
- `1615481` — docs: verifikasi & bersihkan `PLAN.md` (Item 17/21/28/
  41.B1/51/53). Item 17 (persist antrian approval sync) & Item 21 (sync
  UI global) terkonfirmasi SELESAI lewat verifikasi kode langsung
  (`lan_sync_service.dart`/`sync_state_provider.dart`) — dihapus dari
  plan. Item 28 (pegawai lanjutkan pesanan owner lintas device) — klaim
  user "sudah selesai" TIDAK terbukti (grep `_isAddMode`/`addToTxId`
  cuma menunjukkan mode 1-device, tidak ada jalur lintas-device) — TETAP
  di plan, butuh klarifikasi. Item 41 B.1 (rotasi/un-pair storeKey) —
  klaim user "selesai lewat Alihkan Owner" TERBUKTI SALAH
  (`pairing_service.dart` masih eksplisit "belum ada mekanisme"; Alihkan
  Owner itu transfer kepemilikan toko PENUH, beda skenario) — catatan
  pembeda diperjelas, tetap terbuka. Item 51 & Item 53 dicoret user
  eksplisit (keputusan bisnis, tanpa perlu verifikasi kode).

## 2026-09-03

- `a3b6235` — fix(preorder): cocokkan pre-order pelanggan TERDAFTAR lewat `customerId`, bukan nama. Item 58 (audit sesi sebelumnya, keputusan user sudah diambil sesi ini, dieksekusi langsung). Akar masalah: `getOpenPreorderRefsForCustomer` (rujukan baris item struk ke nota pre-order asal) & `getLaciMejaPending` (pengingat cart bar) mencocokkan pre-order pelanggan SELALU lewat `PreorderEntries.customerName` persis, walau kolom `customerId` sudah ada & terisi dari checkout sejak v35 — dua pelanggan TERDAFTAR beda id yang kebetulan namanya sama (mis. dua "Budi") bisa saling tertaut pre-order-nya (pengingat & rujukan salah pelanggan). Fix: kalau pemanggil punya `customerId` (pelanggan terdaftar tersedia di titik pemanggilan), pencocokan MURNI lewat `customerId` — TIDAK di-OR dengan nama. Pembeli ad-hoc (`customerId` null) tetap fallback ke `customerName` seperti sebelumnya, satu-satunya identitas yang tersedia utk kasus itu — tidak diubah. `receipt_screen.dart` diteruskan `customer?.id` (variabel lokal yang sudah dimuat, sebelumnya tidak diteruskan ke query). Test baru `preorder_match_by_customer_id_test.dart` (skenario PERSIS nama kembar: 2 `customers` beda id nama sama, masing² punya pre-order terbuka beda nota — panggil dgn `customerId` A harus cuma dapat pre-order A, bukan B; + regresi negatif ad-hoc tetap match by nama) revert-verified (fix dicabut, test gagal dgn hasil salah — dapat 2 pre-order padahal expect 1, atau map berisi produk milik pelanggan lain). 2 test existing (`laci_meja_marks_and_reminder_test.dart`, `cart_bar_reminder_lines_test.dart`) diperbarui — seed `addPreorderEntry` ditambah `customerId` supaya mencerminkan alur nyata checkout pasca-v35 (checkout SELALU mengisi kolom ini utk pelanggan terdaftar).
- `89cfeaf` — fix(sync): `laci_meja_events` tidak pernah lolos delta setelah host approve. Item 57 (audit sesi sebelumnya, sudah diputuskan user, dieksekusi langsung). Akar masalah: `laci_meja_events` (log kejadian ambil/kembali/penuhi/batal/edit/bayar Laci Meja) tidak punya kolom `updated_at` — delta `dumpSince` host->klien murni pakai `created_at >= since` (waktu kejadian FISIK, sengaja tidak boleh diubah, beda dari `updated_at` tabel lain yang boleh dicap ulang). Kalau watermark sync klien sudah lewat `created_at` event itu SEBELUM owner sempat approve usulannya, event itu tidak pernah lagi lolos filter delta ke sync klien manapun — flag `locally_modified` klien nyangkut selamanya, event yang sama terus diusulkan ulang tiap sync (data tetap AMAN, host membuang usulan identik via `filterUnchangedLaciMejaProposals`, tapi payload usulan tumbuh tak terbatas seiring waktu). Fix: kolom baru `appliedAt` (nullable, migrasi v37->v38, `schemaVersion=38`) dicap `DateTime.now()` di `applyLaciMejaProposals` HANYA saat host benar-benar menyetujui/menerapkan baris `laci_meja_events` — `createdAt` asli tidak pernah disentuh. `dumpSince` case khusus `laci_meja_events` dapat kondisi tambahan `OR applied_at >= ?` di filter `WHERE created_at >= ?` yang sudah ada, sehingga event yang baru disetujui host kapan pun tetap dijamin lolos ke sync klien berikutnya. `mergeRows` sisi klien tidak perlu perubahan — `laci_meja_events` bukan `appendOnlyTables`, sudah otomatis lewat jalur generic `INSERT OR REPLACE` (last-write-wins) yang membawa serta kolom baru ini tanpa kode tambahan. Test baru `laci_meja_events_applied_at_sync_test.dart` (2 `AppDatabase` nyata, host+klien) mereplikasi skenario bug asli end-to-end: klien buat event & upload usulan -> host simpan pending (belum approve) -> klien sync-download SEKALI LAGI dgn watermark sudah lewat `created_at` (dibuktikan event itu TIDAK ikut, persis kondisi bug) -> host BARU approve (`appliedAt` tercap, beda dari `createdAt`) -> klien sync-download lagi dgn watermark yang SAMA (sebelum approve) HARUS tetap membawa event ini balik (`applied_at >= since`) -> `mergeRows` mereset `locally_modified` klien ke 0. Revert-verified (fix dicabut sementara, test gagal dgn pesan jelas di 2 titik: `appliedAt` null & event tidak ikut dump kedua). 17 test `migration_vN_test.dart` yang hardcode assert `PRAGMA user_version` diupdate ke 38 (bump rutin, sama pola sesi-sesi sebelumnya).
- `e71d66d` — fix: kunci qty/jaminan edit pre-order ke qty baris nota tertaut. Item 56 (keputusan user, tindak lanjut audit yang menemukan `a724aa8`) — sheet "Ubah pre-order" (tombol pensil kartu Pre-order di nota, `_showLaciMejaEditSheet` dipanggil dari `_editPreorderEntry` di `receipt_screen.dart`) sebelumnya membiarkan `qtyOrdered`/`depositQty` diedit bebas walau entri tertaut ke baris `transaction_items` tertentu (`PreorderEntries.transactionItemId`, diisi checkout/Tambah Belanjaan sejak v36). Root cause kasus nyata user (nota `A1-20260902-0014`): entri pre-order #1 (tertaut baris nota qty 1) diedit manual via sheet ini jadi qty 2/jaminan 2 TANPA baris nota ikut naik, lalu "Tambah Belanjaan" menambah entri #2 (qty 1 baru) — hasilnya 2+1=3 "titipan" padahal barang fisik cuma 2 unit. Keputusan user: "edit tidak melebihi item produk... untuk penambahan entri produk baru, harusnya lewat tambah pesanan/barang." Fix: `_showLaciMejaEditSheet` dapat parameter baru `maxQty` (nullable) — kalau di-set, field qty menolak nilai di atasnya dgn pesan jelas ("Tidak boleh melebihi qty di nota (N) — tambah lewat Tambah Belanjaan", bukan clamp diam-diam), field jaminan ikut ditolak kalau melebihi qty YANG BERLAKU (nilai qty baru yg diketik, bukan qty lama sebelum diedit). `_editPreorderEntry` menghitung `maxQty` dari qty baris nota tertaut (dicari di `_items` yang sudah dimuat, bukan query DB baru) HANYA kalau `p.transactionItemId` tidak null — entri tanpa tautan (lama, atau titip wadah tanpa beli) tidak terpengaruh sama sekali, tidak ada apa pun utk dibandingkan. 2 kategori laci-meja lain (titip/ketinggalan, pinjaman) TIDAK tersentuh — `maxQty` opsional, default null di pemanggilnya. 4 test widget baru `preorder_edit_max_qty_test.dart` (qty melebihi cap ditolak, qty di bawah cap diterima, jaminan ikut dikunci ke qty berlaku, entri tanpa tautan bebas spt sebelumnya) revert-verified.
- `a724aa8` — fix(kasir,laci-meja,sync): label Kasir pembuat nota, jaminan pre-order per baris, detail event, guard approve tutup entri. Tiga bug dilaporkan user dari 2 screenshot nota `A1-20260902-0014` (dibuat device ASISTEN, dilihat dari HP OWNER) + audit "deep debug laci meja/pre-order & sync". **Bug 1**: header nota tertulis "Kasir: Owner" — `receipt_screen.dart` memakai `device.deviceName` milik device yang SEDANG MELIHAT, bukan pembuat. Nama device lain tidak tersimpan di DB (`deviceName` cuma di SharedPreferences tiap device), tapi `transactions.kasirId` (kode device pembuat) tersimpan & ikut sync. Fix: helper top-level `kasirLabel(tx, device)` — nama device sendiri HANYA kalau `kasirId` cocok `deviceCode` (atau kosong = nota lama), selain itu kode pembuat ("Kasir: A1"), konsisten dgn kolom "Kasir" tab Transaksi laporan yg sudah pakai `kasirId`. Grep `deviceName`: struk share/ESC-POS/nota gabungan TIDAK mencetak nama kasir — cuma 1 titik ini. **Bug 2**: satu nota punya 2 baris LPG (asli + "Tambahan 09:06"), entri pre-order masing-masing (2 & 1 jaminan) — kartu Pre-order benar (2+1) tapi KEDUA baris item tertulis "Titip 1". Akar: `getPreorderDepositForTransaction` keyed `'$productId|$productUnitId'` saja, entri kedua MENIMPA yg pertama (komentar lama "cukup presisi krn satu baris cart per produk+satuan per nota" SALAH — "Tambahan" bisa menambah baris produk sama). Fix: key `transaction_item_id` (kolom sudah ada sejak v36, diisi checkout & tambah belanjaan) per baris, fallback `product|unit` DIJUMLAHKAN utk entri lama tanpa tautan; nilai = jaminan SISA (`sisaDeposit`, dikurangi qty dipenuhi sebagian — konsisten dashboard/laporan salin, sebelumnya `depositQty` mentah). `sisaDeposit` dipindah ke `lib/core/utils/preorder_calc.dart` (`features/laci_meja/preorder_calc.dart` jadi re-export, import lama tetap jalan) supaya `AppDatabase` bisa memakainya; helper baru `preorderDepositForLine(marks, item)` dipakai KETIGA penampil (struk in-app, `_ReceiptPaper` share, ESC/POS `printer_service.dart`) — ESC/POS tidak bisa dites di sandbox (`CapabilityProfile.load()` hang), diverifikasi manual. **Bug 3**: kartu Pre-order menampilkan event "Diubah" tanpa APA yang diubah & device mana — `note` event (ringkasan `_laciMejaChangeSummary`, mis. "jumlah 1 -> 2, jaminan 1 -> 2"; utk 'bayar' nominal DP) & `deviceCode` tidak pernah dirender di `_laciMejaEntryBlock`. Fix: label aksi + suffix " · A1" utk SEMUA jenis event, `note` baris kecil italic di bawahnya. `LaciMejaProposalReviewScreen`: aksi 'edit' dapat label "Diubah" (sebelumnya jatuh ke raw "edit"; `note`-nya sudah tampil sbg subtitle). **Audit sync 4c-i (bug nyata, diperbaiki)**: `applyLaciMejaProposals` `INSERT OR REPLACE` menimpa SELURUH baris host dgn versi klien — skenario: klien edit entri (locally_modified=1, versi TERBUKA), owner memenuhi/menutup entri itu di host SEBELUM approve → approve mengosongkan `fulfilled_at` (event 'penuhi' tetap ada → entri "terbuka" sisa 0, tidak bisa ditutup lagi). `filterUnchangedLaciMejaProposals` tidak melindungi (barisnya memang beda). Guard baru: kolom status-tutup satu arah (`fulfilled_at`/`cancelled_at` pre-order, `collected_at` titip, `fully_returned_at` pinjaman) dipertahankan dari host kalau usulan null; `qty_returned` (cache dari log) ambil yang terbesar. 4 test baru — `receipt_kasir_label_creator_device_test.dart`, `preorder_deposit_per_line_test.dart` (DB per-baris/fallback dijumlah/sisa + widget skenario persis user), `receipt_laci_meja_event_detail_test.dart`, `apply_laci_meja_proposals_keep_closed_test.dart` (2 DB nyata host+klien via `dumpLaciMejaProposals`→`applyLaciMejaProposals`) — SEMUA revert-verified. Temuan audit lain (catatan desain, bukan bug): lihat HANDOFF + PLAN Item 56-58.
- `4a331d1` — fix(kasir): filter nama pelanggan di Riwayat Transaksi dipindah ke SQL WHERE, tidak lagi tertimbun limit query. Bug dilaporkan user: cari SATU PELANGGAN di search box Riwayat Transaksi TANPA filter tanggal dulu ("dari awal"/lihat keseluruhan), lalu persempit ke BULAN TERTENTU — bulan itu tidak muncul sama sekali walau transaksinya ada. Investigasi mendalam via test widget nyata (`AppDatabase` sungguhan) sebelum menyimpulkan akar masalah — hipotesis awal (limit 1000 aktif krn search tanpa filter tanggal, lalu TETAP salah setelah tanggal dipersempit) TERBUKTI SALAH lewat test nyata: begitu tanggal dipersempit, query SQL baru re-run bersih & benar. Akar masalah SEBENARNYA: teks search (`_query`, nama pelanggan/no. transaksi) difilter CLIENT-SIDE *setelah* `_txHistoryProvider` menjalankan SQL `ORDER BY created_at DESC LIMIT 1000/100` — kalau rentang tanggal yang dipilih (bulan tertentu) SENDIRI sudah punya >1000 transaksi dari SEMUA pelanggan (toko cukup ramai), transaksi pelanggan target yang lebih "tua" di dalam rentang itu tertindih transaksi pelanggan LAIN yang lebih baru sebelum sempat difilter namanya — sama sekali tidak muncul, bukan cuma sebagian terpotong. Dibuktikan gagal dulu (revert-verified) via `test/tx_history_busy_month_search_limit_test.dart`, seed 1200 transaksi dalam SATU bulan + 1 transaksi pelanggan target di awal bulan itu. Fix: `_HistoryQuery` dapat field `search` baru (jadi bagian key provider), `_txHistoryProvider` diubah dari `SimpleSelectStatement` jadi `JoinedSelectStatement` (LEFT JOIN ke `customers` via `customerId`), filter nama/no.transaksi (`localId`/`customerName` snapshot/`customers.name`, TERMASUK pelanggan soft-delete — tanpa filter `isActive`, sama seperti `getAllCustomerNamesIncludingInactive`) diterapkan sbg SQL `WHERE` sebelum `LIMIT`, bukan `.where()` Dart sesudahnya — sama pola dgn kenapa `getCustomerTransactions` (statistik pelanggan) tidak pernah kena bug ini (SUDAH `WHERE customer_id = ?` di SQL). Filter box di UI tidak lagi menyaring ulang hasil (`filtered = txs` langsung). **Bug serupa (belum diperbaiki) ditemukan di filter produk** (`_HistoryQuery.product` → `findTxIdsWithProduct`, pola post-limit-filter yang sama persis) — dicatat sbg Item 55 di `PLAN.md`, di luar lingkup laporan user kali ini. 2 test widget baru (`tx_history_customer_search_no_date_test.dart`, `tx_history_busy_month_search_limit_test.dart`) revert-verified.
- `e6076c5` — fix(sync,laci-meja): tambah `updated_at` ke `transactions` + jaminan sisa di laporan pre-order. Item 62 (bug finansial serius): tabel `transactions` tidak punya kolom `updated_at` sama sekali, dan `dumpSince` memperlakukannya sbg append-only murni (filter `created_at >= since`, dan `mergeRows` SKIP baris yang PK-nya sudah ada di sisi penerima). Begitu satu nota sudah pernah tersinkron sekali, perubahan ke baris itu sendiri sesudahnya (void, ganti pelanggan, poin loyalitas) tidak pernah terkirim lagi ke device lain — laporan/omzet di device lain terus menghitungnya sbg data lama. `total`/`paid`/`changeAmount` kebetulan aman krn direkonstruksi dari `transaction_items`/`transaction_payments` via `reconcileTransactionsByIds`, tapi status/customer/points tidak bisa. Fix: kolom `updatedAt` nullable baru (migrasi v36→v37, `schemaVersion=37`), dicap ulang eksplisit di semua titik UPDATE ke `transactions` yang genuinely butuh propagasi lintas-device (`voidTransaction`, `changeTransactionCustomer`, `awardLoyaltyPointsIfEligible`, `_reconcileTransactionTotals`, `addPaymentToTransaction`, `settleMergedDebt`, dan 2 titik "lunas setelah retur/edit item"). `checkedItemIds` SENGAJA tidak ikut dicap — field itu murni per-perangkat (dokumentasinya sendiri sudah bilang tidak ikut sync, sama seperti `changeTaken`). `dumpSince` dapat case khusus `'transactions'` (`OR updated_at >= since`, pola sama `transaction_payments`/`expenses`). `mergeRows` dapat case khusus: baris `transactions` yang PK-nya sudah ada sekarang di-UPDATE (last-write-wins by `updated_at`) utk kolom status/customer_id/customer_name/points_earned, bukan cuma di-skip. Bug kedua (dilaporkan user): `buildPreorderReportText` (`preorder_report.dart`) memakai `depositQty` MENTAH utk "N jaminan" (per-entri, header grup, total atas/bawah) — tidak berkurang seiring pre-order dipenuhi sebagian, beda dari logic dashboard yang sudah benar (`sisaDeposit = depositQty - taken`). Komentar lama yang mengklaim ini "keputusan disetujui user" SALAH — itu cuma kebetulan dari contoh dummy sesi sebelumnya yang tidak menunjukkan kasus dipenuhi sebagian. Fix: logic `sisaDeposit` diekstrak ke `preorder_calc.dart`, dipakai BERSAMA oleh dashboard (`_preorderTile`, `depositByProduct`) dan laporan salin-teks — SATU sumber kebenaran. Audit tambahan diminta user (bandingkan `preorder_report.dart` vs dashboard satu per satu): `sisaOf`/`statusOf` (`e.paid`)/resolusi nama pelanggan (live vs beku)/cakupan filter (`watchPreorderEntries` default `includeClosed=false`)/urutan FIFO (`orderBy created_at asc`) SEMUA sudah konsisten — tidak ada perbedaan lain selain jaminan. `transaction_updated_at_sync_test.dart` baru (DB nyata, `dumpSince`+`mergeRows` end-to-end host→klien: void & ganti nama pelanggan setelah sync pertama), `preorder_report_test.dart` diperbarui — keduanya revert-verified. 17 test migrasi `schemaVersion` diupdate ke 37 (bump rutin).
- `952382f` — polish(laci-meja): field cari expandable + filter produk jadi dropdown. Permintaan user (2 screenshot HP 360-400dp): baris pencarian full-width di dashboard tab Pre-order & layar Riwayat Laci Meja memakan tempat baris filter di bawahnya sampai terpotong/berdesakan. (1) Field pencarian jadi expandable/collapsible — widget baru `LaciMejaExpandableSearch` (`lib/features/laci_meja/laci_meja_expandable_search.dart`), PERILAKU direplikasi dari pola `_KasirTopbar` yang sudah ada (`kasir_screen.dart`): collapsed = ikon kaca pembesar, tap/fokus -> melebar, blur/tombol x saat kosong -> mengecil (teks tetap tersimpan). Implementasi teknis disederhanakan dari `_KasirTopbar` (bukan `Stack`+`AnimatedPositioned` menimpa baris tombol lain) krn 2 layar Laci Meja ini tidak punya baris tombol yang perlu ditimpa dgn fade — dipakai ULANG sebagai satu widget di kedua layar (dashboard + Riwayat), bukan diduplikasi. (2) Filter produk (chip `ChoiceChip` horizontal di dashboard & Riwayat) diganti dropdown pola SAMA PERSIS dgn pemilih jaminan yang sudah ada (`_ProductPickerChip`) — permintaan user eksplisit "opsi dropdown design anda kemarin itu sudah paling pas". Diekstrak jadi widget reusable `ProductPickerDropdown` (`product_picker_dropdown.dart`), dipakai di 3 tempat: pemilih jaminan dashboard, filter produk dashboard, filter produk tab Pre-order Riwayat. **Bug ditemukan & diperbaiki saat implementasi**: `PopupMenuButton<T>` Flutter memperlakukan hasil rute `null` sbg "menu ditutup TANPA memilih apa pun" (panggil `onCanceled`, BUKAN `onSelected`) — opsi "Semua Produk" yang tadinya dipasang literal `PopupMenuItem<String?>(value: null)` TIDAK PERNAH bisa terpilih sama sekali (ketemu via widget test baru). Fix: `PopupMenuButton` generik non-nullable pakai sentinel string utk baris "Semua", dipetakan balik ke null sebelum diteruskan ke pemanggil. Filter tanggal di layar Riwayat SUDAH satu chip/tombol tunggal (tap buka date-range picker, ada tombol hapus saat aktif) sejak awal dibuat — DIKONFIRMASI sudah sesuai permintaan user, TIDAK diubah. 18 test baru/diperbarui (widget test murni utk kedua widget baru + perbaikan test existing yg reference widget lama) revert-verified.
- `326e39d` — feat(pengaturan): reorder metode pembayaran via drag-handle. Permintaan user (owner): urutan metode pembayaran (`PaymentMethods.sortOrder`, `lib/core/database/tables/settings_tables.dart`) bisa diubah. Kolom `sortOrder` SUDAH ADA sebelumnya (dipakai beberapa query, mis. `debt_payment_sheet.dart`) — tidak perlu migrasi schema baru, cukup tambah UI. `PaymentMethodsScreen` diganti dari `ListView.separated` jadi `ReorderableListView.builder` (`buildDefaultDragHandles: false` + `ReorderableDragStartListener` pada ikon drag-handle terpisah — bukan seluruh baris, supaya tidak bentrok dgn gestur swipe-to-delete `Dismissible` yang sudah ada), `onReorder` menulis ulang `sortOrder` via helper baru `AppDatabase.reorderPaymentMethods` (transaksi, pola sama `reorderProductGroups` Item 54). "Tunai" TIDAK diberi posisi khusus — tidak ada indikasi di kode/komentar lama yang mewajibkan ia selalu di urutan pertama, jadi bebas direorder spt metode lain (default paling masuk akal tanpa batasan eksplisit). Audit SEMUA query `paymentMethods` di `lib/`: 2 tempat (`receipt_screen.dart` `_activeQrisMethod`, `cart_sheet.dart` `_activeQrisMethodForPreview`) belum `orderBy(sortOrder)` — ditambahkan supaya pemilihan metode QRIS aktif konsisten dgn urutan yang owner atur. Metode baru yang ditambah manual via sheet tambah sebelumnya selalu insert `sortOrder` default 0 (tie dgn baris lain, urutan jadi tidak konsisten) — fix: helper baru `AppDatabase.paymentMethodsMaxSortOrder` dipakai utk menaruh metode baru di posisi PALING BAWAH (`maxSortOrder + 1`). 3 test baru (2 DB murni: reorder + max-sort-order; 1 widget: drag-handle reorder via `tester.startGesture`/`moveBy`, pola sama `produk_form_reorder_alt_price_test.dart`) revert-verified.
- `2e6bd69` — feat(laci-meja): tombol salin laporan pre-order + fix hitungan "N hari lalu" jadi kalender. Permintaan user: dashboard Laci Meja tab Pre-order butuh cara cepat merangkum antrian TERBUKA jadi teks siap tempel (WhatsApp/nota manual) — format sudah direview & disetujui user sebelum implementasi. Logic murni `buildPreorderReportText` (`lib/features/laci_meja/preorder_report.dart`) — testable tanpa widget, dipanggil dari tombol ikon baru "Salin Laporan" (sebelah tombol Kuota) di `laci_meja_dashboard_screen.dart`: Format A (satu produk — filter aktif ATAU cuma 1 produk terbuka walau filter "Semua Produk") judul per-produk + tiap entri 3 baris; Format B (>1 produk, filter "Semua Produk") judul generik dikelompokkan per produk (header agregat qty/jaminan HANYA muncul kalau grup >1 entri), nomor urut BERKELANJUTAN lintas kelompok. "Sisa" reuse `getLaciMejaTakenQty` (SATU sumber kebenaran, sama dgn progress bar dashboard) — TIDAK dihitung ulang. "N jaminan" per-entri & total header pakai `depositQty` MENTAH (beda dari versi "sisa" 1:1 yang dipakai `_preorderTile` dashboard) — sesuai contoh yang disetujui user. Status Tempo/Lunas pakai `e.paid` (field yang sama & sudah bug-fixed dipakai dashboard, BUKAN query terpisah `transactions.status`), disembunyikan kalau `transactionId` null. Nama pelanggan ad-hoc diberi akhiran " (Umum)". Susulan (permintaan user, di luar lingkup awal tapi menyentuh logic sama): SEMUA perhitungan "N hari lalu" fitur Laci Meja (dashboard, riwayat, laporan pre-order — sebelumnya 3 salinan logic terpisah) disatukan jadi satu helper `calendarDaysSince` (`lib/features/laci_meja/laci_meja_date_utils.dart`) — versi lama pakai `Duration.inDays` LITERAL 24 jam, salah untuk kasus lewat tengah malam (entri kemarin 23:50, dilihat hari ini 00:10 -> harusnya "1 hari lalu", `inDays` mentah bilang "0"). Fix: normalisasi kedua tanggal ke tengah malam dulu baru diselisihkan. 18 test baru (14 logic murni + widget tombol copy + 4 kalender-hari) revert-verified.
- `302167e` — feat(laci-meja): layar Riwayat Laci Meja terpisah (arsip 3 kategori + cari + filter tanggal/produk). Permintaan user: "pisahkan antara ketiga kategori laci meja... tambahkan fitur pencarian, filter per tanggal, dan untuk riwayat kategori pre-order, berikan filter per produk juga." Sebelumnya ikon "Riwayat" di dashboard cuma toggle inline yang menampilkan log gabungan ketiga kategori bercampur satu daftar (`_buildEventLog`, `laciMejaEventLogProvider`) — tanpa pemisah kategori, tanpa pencarian, tanpa filter. Diganti total dgn layar arsip terpisah `RiwayatLaciMejaScreen` (route `/kasir/laci-meja/riwayat`, anak `laci-meja` spt `struk/:txId` — tetap di dalam ShellRoute): TabBar 3 tab (Titip/Ketinggalan, Pinjaman, Pre-order, pola sama `laporan_screen.dart`) — beda dari dashboard yang cuma tampilkan entri TERBUKA, di sini SEMUA entri (terbuka+selesai) muncul sbg arsip dgn status jelas per baris ("Selesai · diambil dd/mm hh:mm" / "Terbuka", dst); kotak pencarian (nama pelanggan/nama barang/catatan) + chip filter rentang tanggal (reuse pola `InputChip`+`showDateRangePicker` dari `tx_history_sheet.dart`); tab Pre-order dapat tambahan filter produk (`ChoiceChip`, pola sama `_PreorderProductFilter` dashboard). TIDAK ada query DB baru — pakai ulang param `includeCollected`/`includeFullyReturned`/`includeClosed` yg SUDAH ADA di `AppDatabase.watchLeftBehindItems`/`watchBorrowedItems`/`watchPreorderEntries` (sudah tertes di `laci_meja_db_test.dart`), cuma belum pernah dipakai UI manapun — filter cari/tanggal/produk dilakukan di sisi Dart (pola sama `_buildPreorderList` dashboard yg sudah lebih dulu memfilter client-side). Provider baru di `laci_meja_provider.dart` (`riwayatLeftBehindAllProvider` dkk.). 9 test widget baru revert-verified.
- `d579c86` — feat(laci-meja): kumpulkan DP/jaminan pre-order yang tadinya dikunci Rp 0. Permintaan user: pre-order dengan jaminan/DP belum dibayar (harganya dikunci Rp 0 saat checkout, `_effectivePrice` di `item_entry_sheet.dart`) — nota bisa sudah LUNAS untuk item lain (kasus nyata: LPG jaminan, item lain sudah dibayar penuh). Sebelumnya TIDAK ADA cara mengumpulkan DP itu begitu pre-order dipenuhi: `editPaidTransactionItem` (jalur edit item nota lunas biasa) sengaja menolak SEMUA kenaikan nilai (hanya izinkan penurunan/refund) — constraint yang benar untuk kasus lain, tapi menutup jalur ini sama sekali. Skema v36: `PreorderEntries.transactionItemId` (tautan PRESISI ke baris nota, diisi `payment_screen.dart` saat checkout) — dipakai `getPreorderDepositOwed` (hitung selisih `originalPrice*qty` vs subtotal berjalan) & `collectPreorderDeposit` (naikkan subtotal item ke harga asli, reconcile total nota via `_reconcileTransactionTotals` yang sama dipakai jalur "Tambah Belanjaan", catat pembayaran via `addPaymentToTransaction` sehingga MUNCUL di Riwayat Pembayaran, tandai `preorderEntries.paid`, catat event 'bayar' ke `laci_meja_events` sehingga MUNCUL di kartu riwayat pre-order struk & di layar review usulan sync). UI: tombol "Penuhi" di dashboard Laci Meja otomatis menawarkan sheet pembayaran (`showDebtPaymentSheet`) kalau ada DP tertunda. 6 test baru (DB + widget) revert-verified. Skema v36 memaksa update 17 file `migration_vX_test.dart` yang hardcode assert schemaVersion=35.

## 2026-09-01

- `e3bf02e` — feat(kasir): struk "Pratinjau Keranjang" — bagikan preview & estimasi total SEBELUM checkout, dgn QR QRIS dinamis. Permintaan user: "kadang pelanggan minta preview serta estimasi total" (mis. lewat WhatsApp) sebelum transaksi disepakati. Mockup Playwright direview & disetujui user sebelum implementasi (instruksi eksplisit). Widget baru `CartPreviewPaper` (lib/features/kasir/widgets/cart_preview_paper.dart) — SENGAJA TERPISAH TOTAL dari `_ReceiptPaper` (receipt_screen.dart), bukan dipakai-ulang dgn parameter kondisional, supaya tidak ada risiko logic status lunas/tempo nota ASLI ikut kecampur dgn kondisi "belum checkout". Dibuat visual tidak mungkin disalahartikan sbg struk resmi: banner terracotta "PRATINJAU KERANJANG", watermark diagonal berulang, TANPA nomor nota, label "Estimasi Total" (bukan "Total"), disclaimer "bukan bukti transaksi/pembayaran". QR QRIS dinamis (nominal = estimasi total saat ini) pakai `injectQrisAmount` (murni offline, TLV EMVCo, tidak perlu transaksi tercatat) via modul `resolveQrisPayload`/`QrisQrBox` yang sudah dipakai bersama checkout & pelunasan hutang. Tombol baru "Bagikan Pratinjau" di header `CartSheet` (ikon share, nonaktif saat keranjang kosong, disembunyikan di mode Katalog) membuka sheet dgn toggle QR terpisah (key SharedPreferences `cart_preview_show_qr`/`cart_preview_qr_dynamic`, tidak menimpa preferensi QR struk asli) → capture `RepaintBoundary` → `Share.shareXFiles`, pola identik "Bagikan Struk". **Ditemukan & diperbaiki saat implementasi**: menambah tombol ke-6 di header keranjang (padat: Tahan/Tempel/Bagikan Pratinjau/Pengaturan/Transfer QR/Kosongkan) OVERFLOW 33px di layar sempit 360dp — semua `IconButton` baris itu dipersempit via `IconButtonTheme` lokal (padding 4, `VisualDensity.compact`, minimumSize 36×36) supaya konsisten & tombol berikutnya otomatis ikut sempit. 8 test baru (`cart_preview_share_test.dart`, `cart_sheet_header_overflow_test.dart`) revert-verified.
- `0d08b26` — polish(laci-meja): tombol Kuota jadi ikon-only + nama produk jaminan berjalan (marquee) kalau kepanjangan. Dilaporkan user via screenshot: baris statistik pre-order (chip "Produk"/"Jaminan" + tombol "Kuota") kepotong di layar sempit — label teks "Kuota" makan lebar tetap yang seharusnya bisa dipakai chip lain. (1) Tombol "Kuota" diringkas jadi `IconButton` simbol saja (tooltip tetap "Kuota"). (2) Nama produk di `_ProductPickerChip` dibatasi lebar (96px) & dibuat berjalan (marquee) kalau kepanjangan, reuse pola `_MarqueeText` yang sebelumnya cuma dipakai di cart bar kasir (nama pelanggan) — diekstrak jadi widget publik `MarqueeText` (lib/core/widgets/marquee_text.dart) supaya dipakai bersama tanpa duplikasi. 2 test baru revert-verified.
- `3be9a9e` — feat(laci-meja): alamat pelanggan di bawah nama + ganti ikon Bagikan Pratinjau. Dua penyesuaian dari feedback user. (1) Ikon "Bagikan Pratinjau" di header keranjang diganti dari `Icons.ios_share` ke `Icons.share_outlined` — disamakan dgn ikon "Bagikan Struk" di receipt_screen.dart (tombol fungsinya identik: buka sheet share). (2) Alamat pelanggan TERDAFTAR ditampilkan sbg baris ke-2 kecil di bawah nama di SEMUA 3 jenis kartu Laci Meja (Titip/Ketinggalan, Pinjaman, Pre-order) — mencegah nama KEMBAR (dua pelanggan beda alamat, nama sama persis) tertukar tanpa buka nota. `getCustomerAddressesForIds` (satu query) + provider `laciMejaCustomerAddressProvider` (keyed `customerId`, beda dari `laciMejaCustomerNamesProvider` yang keyed `transactionId`). Pelanggan ad-hoc otomatis tidak dapat alamat (tidak punya record `Customers`). 6 test baru revert-verified.
- `ca1590d` — feat(produk): Reset Stok — timpa stok seluruh/kategori produk jadi 0 sekaligus. Permintaan user: "beri opsi untuk reset seluruh atau grup produk tertentu stoknya jadi 0". Proposal UI/UX direview & disetujui user sebelum implementasi. Numpang mekanisme Stock Opname yang sudah ada (`commitOpname`) — "reset ke 0" adalah kasus khusus opname (hitung fisik = 0 utk semua produk terpilih), otomatis dapat atomic write, jejak audit `stock_ledger`, DAN muncul di "Riwayat Opname" yang sudah ada tanpa layar riwayat baru (`buildOpnameNote` param `isReset`, note tetap diawali "Opname " supaya lolos filter `getOpnameSessions`). Screen baru `ResetStockScreen`: pilih cakupan (chip kategori/"Semua") → review (Sistem vs Baru=0, TANPA hitung buta) → dialog wajib ketik "RESET" sebelum tombol commit aktif → commit. Produk stok 0 otomatis disaring dari daftar. Entry point ikon "Reset Stok" di app bar Cek Stok, OWNER-ONLY (`device.isOwner`). 11 test baru (4 DB, 7 widget) revert-verified.
- `369697c` — feat(struk): pre-order dgn jaminan dititip ikut ditandai di print & share struk. Permintaan user: "di print serta share struk, jika ada pre-order dengan jaminan dititip, tulis keterangan titip juga di samping nama item" — struk in-app sudah punya penanda ini ("· Titip N"), tapi struk gambar (share, `_ReceiptPaper`) & cetak ESC/POS (`printer_service.dart`) belum. `_ReceiptPaper` dapat param baru `preorderDeposit` (key sama dgn provider on-screen), ditambahkan ke text run nama item sbg "· Titip N". `printer_service.dart` dapat param sama, dicetak sbg "- Titip N" (ASCII polos, bukan "·" — printer ESC/POS tidak dijamin dukung non-ASCII). Cetak ESC/POS TIDAK diverifikasi test otomatis — `CapabilityProfile.load()` (esc_pos_utils_plus) hang tanpa batas di sandbox test lingkungan ini (TimeoutException 10 menit); diverifikasi manual (pola identik dgn `_ReceiptPaper` yang sudah teruji). 3 test baru (struk gambar) revert-verified.
- `28349d0` — polish(kasir): persingkat teks disclaimer di struk Pratinjau Keranjang. Permintaan user: buang keterangan dalam kurung ("mis. diskon, stok, atau item ditambah/dikurangi"), dibuat singkat tapi tetap informatif.
- `481b92d` — fix(sync): tandai status Dipenuhi/Dibatalkan/Sudah diambil di layar review usulan Laci Meja. Ditanyakan user: "apakah pre-order yang sudah dipenuhi client akan muncul lagi sbg usulan sync ke host? kalau iya, apakah itu cuma riwayat sync (bukan bug)?" — jawabannya YA, itu perilaku yang benar (bukan bug, `filterUnchangedLaciMejaProposals` sudah mendokumentasikan ini), TAPI layar review (`LaciMejaProposalReviewScreen`) menampilkan baris itu identik dgn entri BARU yang masih terbuka — tidak ada penanda status sama sekali. Fix murni tampilan: pre-order dapat "· Dipenuhi"/"· Dibatalkan", titip/ketinggalan dapat "· Sudah diambil", pinjaman dapat "· Sudah kembali semua"/"· Dikembalikan sebagian (N)". Baris `laci_meja_events` (log "diambil N" dkk) SEBELUMNYA ikut dikirim & diterapkan sbg usulan TAPI TIDAK PERNAH ditampilkan di layar ini — sekarang tampil sbg section "Riwayat Kejadian" berlabel manusiawi + keterangan eksplisit "bukan permintaan baru". 6 test baru revert-verified.

- `5eeb7f1` — fix(backup): tolak restore backup dari app versi lebih baru dgn pesan jelas. Bug nyata dilaporkan user (screenshot): owner restore backup dari device kasirnya (app versi lebih baru, skema DB sudah migrasi ke kolom baru mis. `transactions.method_name`) ke device pribadinya (app versi lebih lama, skema lokal ketinggalan) -> gagal `SqliteException(1): table transactions has no column named method_name` mentah. Akar: `restoreFromDump` menyusun `INSERT` dari kolom apa pun yang ada di dump TANPA tahu skema lokal ketinggalan migrasi, tanpa pengecekan versi sama sekali. Fix: payload backup (`exportPortable`/`exportOwnerTransfer`) sekarang membawa `schemaVersion` (dari `AppDatabase.schemaVersion` saat export); `DbExportService.restore()` menolak SEBELUM `restoreFromDump` dipanggil kalau schemaVersion payload > schemaVersion lokal, dgn pesan actionable ("Update aplikasi dulu"). Backup lama tanpa field ini tetap kompatibel. 3 test baru revert-verified.

## 2026-08-31

- `3afab62` — fix(struk): jumlah tempo (Sisa) hilang dari struk cetak/share saat nota juga punya kembalian. Dilaporkan user. Baris "Kembali"/"Sisa" di 3 tempat (`printer_service.dart` cetak ESC/POS tunggal + nota gabungan, `receipt_screen.dart` `_ReceiptPaper`/struk share) ditulis sbg if/else-if — saling meniadakan. Skenario nyata: nota sempat dibayar lebih (kembalian, status lunas), lalu ada tambahan barang ("Tambah Belanjaan") yang menaikkan total lagi (kembali kurang_bayar/tempo), tapi kembalian dari pembayaran sebelumnya tetap tercatat — baris "Sisa" yang seharusnya tetap tampil ikut hilang gara-gara if/else-if. Ringkasan on-screen sudah benar pakai 2 kondisi independen sejak awal, 3 jalur cetak/share yang menyimpang. 1 test baru (`_ReceiptPaper`, satu-satunya jalur yang punya harness test) revert-verified; 2 lokasi lain diverifikasi manual (pola identik, tidak ada harness pendekode ESC/POS di repo).
- `8aec6ea` — feat(aksen-pelanggan): teks nama pelanggan terdaftar ikut aksen terracotta di cart bar, semua kartu Laci Meja, & header nota. Permintaan user susulan: aksen sebelumnya cuma kena ikon (kecil, gampang terlewat), nama pelanggannya sendiri masih netral. Cart bar `_MetaChip` dapat parameter `accent` yg skrg ikut mewarnai teks; kartu Laci Meja (`_cardHeader`) dapat parameter `accent` baru via helper `_isRegisteredCustomer`; header nota ikut `AppTheme.accent` saat `tx.customerId != null`. **Jebakan ditemukan**: warna lama nama ad-hoc terisi di header nota (`scheme.primary`) TERNYATA SAMA PERSIS dgn `AppTheme.accent` (tema app set `primary: accent`) — diganti `scheme.onSurface` supaya ad-hoc tidak ikut ke-render terracotta. 3 test baru revert-verified.
- `935cd95` — feat(laci-meja): dropdown custom-desain utk pemilih produk jaminan + angka "Jaminan: N" dinamis per produk. (1) Dropdown pemilih produk didesain ulang total (permintaan user "bukan default flutter") — kartu aksen, indikator radio custom, badge qty, tetap pakai mekanisme `PopupMenuButton` (cuma tampilannya yg di-custom lewat `shape`/`color`/`elevation`+child custom). (2) Bug: angka "Jaminan: N" selalu total semua produk, bukan produk yg sedang dipilih — `_JaminanDropdownChip` dipecah jadi `_ProductPickerChip` (pemilih nama) + `_StatChip` "Jaminan: N" terpisah yg ambil angka dari `depositByProduct[selectedId]`. Test lama disesuaikan + 1 test baru.
- `9ecadfc` — feat(laci-meja): chip jaminan pre-order bisa ganti produk yang ditampilkan lewat dropdown. Permintaan user: rincian jaminan per produk sebelumnya cuma bisa dilihat lewat tooltip yang harus di-tap ulang tiap kali. Satu produk sekarang ditampilkan LANGSUNG di chip (default: produk pertama berjaminan terbuka), produk lain dipilih lewat dropdown `PopupMenuButton` (cuma muncul kalau >1 produk berjaminan). Total keseluruhan jaminan dipindah keluar chip jadi teks polos di sebelahnya — gaya sama dgn "N entri" di sebelah chip "Produk". Provider baru `_preorderJaminanDisplayProvider` (per productId, fallback ke produk pertama kalau pilihan lama sudah tidak ada jaminan terbuka); `depositByProduct` diubah dari `Map<name, qty>` jadi `Map<productId, (name, qty)>`. Tetap satu baris (scroll horizontal). Test lama disesuaikan + 1 test baru.
- `beeb427` — polish(laci-meja): kembalikan gradasi kartu (label normal + angka bold) di statistik pre-order, tetap padat. Permintaan user: versi "satu baris teks polos" sebelumnya (`0d21cb4`) terlalu ringkas — gradasi visual kartu lama yang disukai (label biasa, angka bold) justru hilang; yang jadi masalah sebenarnya cuma ukuran kartunya. `_PreorderStatsLine` sekarang merender chip mini (`_StatChip`, baru) — border+bg tipis (identitas "kartu" dipertahankan), tapi satu baris `Text.rich` per chip (bukan `Container` tinggi dgn Column label/value/sub terpisah spt kartu lama). "Produk"/"Jaminan" tetap 2 elemen terpisah, sekarang berdampingan sbg 2 chip kecil + tombol Kuota, satu baris. 3 test disesuaikan.
- `0d21cb4` — polish(laci-meja): kartu statistik pre-order jadi satu baris ringkas dgn tombol Kuota. Permintaan user: kartu "Total produk"/"Total jaminan" masih terlalu besar setelah redesain sebelumnya. Diganti satu baris teks ("N entri · X produk · Y jaminan") digabung dgn tombol Kuota di baris yang sama — `_PreorderStats`/`_StatTile` (2 kartu besar) dihapus total, diganti `_PreorderStatsLine`. Rincian jaminan per produk dipindah ke tooltip ikon info (detail tetap ada, tidak makan baris tambahan). Chip filter produk tetap baris tersendiri (kebutuhan beda, bisa perlu scroll horizontal), tombol Kuota kini cuma sekali muncul (sebelumnya duplikat tergantung ada-tidaknya chip). 3 test lama disesuaikan.
- `b3262e2` — polish(laci-meja): rapikan tata letak filter produk + kartu statistik pre-order. Permintaan user: elemen-elemen ini terlihat "asal tempel" saat daftar discroll. Filter chip + kartu statistik dibungkus jadi satu panel (border+bg halus, padding seragam), tombol kuota dapat label ("Kuota") + border (bukan ikon telanjang), kartu statistik dapat ikon header + warna surface yang menonjol dari panel. Test diperbaiki pakai `setSurfaceSize` generus + `ensureVisible` (panel lebih tinggi bisa mendorong entri ListView keluar viewport test, & tap ke chip yang ter-clip horizontal scroll bisa jatuh ke widget lain).
- `723b292` — fix(sync): kuota pre-order tidak ikut sync host<->klien + `setSetting` tidak restamp `updated_at` saat diubah. Audit permintaan user atas cakupan sync semua atribut Laci Meja sesi ini. (1) `preorder_quota_thresholds` ditambahkan ke `syncableSettingKeys` — ini kebijakan toko (owner set, kasir baca), bukan scratchpad lokal spt `saved_catalogs`. (2) Bug lebih dalam: `setSetting` tidak menstempel `updatedAt` eksplisit saat UPDATE (`insertOnConflictUpdate` cuma pakai default kolom yg berlaku SAAT INSERT) — mengubah setting yang sudah pernah tersinkron sebelumnya diam-diam TIDAK PERNAH lagi lolos filter `dumpSince`, memengaruhi SEMUA setting di allowlist, bukan cuma kuota. 5 kolom Laci Meja lain (`last_edited_at` x3, `pinned`, `preorder_entries.customer_id`) diverifikasi sudah terikut otomatis lewat `SELECT *` di jalur dump/proposal existing, tidak perlu perubahan. 2 test baru revert-verified.
- `b2c6ac0` — fix(laci-meja): garis kuota pre-order tidak turut menghitung pemenuhan sebagian + statistik basi + gaya visual. 3 penyesuaian dari feedback user atas fitur kuota yang baru dirilis. (1) **Bug**: `preorderIdsBeyondQuota` menjumlahkan `qtyOrdered` MENTAH tanpa mengurangi yang sudah dipenuhi sebagian (`laciMejaTakenQtyProvider`) — entri yang sudah dipenuhi 4 dari 5 tetap membebani kuota seolah utuh 5, sehingga entri di bawahnya "tidak naik" walau barangnya sudah nyaris habis diserahkan (dilaporkan user via screenshot, kasus Abdul Ghani). Fix: kumulatif kuota sekarang pakai SISA (`qtyOrdered - taken`), parameter baru `takenQty` di `preorderIdsBeyondQuota`. (2) **Konsisten**: baris kartu pre-order & statistik "Total produk"/"Total jaminan" ikut ditampilkan sbg SISA (bukan angka pesanan awal yang sudah basi begitu ada pemenuhan sebagian) — jaminan diasumsikan konsumsi 1:1 dgn item (tabung kosong ditukar isi), sama seperti default toggle jaminan di `item_entry_sheet.dart`. (3) **Visual**: garis pembatas ganti jadi putus-putus (`_DashedLine`, `CustomPaint` manual — tanpa dependency baru) warna abu-abu netral (sebelumnya warna peringatan kuning/oranye, terlalu "alarm"), label "Batas kiriman normal (N)" jadi "Batas kiriman (N)" (kata "normal" dibuang). 4 test baru (2 unit `preorderIdsBeyondQuota`, 2 widget end-to-end) revert-verified.
- `57a7189` — fix(laci-meja): garis pembatas kuota tidak pernah muncul saat cuma satu produk yang diantri. Dilaporkan user lewat screenshot (kuota LPG sudah disetel, daftar tetap tanpa garis pemisah). Akar masalah: garis & nomor antrian cuma dihitung saat filter produk aktif, sementara chip filternya SENGAJA disembunyikan kalau hanya satu produk yang punya antrian — jadi filternya mustahil dipilih & garisnya tidak pernah muncul, tepat di kondisi paling lazim di toko (cuma LPG yang diantri). Fix: satu-satunya produk yang punya antrian terbuka dianggap terpilih dgn sendirinya. 1 test regresi baru revert-verified.
- `cdd3ff2` — feat(laci-meja): kuota antrian pre-order, edit atribut entri, pin pinjaman, & aksen pelanggan tetap vs ad-hoc. Empat permintaan user sekaligus. (1) **Kuota/garis pembatas pre-order per produk**: ambang disimpan sbg blob JSON di tabel settings (key `preorder_quota_thresholds`, pola `saved_catalogs`, tanpa migrasi), diatur lewat sheet "Kuota Pre-order" di dashboard; garis pembatas + nomor antrian muncul saat daftar difilter ke satu produk. Posisinya DIHITUNG ULANG tiap render dari antrian yang masih terbuka (`preorderIdsBeyondQuota`), bukan angka beku — permintaan eksplisit user: kalau ada entri yang dipenuhi di luar urutan, garisnya harus ikut menyesuaikan. Chip filter produk baru (pelengkap search box, disembunyikan kalau cuma 1 produk yang punya antrian). (2) **Semua atribut entri Laci Meja jadi editable** dari kartu di nota (`editLeftBehindItem`/`editBorrowedItem`/`editPreorderEntry` + sheet edit di `receipt_screen.dart`) — sebelumnya entri salah input cuma bisa "diperbaiki" dgn dipenuhi/diambil lalu dibuat ulang, yang bikin riwayat audit mencatat kejadian fisik yang tidak pernah terjadi. Qty tidak boleh turun di bawah yang sudah tercatat di log. Tiap edit menulis baris log `aksi = 'edit'` berisi ringkasan perubahan. Kolom baru `last_edited_at` SENGAJA terpisah dari `updated_at` (yang ikut tersentuh aksi operasional/sync) supaya baris "Terakhir diedit" di struk cuma muncul untuk entri yang benar-benar pernah diedit. (3) **Pin kartu Pinjaman** (`borrowed_items.pinned`, `setBorrowedPinned`) — kartu tersemat naik ke atas mengabaikan FIFO `created_at`; pin dinyalakan per GRUP pelanggan sekaligus (kartu dashboard dikelompokkan per pelanggan) & tidak menstempel `last_edited_at`. (4) **Aksen pelanggan terdaftar vs ad-hoc** di cart bar, kartu Laci Meja, & header nota: ikon terisi + aksen terracotta vs ikon garis + warna netral, sinyalnya `customerId` (null = nama manual tanpa record `Customers`). `preorder_entries.customer_id` DITAMBAHKAN (kolom yang sudah lama ada di 2 tabel Laci Meja lain) — tanpa itu pre-order satu-satunya kategori yang tidak bisa dibedakan sama sekali; diisi dari checkout, tambah-belanjaan, & "Jadikan Pre-order". schemaVersion 34→35 (semua kolom aditif nullable/berdefault, ikut jalur sync proposal yang sudah ada lewat `SELECT *`). 11 test baru revert-verified + 17 test migrasi diperbarui ke 35.
- `73338c8` — fix(sync): atribut Laci Meja milik transaksi VOID tidak boleh ikut tersinkron ke host. Diminta user saat diskusi laci-meja: `voidTransaction` sengaja tidak menghapus `borrowed_items`/`left_behind_items`/`preorder_entries` yang menempel pada nota yang dibatalkan (jejak audit, pola soft-delete konsisten), tapi `dumpSince` men-dump ketiga tabel itu tanpa filter thd status transaksi induknya. Opsi diputuskan bersama user: filter di titik keluar data (`dumpSince`), BUKAN hapus data lokal (lebih aman/reversibel). Baris `transaction_id` NULL tetap lolos apa adanya. `laci_meja_events` (log kejadian, `entry_id` polimorfik) ikut difilter supaya tidak jadi log yatim. Tidak menutup celah data yang sudah kadung tersinkron sebelum void (di luar cakupan, disepakati sbg trade-off). 3 test DB baru revert-verified.
- `31a7376` — fix(kasir): toggle "Pre-order?" di item_entry_sheet tidak prefill dari cart line yang sudah ada. Ditemukan saat review logika laci-meja: toggle SELALU mulai dari default (Tidak) tiap sheet dibuka walau item sudah tersimpan sbg pre-order — membingungkan, dan kalau kasir tetap tap "Tambah ke Keranjang" tanpa sadar, status pre-order aslinya BENERAN hilang krn `_submit()` menulis ulang cart line dgn state yang salah. Fix: `_load()` prefill `_isPreorder`/`_dpPaid`/`_depositQty` dari cart line yang ada, sama pola dgn qty/harga/catatan. 1 test baru revert-verified.
- `49985ff` — fix+feat(kasir): 3 perbaikan/fitur retur & pre-order dari review logika retur/kembalian (permintaan user). (1) fix: "Batalkan Pembayaran" pada baris refund retur (amount negatif, uang & stok sudah permanen berubah) atau marker retur nota belum-lunas (method 'retur'/'edit') bikin kembalian HANTU — `paid` naik lagi tanpa total/item ikut balik. Fix 2 lapis: sembunyikan tombol UI + guard di `voidPayment` sendiri. (2) fix: sheet retur salah hitung sisa returnable saat produk sama ada di >1 baris nota (baris asli + "Tambahan" dari Tambah Belanjaan, skenario retur→tambah→retur berkali-kali) — `remainingFor` diganti pool BERSAMA per productUnitId yang menyusut dinamis antar baris, bukan pengurangan independen per baris. (3) feat: "Jadikan Pre-order" dari struk in-app (nota lunas/tempo) — skenario kasir lupa input pre-order saat checkout, baru sadar setelah nota tersimpan; long-press item → dialog qty+catatan → insert `PreorderEntries` bertaut ke nota INI tanpa mengubah nota itu sendiri. 7 test baru revert-verified.

## 2026-08-30

- `ddaa631` — feat(kasir): filter Riwayat Transaksi by kategori metode pembayaran. Permintaan user: karena metode pembayaran sudah tercatat per-nota, `tx_history_sheet.dart` kini bisa difilter berdasarkan kategori metode (Tunai/Transfer Bank/QRIS/E-Wallet/Tempo), sejajar dgn filter status & tanggal yang sudah ada. Chip baru membuka picker bottom sheet, filter diterapkan di query DB (`t.paymentMethod.equals(...)`). Pakai kategori (bukan `methodName` spesifik) krn banyak nota lama null di kolom itu. 2 test widget baru revert-verified. Full suite 1194 lolos / 1 gagal (flaky pre-existing), `flutter analyze` 0 issue.
- `e3216f9` — feat(laporan): chart Penjualan Harian ganti ke garis + label nominal puncak permanen. Permintaan user (screenshot): chart bar-per-hari label tanggalnya tumpuk di hari-hari akhir bulan — akar masalah sama yang sudah diperbaiki di `StatsTrendChart` (chart "Tren penjualan" produk). Diganti pakai widget yang sama (dipakai ulang). Sekalian `StatsTrendChart` dapat garis putus-putus + label PERMANEN di level nilai puncak (bukan cuma saat tap/drag) via `ExtraLinesData`/`HorizontalLine` fl_chart — berlaku otomatis di kedua pemakainya. 2 test baru revert-verified. Full suite 1194 lolos / 1 gagal (flaky pre-existing), `flutter analyze` 0 issue.

- `ba3c9b1` — feat(kasir/laporan): nama SPESIFIK metode pembayaran (mis. "GoPay", "BCA") di struk & riwayat. Permintaan user: cetak struk/share struk/struk in-app sebelumnya cuma tampilkan kategori generik (Tunai/Transfer/QRIS/E-Wallet). schemaVersion 33→34: `Transactions.methodName`/`TransactionPayments.methodName` (nullable, snapshot dari `PaymentMethods.name` saat transaksi/pembayaran dibuat — nota lama tetap null, semua tempat tampilan fallback ke label generik). Ditulis dari checkout (`payment_screen.dart`), tambah bayar tempo (`addPaymentToTransaction` via `debt_payment_sheet.dart`/`hutang_tab.dart`/`tx_history_sheet.dart`/`transaksi_tab.dart`), lunasi gabungan (`settleMergedDebt`), refund (`returnPaidTransactionItems`); ditampilkan di `printer_service.dart` & `receipt_screen.dart` (`_methodLabel`/`_methodShort`, fallback switch kalau null/kosong). 4 test DB baru (round-trip lewat ketiga fungsi tulis + kasus null) revert-verified. 17 test migrasi lama (hardcode literal schemaVersion=33) diperbarui ke 34 + 8 fixture ditambah stub tabel `transactions`/`transaction_payments` (migrasi v34 `ALTER TABLE` keduanya, fixture versi lama belum tentu punya tabel itu). Bump versi 2.19.20+45 → 2.19.21+46. Full suite 1189 lolos / 2 gagal (flaky pre-existing, port sudah dipakai), `flutter analyze` 0 issue.
- `4cfebd5` — feat(printer): toggle auto-disconnect setelah cetak (Koneksi Printer). Permintaan user: sebagian printer thermal tidak mendukung 2 koneksi BT sekaligus, & toggle BT off/on lalu cetak langsung kadang gagal walau printer sudah menyala. `PrinterSettings.autoDisconnectAfterPrint` baru (SharedPreferences, default FALSE — perilaku lama "tetap tersambung" dipertahankan krn user lain justru butuh cetak cepat tanpa reconnect). Ketiga fungsi cetak (`printReceipt`/`printProductLabel`/`printMergedReceipt`) lewat helper bersama `PrinterService._writeBytes` yang disconnect otomatis kalau toggle aktif. UI: section baru "Koneksi Printer" di `printer_screen.dart`. `flutter analyze` 0 issue. Tidak ada test baru khusus (wiring native channel `disconnect()` yang sudah ada & teruji manual, percabangan trivial).
- `6a5444f` — feat(ringkasan): rincian chart Penjualan Per Jam via TAP, permanen sampai tap lain/scroll. Permintaan user: sebelumnya pakai `Tooltip` tekan-tahan bawaan Flutter yang otomatis hilang begitu jari dilepas. Sekarang tap satu bar menampilkan detail (jam + nominal) di atas chart secara PERMANEN, ganti bar lain untuk pindah, tap area kosong di layar atau scroll menutupnya. State di provider level (`_pinnedHourProvider`, `StateProvider<int?>`) krn kedua pemicu "tutup" (tap-elsewhere, scroll) berasal dari widget leluhur `_HourlyChart`. 4 test baru (tap pin, ganti bar, tap-elsewhere, scroll) revert-verified. `flutter analyze` 0 issue.

## 2026-08-29

- `bbbd4f4` — chore(lisensi): hapus panel debug "Binding Device" (user sudah konfirmasi test manual berhasil di device produksi — simulasi mismatch mengunci benar, reaktivasi kode valid membuka kunci lagi). Dihapus: seksi debug di `device_license_screen.dart` + `LicenseNotifier.debugSimulateDeviceMismatch()`/`debugResetDeviceBinding()`. Logic inti (`computeDeviceMismatch`, `rebindDeviceId`, `_checkDeviceBinding`) TIDAK disentuh, tetap tercakup test permanen. Full suite 1183 lolos / 0 gagal, `flutter analyze` 0 issue.
- `d2f3c6b` — fix(lisensi): reaktivasi valid tidak pernah membuka device ter-flag `deviceMismatch` (bug produksi ditemukan user di device asli, bukan skenario test). `activate()` sebelumnya tidak menyentuh baseline ANDROID_ID sama sekali — sekali `deviceMismatch=true`, kode valid sekalipun tidak membuka gerbang lagi (pelanggan jujur bisa terkunci permanen kalau ANDROID_ID berubah krn update OS/factory reset resmi). Fix: `LicenseNotifier.rebindDeviceId` (baru, `@visibleForTesting`) merekam ulang baseline ke device sedang dipakai & membersihkan `deviceMismatch` begitu kode terverifikasi valid & tidak revoked. Trade-off disadari & disampaikan: device clone bisa buka diri lagi kalau juga punya kode valid — beda kelas ancaman dari "clone diam-diam tanpa jejak apa pun". 2 test baru revert-verified. Full suite 1183 lolos / 0 gagal, `flutter analyze` 0 issue.
- `f18c8db` — debug(lisensi): panel sementara "Debug — Binding Device" di `device_license_screen.dart` (tampil ANDROID_ID terbaca + tombol "Simulasikan Device Lain"/"Reset") supaya user bisa verifikasi manual binding ANDROID_ID (Item 25c susulan) langsung dari HP, tanpa laptop/adb — `LicenseNotifier.debugSimulateDeviceMismatch()`/`debugResetDeviceBinding()` ditandai jelas utk DIHAPUS begitu user konfirmasi hasil test berhasil. Tidak bump versi (bukan rilis). `flutter analyze` 0 issue.
- `a539f0b` — feat(lisensi): 2 lapis deteksi baru di gerbang lisensi (Item 25c susulan) — (1) manipulasi jam sistem base-offset (set mundur sekali sebelum aktivasi lalu dibiarkan jalan normal, `isClockRewound` lama tak berdaya krn cuma bandingkan relatif ke riwayat sendiri): cross-check header `Date` dari ping `revoked.json` yang sudah ada (`computeClockManipulated`, toleransi 24 jam, fail-open kalau offline); (2) clone data app ke device fisik lain via tools "Pindah Data" OEM (bisa lewati layar aktivasi sepenuhnya): binding `Settings.Secure.ANDROID_ID` (channel native baru `com.thepos/device_id`, `DeviceIdService`, `computeDeviceMismatch`), baseline direkam sekali saat pertama terlihat (fresh install maupun update dari versi lama, bukan mismatch), fail-open kalau gagal baca. Kedua flag ikut `LicenseState.isLocked` tanpa mengubah `AktivasiScreen` (sudah sengaja generik utk semua alasan terkunci). 9 test pure-logic baru + 4 test integrasi `load()`×platform-channel-mock, semua revert-verified. Channel native tak bisa diverifikasi otomatis (butuh device Android asli). Full suite 1181 lolos / 1 gagal (flaky pre-existing), `flutter analyze` 0 issue.
- `d3299d4` — fix(license-generator): buang fitur scan kamera (dilaporkan user sudah tidak berfungsi — panel "Scan Serial dari HP Pelanggan", `BarcodeDetector`, CSS & JS terkait dibuang total, input manual fingerprint tetap jalan) + tambah tombol "+ Tambah Manual" di tab Data Pelanggan (catat pelanggan yang sudah aktivasi SEBELUM fitur pencatatan ini ada, tanpa perlu generate ulang serial — field nama wajib, device/fingerprint/masa-berlaku/catatan semua opsional). Diverifikasi manual via Playwright headless, nol console/page error.
- `4a49037` — feat(license-generator): tab "Data Pelanggan" di `scripts/license-generator.html` (alat offline terpisah dari app, tidak ikut versi rilis). Tiap kode aktivasi dibuat kini otomatis tercatat (nama pelanggan/toko + nama device opsional utk kasus 1 pelanggan >1 HP, fingerprint, masa berlaku dgn status Aktif/H-7/Lewat dihitung ulang tiap render), bisa ditandai "Revoked" manual (murni penanda lokal, bukan otomatis nulis `revoked.json`), dihapus per baris, dicari, diekspor CSV. Data pelanggan disatukan ke file cadangan kunci privat yang sudah ada (field `customers` baru, backward-compatible dgn backup lama). Diverifikasi manual via Playwright headless (generate → catat → backup → toggle revoked → re-backup → impor di halaman fresh → data pulih utuh), tidak ada console/page error.
- `1b6dd8b` — feat(laporan): qty terjual produk lintas satuan dikonversi ke satuan dasar. Usulan user setelah tanya "produk >1 satuan tampilnya gimana di laporan trend penjualan" — jawaban: sebelumnya `getProductStatsSummary`/`getProductDailySales` cuma `SUM(ti.qty)` mentah digabung lintas SEMUA satuan produk tanpa konversi (2 dus + 20 pcs tampil "22", padahal 1 dus bisa puluhan pcs). Fix: `qtySold` (ringkasan & tiap titik tren harian) dikonversi ke satuan DASAR (`ti.qty * ratioToBase`). `ProductStatsSummary` dapat 2 field baru: `unitName` (satuan dasar) & `unitBreakdown` (satuan non-dasar yang ikut terjual, qty mentah apa adanya, mis. "3 dus"). UI tampilkan "100 pcs" + keterangan "dari itu: 3 dus" (`StatTile.caption` baru), tooltip tren ikut sertakan nama satuan. Test baru (2, revert-verified) + 1 test lama diperbarui. Full suite 1165 lolos / 1 gagal (flaky pre-existing), `flutter analyze` 0 issue.

## 2026-08-23

- `2ab6a29` — fix(kasir): dropdown saran pelanggan di struk in-app MASIH tidak bisa discroll (putaran ke-2, laporan susulan setelah fix `fffa8d0`). Akar SEBENARNYA: `_onCustQueryChanged` memotong hasil pencarian `.take(5)` — pelanggan ke-6+ yang cocok TIDAK PERNAH masuk `_custSuggestions` sama sekali, jadi scroll apa pun mustahil memunculkannya (datanya sudah dibuang sebelum sempat dirender). Widget-nya sendiri SUDAH genuinely scrollable sejak fix sebelumnya, tapi dari sudut pandang user efeknya identik dgn "tidak bisa discroll". Fix: cap dibuang, samakan dgn `payment_screen.dart::_searchCustomers` yang SUDAH BENAR sejak awal (menampilkan SEMUA hasil pencarian, mengandalkan scroll sungguhan utk daftar panjang). Test `receipt_customer_dropdown_scroll_test.dart` diperbarui (seed 8 pelanggan, bukan lagi berhenti di 5) + 1 test baru "pelanggan ke-6+ harus bisa dijangkau via scroll" (drag ke bawah, cek nama terakhir muncul) — revert-verified (assersi `childCount`/nama pelanggan ke-8 gagal tepat sasaran saat cap `.take(5)` dikembalikan). Full suite 1162 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `fffa8d0` — fix(kasir): dropdown saran pelanggan di struk in-app tidak bisa discroll. Bug dilaporkan user: field inline edit "Pelanggan" (`_buildCustomerEditor`, `receipt_screen.dart`) merender dropdown saran lewat `Column` polos — TIDAK PUNYA mekanisme scroll SAMA SEKALI, jadi baris di luar sisa ruang layar (terutama saat keyboard terbuka) mustahil dijangkau. Fix ikuti pola yang SUDAH BENAR di `payment_screen.dart` (dropdown pelanggan checkout, fitur beda tapi masalah identik): `ListView.builder` sungguhan dibungkus `constraints: BoxConstraints(maxHeight: 240)`, plus `Scrollable.ensureVisible` via `GlobalKey` (`_scrollCustIntoView`) supaya field & dropdown-nya ikut naik di atas keyboard saat field disentuh/diketik. Test baru `receipt_customer_dropdown_scroll_test.dart` (2) — revert-verified (assersi `maxHeight`/`ListView` gagal tepat sasaran saat Column polos dikembalikan); test kedua pakai `textScaler` diperbesar (2.2x) supaya 5 saran (cap `_onCustQueryChanged`) PASTI melebihi 240px scr deterministik, membuktikan drag benar-benar menggeser posisi scroll. Full suite 1161 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `53bb405` — feat(laci-meja): pre-order bisa dirujuk dari transaksi lain (usulan user). Skenario: pelanggan pre-order tanpa DP, lalu belanja lagi di nota BERBEDA — sekarang ada link ke nota asli di 2 titik: (1) cart bar, begitu nama pelanggan dipilih (bukan menunggu produk yang sama masuk keranjang); (2) struk in-app, nama item yang produknya cocok dgn pre-order terbuka pelanggan itu jadi bisa diklik. **DB**: `PreorderPendingLine` typedef +2 field (`id`, `transactionId`); method baru `getOpenPreorderRefsForCustomer` (cocokkan lewat `customerName`, pola SAMA PERSIS dgn `getLaciMejaPending` — bukan resolusi identitas "live" lewat `transactions.customerId` spt fix "nama ikut nota" sebelumnya, krn kolom identitas pre-order MEMANG cuma `customerName` sejak awal), exclude nota yang sedang dilihat sendiri, kalau >1 pre-order utk produk sama pilih yang PALING LAMA (FIFO). **Cart bar**: `LaciMejaReminder.bar` sekarang terima `LaciMejaPending?` lengkap (bukan `List<String>` hasil `linesOf`), baris pre-order dirender via `Wrap` dari `InkWell` per produk (bukan `TextSpan+TapGestureRecognizer` — `InkWell` otomatis dibuang lewat siklus widget, tidak perlu dispose manual di widget statis begini); produk tanpa `transactionId` (titip wadah tanpa beli apa pun) tetap teks biasa. **Struk in-app**: span " · Pre-order sebelumnya" (garis bawah, warna `laciFg`) ditambahkan ke text-run nama item — `TapGestureRecognizer` DIPAKAI ULANG per `item.id` (`Map<String, TapGestureRecognizer>`, didispose di `dispose()`), pola sama `inline_banner.dart`. Test baru `preorder_refer_previous_tx_test.dart` (11) — 3 skenario revert-verified (exclude-self, span, link cart bar). `cart_bar_reminder_lines_test.dart` diperbarui (baris pre-order sekarang beberapa fragmen widget, bukan 1 `Text` utuh). Full suite 1159 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `34a0424` — fix(laci-meja): status Tempo/Lunas Pre-order pakai kolom SALAH — semua entri tampil "Lunas". Bug dilaporkan user: status Tempo/Lunas sebelumnya dihitung dari `depositQty` (jaminan WADAH FISIK, mis. tabung kosong LPG), padahal `item_entry_sheet.dart` OTOMATIS mengisi penuh `depositQty = qty` begitu toggle pre-order dinyalakan utk unit yang `requiresDeposit` (baris ~480-482) — TERLEPAS dari apakah DP-nya (uang) benar-benar dibayar. Akibatnya hampir semua pre-order kebetulan punya `depositQty > 0` dan selalu tampil "Lunas". Fix: pakai `e.paid` (`preorderPaid`/toggle "DP sudah dibayar" di form, yang benar-benar mengendalikan `_effectivePrice`) — nota bisa "lunas" krn barang lain di keranjang yang sama, tapi baris pre-order itu sendiri tetap "Tempo" kalau DP belum masuk, sesuai klarifikasi user. Test lama yang salah asumsi ditulis ulang jadi 3 skenario (jaminan wadah ada tapi belum bayar -> tetap Tempo; tanpa jaminan & belum bayar -> Tempo; sudah bayar -> Lunas), revert-verified (assersi baru gagal tepat saat logic lama `depositQty>0` dikembalikan). Full suite 1148 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `edafd77` — feat(laci-meja): status Tempo/Lunas Pre-order — 2 penyesuaian susulan user. (1) Keterangan "sudah bayar" (`e.paid`) DIHAPUS dari kartu — fungsinya sudah digantikan status Tempo/Lunas, tampil berdampingan dulunya jadi redundan. (2) Posisi label dipindah: sebelumnya di ujung kanan baris (`Row` terpisah), sekarang menempel LANGSUNG setelah kata "jaminan" (kalau ada jaminan) atau setelah nama produk (kalau tidak ada jaminan, tidak ada kata "jaminan" utk ditempeli) — `line2` dikembalikan jadi `Text.rich` tunggal (bukan `Row`) supaya alur teksnya menyatu, bukan terpisah ke kanan. Test 2 di antaranya ditulis ulang (posisi + isi teks berubah total), revert-verified. Full suite 1148 lolos SEMUA, `flutter analyze` 0 issue.
- `570e3b5` — feat(laci-meja): status "Tempo"/"Lunas" di samping baris produk kartu Pre-order dashboard. Permintaan user: pre-order TANPA jaminan (DP) tampilkan label merah bold "Tempo", pre-order DENGAN jaminan tampilkan label hijau bold "Lunas" — warnanya pakai `AppTheme.debtFg`/`changeFg` (pola sama di seluruh app). Sinyal ini BEDA dari `e.paid` (centang "sudah bayar" manual di form, independen dari jaminan) — keduanya sengaja tetap tampil berdampingan, bukan saling menggantikan. `_preorderTile` line2 diubah dari `Text.rich` polos jadi `Row` (produk di `Expanded`, label status di ujung kanan). Test baru (2, revert-verified: label hilang total saat fix di-revert). Full suite 1147 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `1ffe791` — feat(laci-meja): hapus tombol "Batal" (X) di kartu Pre-order dashboard. Permintaan user: tombol batal/hapus pre-order dari kartu langsung dihilangkan — hanya "Penuhi" yang tersisa. `cancelPreorderEntry` & aksi log `'batal'` TIDAK dihapus dari DB (masih dipakai jalur sync/riwayat lama), hanya pemicu UI dari kartu ini yang dicabut. Tidak ada route khusus utk aksi ini (murni `onPressed` inline), jadi tidak ada yang perlu dibongkar di `app_router.dart`. Full suite 1144 lolos / 2 gagal (keduanya flaky pre-existing tidak terkait — `proposal_unchanged_end_to_end_test.dart` & `CryptoService decrypt dengan key salah gagal`, lolos terisolasi), `flutter analyze` 0 issue.
- `25f7090` — feat(laci-meja): field "Nama barang" (di luar nota, dialog Titip/Ketinggalan) dukung multi-baris. Permintaan user: input bebas ketik ini sebelumnya terpaku `maxLines` default (1) — teks panjang cuma menunggu penuh kiri-ke-kanan/terpotong, tidak bisa Enter untuk baris baru (mis. daftar beberapa barang dipisah per baris). Fix: `minLines: 1, maxLines: null` (field tumbuh sesuai isi) + `keyboardType: TextInputType.multiline` + `textInputAction: TextInputAction.newline` (Enter jadi baris baru, bukan submit/tutup keyboard). Field lain (qty, nama pelanggan, dialog Pinjaman) TIDAK disentuh — hanya field ini yang isinya lazim berupa daftar bebas. Test baru revert-verified (newline tersimpan utuh di `itemName`, properti field diverifikasi eksplisit). Full suite 1146 lolos, `flutter analyze` 0 issue.
- `39c6e7f` — feat(laci-meja): ambil/penuhi parsial + riwayat per-nota + log global (PLAN.md Item 54, poin 1/2/5 dari 5 usulan user). **Arsitektur** (dipilih user setelah analisis trade-off): pengambilan dicatat sbg baris **log append-only** tabel BARU `laci_meja_events` (`entityType`/`entryId`/`aksi`/`qty`/`createdAt`), BUKAN mengurangi kolom qty — ketiga tabel Laci Meja itu master-data *last-write-wins*, jadi read-modify-write bikin dua pengambilan bersamaan saling menimpa & salah satu HILANG (ledger barang fisik, selisihnya nyata). Sisa = `qty − Σ log`. **Sinkronisasi**: tetap lewat antrian persetujuan owner spt 3 tabel induknya (keputusan eksplisit user), BUKAN auto-merge ala `appendOnlyTables`; append-only di sini soal BENTUK data, bukan jalur sync. schemaVersion 32→33. **Poin 1**: `collectLeftBehindQty`/`fulfillPreorderQty` baru + dialog qty bersama (2 tombol saja per gotcha overflow `AlertDialog`), qty 1 tetap langsung tanpa dialog; `returnBorrowedItemQty` sekarang menulis log DAN menghitung ulang `qtyReturned` dari log (bukan `+= delta`) sehingga kolom itu jadi cache murni. **Poin 2**: kartu riwayat per-nota utk KETIGA kategori di `receipt_screen.dart` — titipan & pinjaman dapat timestamp (sebelumnya tidak ada), pre-order dapat kartu BARU (sebelumnya tidak punya kartu sama sekali, cuma penanda inline); format baris meniru Riwayat Pembayaran. **Poin 5**: layar "Riwayat Laci Meja" (ikon di AppBar dashboard) — log gabungan 3 kategori urut terbaru dulu, dikelompokkan per hari, nama hari/bulan dirakit MANUAL (gotcha `LocaleDataException`). **Migrasi v33**: `qty_returned` pinjaman lama di-backfill jadi 1 baris log historis (id deterministik `bf-<id>`) — tanpa ini riwayat entri lama tampak kosong padahal qty sudah berkurang. **Tutup Buku**: baris log ikut dihapus SEBELUM 3 tabel induknya (`entry_id` bukan FK fisik krn polimorfik → SQLite tidak membersihkan sendiri, kalau tidak jadi yatim permanen & menumpuk tiap tutup buku). **Fix bawaan**: `applyLaciMejaProposals` dulu SELALU mencap `updated_at` walau tabelnya tidak punya kolom itu — sekarang dicek dulu; plus guard `entry_id` (lewati baris log yang induknya belum tersinkron, pola sama guard `transaction_id`). Test baru: `laci_meja_events_db_test.dart` (11), `laci_meja_partial_and_log_test.dart` (6), `receipt_laci_meja_history_test.dart` (4), `migration_v33_test.dart` (1), +1 di `tutup_buku_laci_meja_guard_test.dart` — 4 skenario revert-verified (parsial jadi menutup entri; `+= delta` menghilangkan pengembalian device lain; backfill dimatikan; orphan-cleanup dimatikan). Assersi SUPERSEDED di `laci_meja_proposal_unchanged_end_to_end_test.dart` (kedua sisi kini perlu `eventId` sama agar benar-benar "persis sama") + 16 `migration_v*_test.dart` (schemaVersion 32→33). Full suite **1145 lolos SEMUA**, `flutter analyze` 0 issue.
- `32713aa` — fix(laci-meja): nama pelanggan di dashboard ikut nota rujukan + redesain kartu 3 tingkat. **Poin 3** (bug user: pre-order LPG dicatat saat nota masih pembeli ad-hoc, notanya diubah ke pelanggan terdaftar, di Laci Meja namanya TETAP lama) — ketiga tabel Laci Meja menyimpan nama pelanggan sbg SALINAN BEKU (`customerNameText`/`customerName`) yang tidak pernah dicap ulang. Alih-alih menambah jalur cap-ulang (utk device kasir = mutasi master-data → antrian persetujuan owner), nama DIBACA HIDUP dari nota di sisi tampilan lewat `AppDatabase.getCustomerNamesForTransactions` + `laciMejaCustomerNamesProvider` (satu query utk ketiga kategori sekaligus) — otomatis mengoreksi entri lama, TANPA migrasi & TANPA backfill. Urutan: nama nota → salinan beku → "Umum". Pre-order paling terdampak krn satu-satunya yang tidak punya `customerId` sama sekali. **Poin 4** — kartu jadi 3 tingkat: baris 1 nama pelanggan (H1, w800, jadi header kartu supaya tidak berulang per baris), baris 2 barang+qty (H2, w700; pinjaman pakai "Sisa x dari y"), baris 3 timestamp `dd/MM/yyyy HH:mm` PERSIS format Riwayat Pembayaran + umur relatif berwarna yang lama tetap dipertahankan. `ListTile` diganti widget `_EntryRow` (kelas terpisah spy test bisa menghitung baris per kartu, pola `_MetaTabDivider`). Test baru `laci_meja_customer_name_live_test.dart` (10) — 3 test bug revert-verified (nama baru tidak ketemu saat fix di-revert). Assersi SUPERSEDED diperbarui di `laci_meja_dashboard_grouping_test.dart` (header w700→w800, hitung `_EntryRow` bukan `ListTile`) & `laci_meja_dashboard_redirect_test.dart`. Full suite 1121 lolos / 1 gagal (flaky pre-existing, lolos terisolasi), `flutter analyze` 0 issue.
- `32713aa` — fix(test): `laporan_pengaturan_accent_color_test.dart` masih menuntut kartu seksi "Eksperimental" (amber) yang seksinya sudah dihapus bersama fitur Import Griyo POS — assersi SUPERSEDED dibuang, dua assersi lain (Sinkronisasi ungu, Manajemen Data merah) tetap. Kegagalan ini TERLEWAT saat rilis penghapusan fitur dilaporkan "1111 lolos + 1 flaky" (sebenarnya 2 gagal).
- `971f647` — feat: buat warna avatar produk kasir dark-aware. Palet "tint lembut" (opsi B, dipilih sesi sebelumnya) dipakai apa adanya di mode gelap sebelumnya, bg pastel terangnya jadi tempelan mencolok di atas panel gelap (ditemukan via mockup Playwright susulan atas permintaan user). `_kAvatarPaletteDark` baru (`kasir_screen.dart`) — bg alpha ~20% dari warna fg, huruf dicerahkan — mengikuti pola `scanFg`/`scanBg` dkk (`app_theme.dart`, Item 33). `_avatarColorsFor` sekarang menerima param `isDark`. Light mode tidak berubah. `flutter analyze` 0 issue, full suite 1111 lolos / 1 gagal (flaky pre-existing, sama seperti sebelumnya).
- `c0a570a` — feat: turunkan intensitas warna avatar produk (kasir grid+list) dari gradient solid pekat jadi tint lembut + huruf berwarna solid, dipilih user dari 3 opsi mockup Playwright (opsi B). `_kAvatarGradients`/`_gradFor` di `kasir_screen.dart` diganti `_kAvatarPalette`/`_avatarColorsFor` (record `{bg, fg}`), dipakai di `_ProductCard` (grid, 34x34) & `_ProductListTileState` (list, 42x42). Tidak ada test yang menguji warna avatar sebelumnya, tidak ada test baru diperlukan.
- `1740d68` — chore: hapus fitur "Import dari Griyo POS" (Settings > Eksperimental) & "Cek Duplikat Data" (Settings > Diagnostik) atas permintaan user. Hapus tile+route (`pengaturan_screen.dart`, `app_router.dart`), hapus `lib/features/pengaturan/duplicate_data_screen.dart` beserta `AppDatabase.findMasterDataDuplicates()`/`MasterDataDuplicate` (dipakai eksklusif oleh screen itu). `csv_import_screen.dart` (dipakai bersama fitur "Import Produk CSV" biasa via flag `griyoMode`) dilucuti jadi hanya varian generik, `CsvImportService` tidak disentuh (tetap dipakai path CSV biasa). Test dihapus: `csv_import_griyo_test.dart`, `duplicate_data_screen_test.dart`, `experimental_flags_test.dart` (seluruhnya soal flag Griyo), `master_data_duplicate_detection_test.dart` (menguji method DB yang baru dihapus). `flutter analyze` 0 issue, full suite 1111 lolos / 1 gagal (flaky pre-existing `proposal_unchanged_end_to_end_test.dart`, terbukti bukan regresi — lolos saat dijalankan terisolasi).
- `c0397ba` — fix: stepper +/- hilangkan lingkaran solid saat di keranjang, kecilkan ikon "+" idle. Redesain susulan (persetujuan user via mockup Playwright): (1) idle -- bentuk & cakupan sentuh tidak berubah, ikon "+" dikecilkan sedikit (0.6->0.5 dari circleSize); (2) sudah di keranjang -- lingkaran solid (hijau utk qty/"+", merah utk "-") DIHILANGKAN TOTAL, hanya angka/ikon yang tampil mengambang tanpa latar, cakupan sentuh TIDAK ikut mengecil (`AnimatedContainer`/`Container` berdekorasi diganti `SizedBox` transparan); (3) warna sekarang milik SISI (kanan/"+" = hijau `changeFg`, kiri/"-" = merah `debtFg`) bukan lagi milik jenis konten (ikon vs angka) -- perilaku "qty pindah sisi setelah tekan +" tidak berubah, warnanya ikut berpindah bersama angka. Logic lama "mainFg gelap di lingkaran hijau mode gelap" (kontras thd fill yg skrg sudah tidak ada) dihapus, diganti langsung `AppTheme.changeFg`/`debtFg` yg sudah teks-aman di kedua mode tanpa perlu pasangan fg/bg. Test lama (`add_control_dark_fg_and_no_debounce_test.dart`, `add_control_idle_flat_style_test.dart`) diupdate, assersi lama SUPERSEDED (fill kembali solid saat qty>0) dibalik jadi "tetap tidak ada fill" -- revert-verified, juga diverifikasi via render widget sungguhan. Full suite 1127 lolos, `flutter analyze` 0 issue.

## 2026-08-22

- `09ce02e` — fix: Ringkasan/Laporan tidak auto-refresh setelah sync (total pendapatan tampak "macet"). Bug nyata dilaporkan user: setelah klien sync dari host (host sync dgn klien 1, lalu beberapa transaksi kemudian sync ke klien 2), total pendapatan hari ini di Ringkasan KLIEN tidak bertambah -- padahal di host normal, dan data DB lokal klien sebenarnya sudah benar (kelihatan kalau tekan refresh manual). Akar masalah: hampir seluruh provider Ringkasan/Laporan (`_ringkasanProvider` + 7 tab di `laporan/tabs/`: stok, arus_kas, hutang, pelanggan, pengeluaran, produk, ringkasan_tab) memakai `FutureProvider` biasa (BUKAN `StreamProvider`/`.watch()` ke tabel DB, sengaja krn query-nya agregat berat) -- HANYA re-fetch saat dibuka pertama, refresh manual, atau rentang tanggal berubah, TIDAK PERNAH otomatis begitu transaksi baru masuk lewat sync LAN (`mergeRows` sendiri SUDAH benar memberi tahu Drift tabel yg berubah via `updates:`, tapi `FutureProvider` tidak pernah "mendengarkan" itu). `transaksi_tab.dart` TIDAK terpengaruh (sudah `StreamProvider`). Fix: `dataSyncedTickProvider` (baru) -- ticker ringan di-bump `SyncStateNotifier` setiap sync sungguhan menerima data (klien menerima dari host via `sync()`, host menerima approve klien via `approveSync()`); kedelapan provider `ref.watch(dataSyncedTickProvider)` di baris pertama supaya ikut re-fetch tanpa perlu jadi live-stream mahal. Test baru `ringkasan_sync_refresh_test.dart` -- revert-verified. Full suite lolos (2 kegagalan `proposal_unchanged_end_to_end_test.dart`/`laci_meja_proposal_unchanged_end_to_end_test.dart` dikonfirmasi flaky pre-existing tidak terkait), `flutter analyze` 0 issue.

- `f878d90` — fix: kecilkan lagi logo QRIS di struk cetak (35% -> 25% lebar kertas). Masih dilaporkan terlalu besar oleh user setelah pengurangan sebelumnya (55% -> 35%, `49a529f`). Diturunkan lagi ke 25%. Test regresi kelipatan-8 dipisah dari rasio spesifik yg sedang aktif (pakai lebar ganjil generik 130px, bukan lagi angka turunan rasio) supaya tidak perlu diupdate lagi tiap kali rasio disesuaikan ke depan -- 25% kebetulan sudah kelipatan 8 di kedua ukuran kertas (96 @58mm, 144 @80mm), test generik tetap membuktikan `_qrisLogo` membulatkan input non-kelipatan-8 apa pun.
- `49a529f` — fix: kecilkan logo QRIS di struk cetak (55% -> 35% lebar kertas). Dilaporkan user: logo terlalu besar, diminta disusutkan kira-kira sepanjang kode QR di bawahnya. Lebar QR sendiri tidak bisa dihitung persis dari kode ini -- `gen.qrcode()` cuma mengirim teks mentah + ukuran modul ke command QR native printer (`GS ( k`), jumlah modul akhir ditentukan printer dari panjang data + level koreksi, bukan dihitung di Dart -- rasio diturunkan sbg pendekatan lebih dekat ke ekspektasi user. Test disesuaikan (384*0.35≈134, 576*0.35≈202), assersi & alur regresi kelipatan-8 tidak berubah.
- `e1db726` — fix: sederhanakan tab meta cart bar jadi baris flat sesuai contoh user (bukan tab folder menumpuk). 2 iterasi bentuk "tab folder" sebelumnya (jajar-genjang miring saling menumpuk, `75a049c`) SAMA SEKALI TIDAK cocok ekspektasi user meski sudah lewat proses mockup Playwright yg direview lebih dulu -- user akhirnya kirim contoh konkret utk ditiru persis: EMPAT segmen RATA/DATAR dlm SATU baris (Pelanggan/Pegawai/Tahan/Bayar), dipisah garis vertikal tipis, sudut ATAS baris membulat sbg SATU GRUP (bukan per-segmen), segmen "Bayar" solid menyatu di ujung kanan tanpa celah. Jauh lebih sederhana -- `_IndexTab`/`_IndexTabPainter` (`CustomPainter` jajar-genjang + bayangan manual) DIHAPUS TOTAL, diganti `Container` (`clipBehavior: Clip.antiAlias`, borderRadius top-only, border top+left+right) + `Row` + `_MetaTabDivider` (garis 1px). Warna tetap default (`cs.surface`) kecuali "Bayar" (accent solid + teks putih). Diverifikasi visual via render widget sungguhan (`RepaintBoundary.toImage`), dibandingkan langsung dgn contoh user -- cocok. `cart_meta_index_tabs_test.dart`: `countIndexTabs` diganti `countMetaDividers` (jumlah segmen = jumlah divider + 1). Full suite 1126 lolos, `flutter analyze` 0 issue.
- `e6b215e` — fix: logo QRIS gagal cetak total (printer diam, byte tak terkirim) krn lebar bukan kelipatan 8. Bug nyata dilaporkan user: printer terhubung (lampu indikator nyala, proses `connect()` jalan), tapi kertas tidak pernah keluar sejak fitur logo QRIS ditambahkan. Akar masalah: `Generator._toRasterFormat` (esc_pos_utils_plus) MEWAJIBKAN lebar bitmap kelipatan 8 -- kalau tidak, cabang "padding" internalnya menimpa `oneChannelBytes` dgn `List.filled` (fixed-length) lalu `insertAll` ke situ, melempar `Unsupported operation: Cannot add to a fixed-length list`. Lebar logo yg dihitung `_buildBytes` (55% lebar kertas: 384*0.55≈211 dot @58mm, 576*0.55≈317 dot @80mm) TIDAK PERNAH kebetulan kelipatan 8 -- SELALU meledak begitu toggle "Tampilkan QR Pelunasan" aktif, exception terjadi DI DALAM `_buildBytes` SEBELUM byte apa pun terkirim ke channel native (tidak ada try/catch), jadi tidak ada snackbar error, printer cuma "diam". Fix: `_qrisLogo` membulatkan lebar target ke kelipatan 8 terdekat (ke bawah) sebelum resize. Dibuktikan via revert-verification: exception PERSIS sama muncul saat fix di-revert sementara, hilang setelah dikembalikan (test baru memanggil `Generator.imageRaster` ASLI, bukan mock). Catatan tooling: kombinasi `testWidgets()` + `CapabilityProfile.load()` TERBUKTI HANG TANPA BATAS di environment ini (timeout 10 menit) -- test yg butuh `CapabilityProfile.load()` harus pakai `test()` biasa.
- `75a049c` — fix: koreksi bentuk tab indeks cart bar sesuai referensi (jajar-genjang, kanan paling depan). Redesain tab indeks sebelumnya (`2569a27`) ternyata meleset dari referensi user setelah dicek ulang: seharusnya jajar-genjang (bukan trapesium biasa), arah kemiringan salah (bawah harus menjorok ke KANAN relatif atas, sebelumnya ke kiri), tab paling KANAN paling DEPAN (tab paling KIRI = "Pelanggan" paling BELAKANG & menyatu lurus dgn tepi cart, sisi kirinya TIDAK miring). Diukur ulang langsung dari piksel file referensi (bukan menerka dari ingatan) -- semua tab tinggi SERAGAM (bukan staircase), miring ~25 derajat (`size.height * tan(25deg)`), sudut ATAS dibulatkan tipis (permintaan user, quadraticBezierTo), bayangan tipis di sisi kanan tab yg tertutupi (linear gradient dlm clipPath) mensimulasikan tab depan "jatuh" ke tab belakang. Warna DIKEMBALIKAN ke default (`cs.surface`, sama dgn badan cart) utk semua tab kecuali "Bayar" (tetap solid accent + teks putih) -- permintaan user eksplisit, warna soft-terracotta sebelumnya bukan fokus utama. Proses lewat beberapa iterasi mockup Playwright (HTML+screenshot) yg direview user SEBELUM kode disentuh, termasuk 1x revert (padding tab yg diperbesar utk "center teks" menggeser ambang fit teks marquee "Buk Khotimah" di `cart_bar_bayar_button_test.dart` -- dikembalikan ke padding asli). `flutter analyze` 0 issue, full suite 1126 lolos.
- `1e56dd0` — feat: logo QRIS resmi di struk share & cetak thermal (poin 3). Permintaan user: logo QRIS resmi ikut ditampilkan di struk gambar (share) DAN cetak thermal -- bukan sekadar teks "QRIS" polos (ide itu sempat diajukan sbg alternatif lebih aman, DITOLAK user eksplisit: "Buat agar logo QRIS juga works di print cetak"). Aset: `assets/qris/qris_logo.png` (1350x512, latar transparan, wordmark hitam solid). Paket `image` dinaikkan dari dependensi transitif jadi langsung. **Bug nyata yang wajib dicegah** (ditemukan sblm nulis kode, baca source `esc_pos_utils_plus`): `Generator._toRasterFormat` (dipanggil `imageRaster`) memanggil `grayscale()`+`invert()` yang HANYA membaca kanal RGB, TIDAK PERNAH melihat alpha -- piksel latar TRANSPARAN di file asli kebetulan RGB (0,0,0), SAMA PERSIS dgn RGB wordmark hitamnya sendiri, jadi tanpa komposit ke kanvas putih dulu printer akan mencetak SATU BLOK PADAT, bukan bentuk logo -- dibuktikan via revert-verification (test kontras gagal sensibel begitu langkah komposit dilewati). `PrinterService._qrisLogo` (baru): decode PNG -> komposit ke kanvas putih (`img.fill`+`img.compositeImage`) -> resize ke lebar target dlm dot printer (~55% lebar kertas, 384 dot @58mm/576 dot @80mm) -> di-cache in-memory. Dicetak via `gen.imageRaster()` TEPAT DI ATAS command QR native -- risiko printer murah tidak mendukung bitmap raster sama spt risiko QR-sbg-gambar yg sudah didokumentasikan. `_ReceiptPaper` (struk gambar/share): logo di atas `QrisQrBox` via `Image.asset` biasa (RepaintBoundary capture menghormati alpha apa adanya, tidak perlu komposit manual). Test baru `printer_qris_logo_test.dart` (3, termasuk revert-verified kontras putih/hitam) + 1 assersi baru di `receipt_screen_qr_toggle_test.dart` (logo tampil di atas QR) -- revert-verified. Regresi: 33 file test receipt/printer terkait tetap lolos. Full suite 1123 lolos + 1 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `a68dc39` — fix: perbaiki test press-scale yang gagal krn AddControl idle melebar. Regresi test murni (BUKAN bug di app) ketahuan dari full-suite run setelah commit stepper idle jadi 1 garis vertikal -- `AddControl` idle sekarang `Row(garis+gap+lingkaran)`, bukan cuma lingkaran tunggal; test lama menaruh `AddControl` langsung sbg child `ListView` lalu tap TITIK TENGAH widget, yang bergeser ke ruang kosong begitu lebar widget berubah. Di app sungguhan `AddControl` SELALU jadi child `Row` biasa (constraint longgar), tidak pernah bare di `ListView` -- murni artefak test. Fix: tap ikon "+" langsung, bukan titik tengah widget.
- `2569a27` — feat: garis idle stepper jadi 1 garis vertikal + tab meta cart bar model indeks. Dua permintaan redesain user (diskusi & mockup dulu sebelum eksekusi, mockup dikirim via screenshot widget SUNGGUHAN bukan mockup HTML, dibangun via `flutter test` + `RepaintBoundary.toImage`): (1) ring putus-putus melingkar (idle stepper, sesi sebelumnya) dinilai "masih terlalu tegas" -- diganti SATU garis putus-putus VERTIKAL di kiri tombol "+" (`_DashedVLinePainter`, `Canvas.drawLine` berulang, jumlah segmen dihitung dari tinggi tersedia bukan angka tetap), warna `outlineVariant` (token pembatas paling samar di app, bukan `onSurfaceVariant` yg setara teks). (2) `_CartMetaTab` (header cart-bar-shrunk: Pelanggan/Pegawai/Tahan/Bayar) diredesain dari SATU tab trapesium tunggal jadi EMPAT tab terpisah model "indeks folder" (`_IndexTab`/`_IndexTabPainter`, tiap tab menjorok sedikit ke tab sebelah kiri via diagonal negatif supaya saling mengunci tanpa celah). Warna aksen terracotta SANGAT LEMBUT (opacity 0.07/0.12/0.17, menguat ke kanan) KECUALI tab "Bayar" yang sengaja dikecualikan (tetap pekat + teks putih) supaya tidak kehilangan penekanan sbg aksi utama (CTA) di antara tab-tab yang kini ikut berwarna. **Bug nyata ditemukan & diperbaiki lewat render preview sungguhan** (bukan cuma dibaca kodenya): versi pertama pakai `IntrinsicHeight` utk menyamakan tinggi tab, tapi `_MetaChip` mengandung `_MarqueeText` berbasis `LayoutBuilder` yang TIDAK MENDUKUNG kueri dimensi intrinsik ("LayoutBuilder does not support returning intrinsic dimensions") -- diganti tinggi eksplisit dihitung dari skala teks sistem. Bug kedua: padding vertikal ganda (`_IndexTab` + `_MetaChip` sendiri) memotong teks nama pegawai -- padding vertikal `_IndexTab` dihapus total (tinggi sudah dipatok pemanggil). Test baru `cart_meta_index_tabs_test.dart` (4) + `add_control_idle_flat_style_test.dart` diupdate (ganti dari ring ke garis vertikal) -- semua revert-verified. `flutter analyze` 0 issue.

## 2026-08-20

- `98ce883`/`4cd7f0c` — revert: perluas konfirmasi getar stepper ke tombol "+" (`b2c3358`/`204a3d9`). User minta dibatalkan setelah dipertimbangkan lagi. `git revert` (bukan reset/force-push, sudah terlanjur di-push & di-merge ke `main` sebelumnya) — `_armedDelta` balik jadi `_armed` (bool), `onTap` "+" balik langsung nambah tanpa gerbang, teks toggle balik ke "Konfirmasi sebelum kurangi qty", 3 test baru + 1 assersi di `cart_minus_confirm_test.dart` ikut hilang. `flutter analyze` 0 issue, test terkait (cart_minus_confirm/add_control_idle_flat_style/kasir_stepper_size/cart_stepper_size) tetap lolos SEMUA.
- `008958d` — feat: stepper "+" idle jadi ring putus-putus netral, flat (tanpa shadow). Keluhan user: stepper idle (belum di keranjang) di grid kasir "terlalu ramai" -- lingkaran solid terracotta + shadow di SETIAP kartu sekaligus menyaingi info produk. Didiskusikan lewat beberapa putaran saran desain (mockup dikirim sbg .jpg utk perbandingan sebelum/sesudah termasuk simulasi grid penuh 6 kartu, dibangun via Playwright/Chromium headless), user pilih kombinasi 3 ide dari beberapa alternatif yang diajukan: (1) netral->berwarna -- idle pakai `onSurfaceVariant`, BUKAN terracotta, baru "hidup" jadi warna brand solid begitu produk BENAR2 masuk keranjang; (2) ring putus-putus (dashed circle) saat idle -- mempertahankan bentuk lingkaran PENUH sbg afordansi tap (area tap TIDAK berubah sama sekali, cuma bobot visualnya) sekaligus memasukkan motif "garis-garis renggang" yang diusulkan user TANPA mengorbankan keterlihatan tombol (beda dari usul awal user yg menghilangkan lingkaran total -- ditolak krn berisiko memperparah missclick yg baru saja diperbaiki sesi sebelumnya); (3) flat total -- `boxShadow` dihapus dari KEDUA state (idle maupun sudah di keranjang), bukan cuma idle. Implementasi: `_DashedCirclePainter` (`CustomPainter` baru) menggambar ring via `Canvas.drawArc` berulang (Flutter tak punya border dashed bawaan) -- jumlah segmen DIHITUNG dari keliling lingkaran (bukan angka tetap) supaya pola menutup 360° tanpa "jahitan" terlihat, konsisten di semua ukuran stepper (32-52px, grid/list/varian/keranjang beda-beda). `AnimatedContainer` mainCircle: `color` jadi transparent saat idle, tetap solid `bgColor` begitu di keranjang; `boxShadow` dihapus total. Ikon "+" pakai `idleColor` (`onSurfaceVariant`) saat idle, `mainFg` (putih/gelap) spt sebelumnya saat di keranjang. Test baru `add_control_idle_flat_style_test.dart` (2: idle -- transparent+no-shadow+ring dashed tampil+ikon bukan putih; di-keranjang -- solid+no-shadow+ring TIDAK digambar lagi) -- revert-verified (fill & warna ikon dikembalikan ke behavior lama -> test pertama gagal sensibel: Expected transparent, Actual 0xffc96442). Regresi: seluruh file test `add_control_*`/`kasir_stepper_size_test.dart` (7 file) tetap lolos tanpa perubahan. Full suite 1117 lolos SEMUA (termasuk `proposal_unchanged_end_to_end_test.dart` yg biasa flaky, kebetulan ikut hijau run ini), `flutter analyze` 0 issue.
- `c1d14f6` — feat: perbesar stepper +/- di kartu grid/list/varian layar Kasir. Keluhan missclick sama seperti stepper baris keranjang (`cart_sheet.dart`, sesi lalu) ternyata juga berlaku di kartu/baris produk layar Kasir. Diperbesar dgn delta +14 YANG SAMA (bukan rasio baru) supaya konsisten di seluruh app: kartu grid 32->46, baris list 34->48, baris varian 28->42. `mainAxisExtent` grid (138->152) ikut ditambah persis +14 supaya kartu tidak overflow -- baris list & varian aman tanpa penyesuaian krn tingginya mengikuti isi (`Row` biasa, bukan tinggi tetap). Test baru `kasir_stepper_size_test.dart` (3: grid/list/varian, masing2 assert `AddControl.size` & tidak overflow di HP sempit 360x800 dgn nama produk panjang) -- revert-verified (ketiga size dikembalikan ke nilai lama -> ketiga test gagal sensibel, mis. "Expected: <42> Actual: <28.0>"). Regresi: 18 file test kasir/sync terkait lain tetap lolos. Full suite 1113 lolos + 2 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `fb1e317` — fix: swipe-turun bisa tutup sheet QRIS statis & toggle QR nota walau overflow. Keluhan user: sheet bayar tempo (state QRIS statis, QR+keypad sekaligus) dan sheet Bagikan Struk (toggle QR nyala) tidak bisa ditutup via swipe turun -- padahal state lain di sheet yang sama bisa. Akar: `showModalBottomSheet` bawaan cuma menghubungkan swipe-turun ke `Navigator.pop` kalau isi TIDAK scrollable. Begitu `SingleChildScrollView` di dalamnya benar-benar overflow, gesture arena SELALU dimenangkan scrollable itu -- dismiss-drag bawaan tidak pernah kebagian giliran, terlepas dari posisi scroll. Limitasi umum `showModalBottomSheet`+`SingleChildScrollView` polos (beda dari `DraggableScrollableSheet` yang punya wiring sendiri) -- kena di KEDUA sheet dgn pola yang sama, bukan bug spesifik satu tempat. Fix: `NotificationListener<ScrollNotification>` membungkus tiap `SingleChildScrollView` di `debt_payment_sheet.dart` & `receipt_screen.dart` (`_showShareSheet`), mengakumulasi `OverscrollNotification` (tetap terkirim walau `ClampingScrollPhysics` — posisi tidak bergerak tapi notifikasi tetap ada) SELAMA satu gesture drag; begitu tarikan ke bawah melewati ambang -60px, panggil `Navigator.maybePop()` manual, reset saat `ScrollEndNotification`. Meniru wiring internal `DraggableScrollableSheet` TANPA mengubah struktur layout (jarak keypad->tombol permanen dari Item 62 lalu TETAP tidak berubah). Test baru di kedua file — masuk ke state overflow, drag turun pada `SingleChildScrollView`, assert sheet ter-pop — revert-verified 2x (ambang dinaikkan jadi mustahil tercapai -> kedua test gagal sensibel). Full suite 1111 lolos + 1 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `198518f` — feat: QR pelunasan di nota share & cetak (nota tempo/kurang_bayar). Poin ke-2 dari 4 permintaan susulan user. Sheet "Bagikan Struk" (`receipt_screen.dart`) dapat toggle "Tampilkan QR Pelunasan" -- muncul HANYA utk nota `tempo`/`kurang_bayar` yang punya metode QRIS aktif terkonfigurasi (`_eligibleForShareQr`); nota lunas/void/retur: toggle tidak ditawarkan sama sekali, sesuai konfirmasi eksplisit user ("jika nota sudah lunas, toggle ini praktis tidak ada"). Menyalakannya menampilkan toggle kedua "QR Dinamis" (default ON); QR dirender di `_ReceiptPaper` di atas footer struk, tanpa nominal terpisah di atasnya (beda dari `_QrisDisplay` checkout) -- nominal sudah ada di baris "Sisa" nota itu sendiri. Nominal QR dinamis = SISA TAGIHAN (`netRemainingOwed`), BUKAN total nota, persis dikonfirmasi user. Caption "Mohon konfirmasi setelah membayar." ditambahkan di bawah QR, baik di gambar struk maupun cetak thermal. Kedua toggle persisten via SharedPreferences (`receipt_show_qr`, `receipt_qr_dynamic`) -- pola sama dgn "Tampilkan Laba" yang sudah ada di file ini, preferensi TAMPILAN milik device yang membuka struk, bukan data per-transaksi, tidak perlu ikut sync. Nilai yang sama dipakai saat cetak thermal (`_resolvePrintQrData`, dipanggil `_printReceipt`) -- state toggle konsisten antara share & cetak sesuai permintaan poin 2 ("ikutkan ke print struk"). `PrinterService.printReceipt`/`_buildBytes` dapat param baru `qrData`, dicetak native via `Generator.qrcode` (ESC/POS `GS ( k`) -- risiko printer murah mengabaikan command QR BELUM diverifikasi cetak nyata (dicatat di HANDOFF). Nota GABUNGAN sengaja TIDAK dapat opsi ini (dikonfirmasi user: pelunasan tempo utk sekarang cukup lewat Laporan > Hutang, nota gabungan murni info total belanja lunas). Reuse `resolveQrisPayload`/`QrisQrBox` dari `payment_qris_view.dart` (dibangun sesi sebelumnya) -- tidak ada logika QRIS baru yang diketik ulang. Test baru `receipt_screen_qr_toggle_test.dart` (4: toggle muncul+QR+caption di nota tempo+QRIS aktif; toggle absen total di nota lunas; toggle absen kalau belum ada metode QRIS; persistensi toggle antar buka-tutup sheet) -- revert-verified 2x (gerbang eligibilitas dipaksa `true` -> bocor ke nota lunas, gagal sensibel; nilai persisted `qrDynamic` diabaikan -> gagal sensibel). 49 test struk terkait lain (regresi) tetap lolos. Full suite 1109 lolos + 1 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `6697931` — docs: catat toggle tema aktivasi, sheet bayar tempo, stepper diperbesar + poin QR nota tempo yg menggantung.
- `29e8ea0` — feat: perbesar stepper +/- baris keranjang (mitigasi missclick). Keluhan user: tombol stepper baris keranjang (`cart_sheet.dart`) masih sering missclick. Pijakan jempol diperbesar 30->44px (+-45%, sengaja sedikit di bawah "setengah lebih besar" literal/1.5x supaya nama produk 2-baris di sebelahnya tetap muat wajar di HP sempit). Stepper LAIN (grid/list produk, baris varian di `kasir_screen.dart`) SENGAJA tidak ikut diperbesar -- itu utk menambah item baru ke keranjang, bukan titik missclick yang dikeluhkan (mengubah qty transaksi yang sudah ada). User eksplisit ingin diskusi lebih dalam soal root cause/desain alternatif nanti -- perubahan ini murni mitigasi cepat, dicatat di HANDOFF supaya tidak dianggap topik selesai. Test baru `cart_stepper_size_test.dart` (ukuran + no-overflow di HP sempit 360x800 dgn nama produk panjang) -- revert-verified. Full suite 1104 lolos + 2 gagal (`proposal_unchanged_end_to_end_test.dart` + saudaranya `laci_meja_proposal_unchanged_end_to_end_test.dart`, keduanya flaky family, lolos sendiri terisolasi, tidak terkait perubahan ini), `flutter analyze` 0 issue.
- `d9b3a31` — feat: toggle terang/gelap di layar aktivasi + rapikan kalkulator bayar tempo. Poin 1+3 dari 4 permintaan susulan user (lihat `198518f` utk poin 2, `29e8ea0` utk poin ini yg terpisah/stepper). (1) Layar gerbang aktivasi (serial key, `aktivasi_screen.dart`) dapat tombol mode terang/gelap -- layar ini muncul SEBELUM `/setup`/DB bisa dibuka, jadi Pengaturan (tempat mode gelap biasa diatur) belum terjangkau. `themeMode` dibaca/ditulis via `themeModeProvider` (SharedPreferences, bukan tabel settings DB, sudah aman dipakai di titik ini). Ikon & tooltip menyebut TUJUAN (bulan = "ke gelap"), bukan keadaan sekarang. Menyusul BUG PRE-EXISTING yang ditemukan saat menguji tombol ini (dibuktikan via probe: gagal juga kalau tema diganti langsung lewat provider, TANPA menyentuh tombol) -- akarnya `QrImageView` (paket `qr_flutter` 4.1.0) melempar assert framework `'debugSize == size'` (text_painter.dart) kalau tema berganti selagi QR terlihat; selama ini tak pernah terjangkau krn dari layar aktivasi memang belum ada cara ganti tema. Fix: `key` yang ikut brightness ditaruh di `QrisQrBox` (shared, SEMUA QR app ikut aman) + `KeyedSubtree` di seluruh body layar aktivasi. (2) Sheet bayar nota tempo (`debt_payment_sheet.dart`) dirapikan 2 hal: (a) jarak keypad->baris tombol kini PERMANEN -- diganti dari `DraggableScrollableSheet` (pecahan tetap layar) ke `Column(mainAxisSize.min)` + `Flexible`+scroll (tinggi ikut isi, tombol mengalir tepat di bawah keypad, persis pola kalkulator checkout), `_scroll` dipindah jadi controller yang di-`dispose()` State sendiri; (b) label tombol switch QR dibalik -- sekarang menyebut TUJUAN bukan keadaan sekarang (state statis -> tertulis "Nominal"), sesuai koreksi eksplisit user ("teks yang ditampilkan adalah destinasi selanjutnya, bukan current state"). Toggle QR checkout (`Switch`) TIDAK ikut diubah -- widget itu memang lazimnya menyatakan keadaan sekarang. Test baru `aktivasi_theme_toggle_test.dart` (4: toggle ada+label destinasi, tap ganti brightness, ganti tema selagi QR terlihat tidak throw/regresi bug qr_flutter, persistensi ke SharedPreferences) + penyesuaian `debt_payment_sheet_test.dart` (test gap keypad->tombol, before/after metadata muncul) -- revert-verified. `flutter analyze` 0 issue.

## 2026-08-18

- `d7a7bad` — fix: metode Transfer Bank salah masuk bucket "Lainnya" di semua laporan (bug ditemukan saat audit tab Ringkasan). Akar: `AppDatabase._paymentBucket` mencocokkan string literal `'transfer'` yang TIDAK PERNAH ada di data nyata — nilai `type` yang sungguhan tersimpan di `transactions.paymentMethod` untuk transfer bank adalah `'bank'` (lihat dropdown `payment_methods_screen.dart`). Sama untuk `_methodLabel`/`_methodShort` di `receipt_screen.dart` (2x), `transaksi_tab.dart`, `printer_service.dart` — SEMUANYA switch pada string mentah `tx.paymentMethod`/`method`, jadi metode bank tampil sbg "bank" apa adanya (fallback `_ => m`) di struk/riwayat/cetak thermal, bukan "Transfer". Ditemukan juga spot ke-5 yang sama sekali belum ke-cover audit awal: `arus_kas_tab.dart` `_methodLabels` (key lokal ke `getCashInByMethod` yang GROUP BY langsung kolom `method` mentah) — sama-sama key `'transfer'` mati. Fix: ganti seluruh case/key `'transfer'`->`'bank'` di ke-5 lokasi yang switch pada RAW method string. TIDAK diubah: `ringkasan_tab.dart`/`report_export.dart` yang key `'transfer'`-nya adalah nama bucket INTERNAL (`byMethod['transfer'] = ...pembayaranTransfer`, decoupled dari raw method) — sudah benar sejak awal begitu bucket sumbernya (`_paymentBucket`) diperbaiki. E-wallet & Tempo TETAP sengaja masuk bucket "Lainnya" (bukan bug, desain 4-bucket `DailySummaries` yang sudah didokumentasikan tabelnya sendiri — bukan diputuskan sesi ini, hanya dikonfirmasi masih berlaku). Test baru `payment_bucket_transfer_bank_test.dart` (DB murni, 5 metode sekaligus: tunai/qris/bank/ewallet/tempo) — revert-verified (case `'bank'` dikembalikan ke `'transfer'` sengaja -> assersi bucket Transfer gagal sensibel 50rb jadi 0). Full suite 1100, `flutter analyze` 0 issue.
- `48fa921` — feat: sheet pelunasan hutang gaya kalkulator checkout (QRIS statis/dinamis + metadata). Permintaan user, dirancang bareng lewat diskusi desain sebelum eksekusi. `debt_payment_dialog.dart` (AlertDialog sempit) DIHAPUS, diganti `debt_payment_sheet.dart` — dipakai di SELURUH 4 pemanggil (`receipt_screen`, `tx_history_sheet`, `transaksi_tab`, `hutang_tab` yang gabungan-nota via `settleMergedDebt` FIFO), bukan ditambah berdampingan, supaya tidak ada 2 UI berbeda utk aksi identik. Alasan pindah dari dialog: `AlertDialog` dibungkus `IntrinsicWidth` & lebarnya dipotong inset/content padding — QR 200px + metadata + keypad TIDAK muat (file dialog lama sendiri sudah pernah kena overflow 3-tombol). Isi sheet: (a) keypad + chip pecahan + pill Kembalian/Sisa, tata letak & warna SAMA dgn kalkulator checkout; (b) chip metode bayar di dalam sheet — memilih metode non-tunai meng-collapse chip lain (AnimatedSwitcher+AnimatedSize) & chip terpilih bergeser kiri, metadata (no. rekening/HP) + tombol salin tampil SEGARIS di kanannya; ketuk lagi chip terpilih → batal, balik ke Tunai. Chip Tunai SENGAJA tidak pernah meng-collapse (ia gerbang masuk metode lain, dan tak punya metadata utk ditonjolkan); (c) QRIS: default SELALU STATIS (beda dari checkout yang totalnya sudah pasti — di pelunasan justru nominalnya yang belum diketahui, kasir mengetiknya dulu), keypad tetap ada, QR statis diletakkan di ATAS nominal tapi di-scroll lewati otomatis (`_scrollPastQrBlock`) supaya yang terlihat lebih dulu tetap keypad — kasir menggulir ke atas saat mau menunjukkan QR; geser toggle ke "Nominal" → keypad disembunyikan & nominal yang tadi diketik TERPATRI di QR. Tombol "Uang Pas" DIGANTI toggle Statis/Nominal khusus saat QRIS (QR dinamis nominalnya sudah pas by definition; statis justru dipakai utk nominal bebas) — "Uang Pas" dipindah jadi chip pecahan supaya tidak hilang. Nominal TIDAK tereset saat toggle bolak-balik (salah pencet aman). Keputusan yang diambil sendiri & didokumentasikan: geser ke dinamis SEBELUM mengetik apa pun → QR & nota pakai sisa penuh (`_qrAmount`), krn QR bernominal nol tidak sah. Perilaku checkout "QRIS Nominal → lewati kalkulator, langsung lunas" SENGAJA TIDAK dibawa ke sini — cicilan sebagian via QRIS harus tetap bisa. Widget QR/metadata diekstrak ke `payment_qris_view.dart` (`resolveQrisPayload`, `QrisQrBox`, label/ikon/salin) dipakai bersama checkout supaya tidak menyimpang diam-diam; TATA LETAK sengaja tetap lokal krn keduanya memang beda. Test `debt_payment_sheet_test.dart` ditulis ulang total (14, dari 6 test dialog lama — kontrak `(amount, method)` tetap sama) termasuk uji HP sempit 360x800 dgn QR statis + keypad sekaligus — revert-verified 2x (`_qrAmount` dipaksa selalu sisa penuh → test nominal-terbawa gagal sensibel 30rb vs 50rb; gerbang collapse dimatikan → test chip gagal sensibel). `tx_history_net_sisa_test.dart` disesuaikan ke presentasi baru (nominal sisa dirender 2 `Text` terpisah, bukan `Text.rich`, supaya tetap bisa di-assert — angka ini yang dicatat sbg pembayaran). Full suite 1097 lolos + 2 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `8862b48` — feat: QRIS mode Nominal lewati kalkulator, langsung lunas penuh. Permintaan user: "untuk mode toggle ke qr dinamis, itu tidak perlu buka kalkulator. Perlakukan seperti sebelum perubahan kasir diterapkan sebelumnya (bayar, langsung lunas)... Untuk QR statis, tetap munculkan kalkulator." Alasannya kuat: di mode Nominal jumlahnya SUDAH dipatri di dalam QR yang discan pelanggan (tidak bisa dikurangi saat bayar), jadi kalkulator cuma langkah tambahan tanpa guna. `_onBayarPressed` sekarang punya cabang awal `_isQrisNominalLocked` (`_selectedMethodType == 'qris' && _qrisDynamic`) → set `_tendered = _total` lalu langsung `_confirm()`, TANPA membuka `_CashKeypadSheet` — persis perilaku non-tunai sebelum Item 62 (selalu lunas exact). QRIS mode **Statis** TETAP lewat kalkulator (pelanggan mengetik sendiri jumlahnya di app e-wallet, jadi bisa saja tidak penuh → tetap perlu kasir input nominal sungguhan). Metode non-tunai lain (bank/e-wallet) TIDAK berubah — tetap pakai kalkulator, krn di sana nominal memang tidak pernah terkunci di mana pun. Efek samping bagus: celah "nominal QR vs nominal ketikan kasir bisa beda" (yang 2x saya angkat ke user sbg pertanyaan menggantung) jadi TIDAK MUNGKIN terjadi di mode Nominal — QR dan nota dijamin sama-sama `_total`. Test `payment_screen_qris_dynamic_test.dart` +2 (mode Nominal: tap Bayar tidak memunculkan kalkulator sama sekali & nota tersimpan `lunas` paid=total change=0; mode Statis: tap Bayar TETAP memunculkan kalkulator lengkap badge "Metode: QRIS" & belum ada nota tersimpan) — revert-verified (gerbang dimatikan sengaja → test mode Nominal gagal sensibel krn "Uang Pas" muncul, test mode Statis tetap hijau = kedua cabang benar-benar dibedakan). Full suite 1091 lolos semua, `flutter analyze` 0 issue.
- `1df5145` — feat: QRIS nominal jadi fitur tetap + toggle Statis/Nominal di kartu QR. User sudah menguji bayar SUNGGUHAN lewat fitur sebelumnya dan dana masuk normal, jadi: (1) label "Eksperimental" DIBUANG dari kartu QR checkout, caption diringkas jadi info operasional saja ("nominal terkunci, pelanggan tidak perlu mengetik jumlah; pembayaran tetap dikonfirmasi manual oleh kasir") — peringatan "bukan server resmi/tanpa kedaluwarsa" dipindah ke dok kode (`qris_dynamic.dart`), tidak lagi dipajang ke kasir tiap transaksi; (2) toggle `Switch` di POJOK KANAN ATAS kartu QR utk berpindah Statis ↔ Nominal, dgn label teks yang ikut berubah; (3) penegasan permintaan user "app ini dipakai banyak toko, acuan pola dinamis = value QR statis yang diinputkan" — ini SUDAH benar sejak implementasi awal (`injectQrisAmount` cuma menyentuh tag `01`/`54`/`63`, sisanya diteruskan apa adanya), sekarang dibuktikan eksplisit lewat test payload merchant BERBEDA (acquirer/NMID/nama/kota/kode pos beda total) yang identitasnya harus lolos utuh. Pilihan toggle PERSISTEN di setting `qris_dynamic_enabled` (default ON bila belum pernah diisi) — bukan state layar sesaat, supaya kasir tidak perlu menggeser tiap transaksi; krn `app_settings` ikut kategori sync masterData 'Pengaturan Toko', pilihan owner otomatis menyebar ke perangkat kasir. Tanpa migrasi DB (key-value). Test: `qris_dynamic_test.dart` +1 (generalitas merchant lain, payload fixture dibangun lewat helper `tlv()` supaya panjang TLV DIHITUNG bukan diketik manual — 2x kejadian salah hitung manual bikin fixture rusak & test menguji hal yang salah), `payment_screen_qris_dynamic_test.dart` +2 (toggle ke Statis → nominal/caption hilang & setting tersimpan `'0'`; setting `'0'` dari awal → QR terbuka mode statis) — revert-verified (gerbang `dynamicMode` diabaikan sengaja → kedua test toggle gagal sensibel). Full suite 1088 lolos + 1 gagal (`proposal_unchanged_end_to_end_test.dart`, flaky pre-existing, dikonfirmasi lolos sendiri terisolasi), `flutter analyze` 0 issue.
- `ccb8a1e` — feat: QR QRIS sisipkan nominal otomatis (EKSPERIMENTAL), payload GoPay asli buat validasi. Permintaan user setelah diskusi analisis (dijawab dulu murni via riset/matematika sebelum coding — sengaja tanpa generate QR sungguhan, classifier keamanan sandbox memblokir aksi itu): "implementasikan saja di aplikasi untuk saya test. Tandai fitur ini sebagai eksperimental." Latar: user kirim 3 payload QRIS GoPay ASLI dari HP-nya sendiri (1 statis, 2 dinamis Rp17rb/Rp20rb, toko nyata) — dipakai sbg fixture pembuktian, BUKAN data sintetis. Terbukti (lihat `test/qris_dynamic_test.dart`): CRC ketiganya valid; beda statis→dinamis PERSIS 3 tag — `01` (`11`→`12`), `54` (nominal, disisipkan tepat setelah tag `53`), `63` (CRC dihitung ulang) — field lain (merchant/NMID/nama toko/kota di tag `26`/`51`/`52`/`53`/`58`-`61`) identik byte-per-byte. Tag `62` sub-`50` (token+timestamp proprietary GoPay, dipakai server mereka utk menegakkan "1x transaksi"/kedaluwarsa) SENGAJA tidak direplikasi — di luar spek EMVCo standar (rentang sub-tag `50-99` reserved vendor), tidak memengaruhi keabsahan CRC/payload. Implementasi baru: `lib/core/utils/qris_dynamic.dart` (CRC-16/CCITT-FALSE murni Dart — divalidasi thd check value baku `crc16("123456789")=="29B1"` — + parser/builder TLV EMVCo + `injectQrisAmount`). `_QrisDisplay` (`payment_screen.dart`) sekarang terima `total`, coba `injectQrisAmount(qrValue, total)` — berhasil → render QR ber-nominal + badge "Eksperimental" + caption peringatan (bukan lewat server resmi, TIDAK ada verifikasi otomatis/kedaluwarsa, kasir tetap konfirmasi manual); gagal parse (`QrisTlvException`, mis. `qrValue` bukan format EMVCo) → fallback DIAM-DIAM ke QR statis polos, checkout tidak boleh gagal krn fitur eksperimental ini. Test baru `qris_dynamic_test.dart` (6, Tier 1, fixture payload asli — termasuk assersi field non-nominal identik dgn dinamis asli) + `payment_screen_qris_dynamic_test.dart` (2, widget, termasuk fallback payload rusak) — revert-verified (CRC poly disalahkan sengaja → gagal sensibel pd payload asli; wiring `_QrisDisplay` dimatikan sengaja → badge/nominal hilang sensibel). Full suite 1086, `flutter analyze` 0 issue.
- `1596283` — feat: kalkulator bayar dipakai sama utk metode non-tunai + tampilkan metadata rekening/akun. Permintaan user: "Saat checkout awal, reuse kalkulator dan persiskan dengan pembayaran tunai untuk pembayaran non tunai. Berikan UI penanda di kalkulator juga untuk state dan informasi tentang pembayaran apa yang dilock. Untuk layar checkout, tampilkan juga metadata." Sebelumnya `_paid` di `payment_screen.dart` dipaksa `= _total` utk semua metode selain `tunai` — tidak ada keypad sama sekali utk QRIS/bank/e-wallet, jadi nominal kurang/cicilan HANYA bisa dicatat lewat "Tambah Bayar" pada nota yang sudah dibuat (lihat riset Q2 sesi ini). Sekarang `_onBayarPressed` membuka `_CashKeypadSheet` yang SAMA utk semua metode selain tempo (tempo tetap pakai tombol "Bayar Nanti" dedicated), dan `_paid` disederhanakan jadi `_tendered` langsung — tempo tetap 0 lewat guard `isTempo` terpisah di `_confirm`/`_confirmAddItems`, tidak berubah. Ganti metode via `ChoiceChip` mereset `_tendered = 0` supaya nominal metode sebelumnya tidak nyasar ke metode baru. `_CashKeypadSheet` dapat parameter baru `methodName` + badge "Metode: <nama>" (ikon gembok) di atas sheet — penanda metode mana yang "dikunci" utk sesi input nominal itu (relevan skrg krn sheet dipakai bergantian, dulu implisit selalu tunai). Widget baru `_PaymentMetadataDisplay` (pola sama `_QrisDisplay`) menampilkan `PaymentMethods.data` (no. rekening/no. HP-akun, sudah lama tersimpan tapi TIDAK PERNAH ditampilkan ke kasir — dikonfirmasi lewat riset codebase sebelum implementasi) sbg Card + tombol salin saat metode `bank`/`ewallet` dipilih. Test baru `payment_screen_noncash_keypad_test.dart` (2: metadata tampil di layar checkout; kalkulator non-tunai membuka dgn badge kunci & mengizinkan nominal kurang → status `kurang_bayar`) — revert-verified (`_paid` dikembalikan ke kondisi lama, assersi status gagal sensibel: `lunas` bukan `kurang_bayar`). Full suite 1077, `flutter analyze` 0 issue.

## 2026-08-14

- `d805143` — feat: dialog Tahan Pesanan pakai dropdown pelanggan (sama cart bar shrinked). Permintaan user: dialog isi label "Tahan Pesanan" (belum ada pelanggan terpilih) tadinya cuma `AlertDialog` polos dgn satu `TextField` bebas — sekarang dipakai ulang PERSIS dropdown "Pelanggan" cart bar shrinked (`showCustomerPickerSheet`, `cart_meta_pickers.dart`): cari pelanggan terdaftar (lengkap atribut hutangnya) atau ketik nama manual. Pelanggan terdaftar yang dipilih ikut disematkan ke `cartMetaProvider` (`setCustomer`), bukan cuma dipakai sbg teks label — payload `held_order` jadi bawa `customerId` sungguhan, konsisten dgn nota checkout normal. "Umum" (tanpa nama) tetap ditahan pakai label default "Pesanan", sama persis perilaku lama saat field label dikosongkan. `_HoldLabelDialog` (bikinan sesi sebelumnya) dihapus total — tidak diperlukan lagi krn logikanya sekarang di-reuse dari `cart_meta_pickers.dart` yang sudah lama benar pola dispose-nya. Test `cart_sheet_hold_button_test.dart` diperbarui (8 test, termasuk baru: atribut hutang tampil di dropdown, `customerId` ikut tersimpan di payload) — revert-verified. Full suite 1073 (2 gagal `proposal_unchanged_end_to_end_test.dart`, flaky pre-existing tidak terkait, dikonfirmasi lolos sendiri), `flutter analyze` 0 issue.
- `aa814d6` — feat: tombol Tahan Pesanan di header keranjang, di samping Tempel Pesanan. Permintaan user: "Tahan Pesanan" sebelumnya cuma bisa lewat tab folder pelanggan/pegawai di `kasir_screen.dart` (`_CartMetaTab`/`_holdCurrent`), yang tersembunyi begitu sheet keranjang dibuka. Tombol baru ditaruh di header `CartSheet` (icon `pause_circle_outline`), di kiri "Tempel Pesanan" — gerbang SAMA PERSIS dgn `_CartMetaTab` (cuma `kMainCartId`; mode Katalog bukan transaksi sungguhan, mode Tambah Belanjaan ikut transaksi asli yang tidak bisa ditahan terpisah). Logika (`_holdCurrent`/`_askHoldLabel`) SENGAJA disalin — bukan dibagi lewat fungsi bersama — krn versi `kasir_screen.dart` terikat state layar itu sendiri (`_heldPanelOpen`, banner kustom). Susulan: dialog input label dipindah dari `TextEditingController` manual (dibuat di fungsi, di-dispose di `finally` setelah `showDialog` resolve) ke `StatefulWidget` `_HoldLabelDialog` dgn `dispose()` sendiri — pola lama ketangkap widget test menyebabkan "TextEditingController was used after being disposed" (dialog ditutup sebelum animasi keluarnya benar-benar selesai, spesifik pada dialog yang dibuka dari dalam `DraggableScrollableSheet`), pola baru sama dgn `debt_payment_dialog.dart`. Test `cart_sheet_hold_button_test.dart` (6) — revert-verified. Full suite 1071 (2 gagal `proposal_unchanged_end_to_end_test.dart`, flaky pre-existing tidak terkait, dikonfirmasi lolos sendiri), `flutter analyze` 0 issue.
- `024bf32` — chore: naikkan versi ke 2.11.0+16.
- `0419df5` — fix: perkecil payload gambar QR transfer transaksi, Copy/Share tetap lengkap. Pertimbangan user: QR menumpuk/padat & sulit di-scan kamera. Akar: gambar QR, tombol Salin, dan Share semuanya memakai satu string yang SAMA persis dari `encodeHandoff()` — termasuk blok manusiawi "PESANAN — toko / daftar produk / Total" yang TIDAK PERNAH ikut di-parse balik (parser cuma baca baris `#PSN:...` + baris meta lewat regex per-baris, blok itu murni beban di gambar QR tanpa manfaat fungsional). Fix: pisah jadi 2 string dari fungsi yang SAMA — gambar QR sekarang pakai `encodeHandoff(storeName: null)` (cuma kode mesin + meta, modul lebih rendah/gampang di-scan), tombol Salin/Share TETAP teks lengkap (`storeName` terisi) supaya enak dibaca kalau ditempel manual WhatsApp/Telegram. Tidak ada perubahan sisi scan/parse — behaviornya identik krn bagian yang dibuang dari QR memang tidak pernah dibaca. Test `kasir_handoff_qr_test.dart` diperbarui (assersi lama "Copy = isi QR persis sama" → "Copy tetap lengkap, beda dari QR yang diperkecil" + assersi baru clipboard mengandung blok "PESANAN — <toko>") — revert-verified.

## 2026-08-13

- `5c46846` — feat: rincian per-produk retur/edit + Sisa per sesi bayar di Riwayat Pembayaran (schemaVersion 32). Permintaan user: kartu "Riwayat Pembayaran" in-app WAJIB menampilkan (1) rincian per-produk tiap momen retur/edit — nama produk (bold) + baris `qty satuan x harga  total` (bold), beberapa produk dalam satu momen tampil berurutan; (2) baris "Sisa" per sesi bayar pada nota tempo/kurang_bayar (pola sama "Kembalian" tapi tanpa centang, krn sisa tempo tidak dipakai ulang); (3) kembalian/sisa yang berlaku PERSIS pada momen retur/edit itu sendiri (klarifikasi user: poin 3 "tersebut" merujuk poin 1). Tabel audit baru `transaction_adjustment_lines` (insert-only, snapshot nama produk/satuan) ditaut ke baris `transaction_payments` via `paymentId`, diisi di 4 fungsi mutasi (`returnUnpaidTransactionItems`, `editUnpaidTransactionItem`, `returnPaidTransactionItems`, `editPaidTransactionItem`) — utk 2 fungsi edit yang qty/harga-nya bisa berubah TANPA delta murni qty, dipakai formula derivasi (`subtotalForLine` eksak dari selisih nilai, `qtyForLine`/`priceForLine` diturunkan darinya) supaya `qty×harga≈subtotal` tetap konsisten. Edit yang cuma ubah catatan (qty/harga sama persis) TIDAK menghasilkan baris rincian. Kolom baru `TransactionPayments.sisaAfter` — simetris dgn `changeGiven` yang sudah ada (`_computePaymentDelta`, gantikan `_computePaymentChangeGiven`): `thisChange > 0` → `changeGiven`, `thisChange < 0` → `sisaAfter` (WAJIB pakai `thisChange` yang sudah dikurangi `priorChangeSum`, bukan `aggregateChange` mentah — kembalian lama yang dipakai ulang sbg pembayaran baru harus dinetkan di KEDUA sisi, bug sempat lolos ke `receipt_sisa_tagihan_net_test.dart` sebelum diperbaiki). Diwire ke `addPaymentToTransaction`, `settleMergedDebt` (per-nota, FIFO), `backfillMissingPayments` (migrasi data lama) selain 4 fungsi retur/edit. Sync LAN: `transaction_adjustment_lines` ditambahkan ke `appendOnlyTables`/`syncCategories`/`dumpSince`/`_allTables`/`childTables` (`lan_sync_service.dart` & `app_database.dart`). UI murni di `_buildPaymentTimeline`/`_AdjustmentLinesBlock` (`receipt_screen.dart`) — **in-app SAJA**, nota share (`_ReceiptPaper`) & cetak (`printer_service.dart`) sengaja TIDAK disentuh (scope eksplisit user). Test `adjustment_lines_sisa_test.dart` (8, DB murni, 4 fungsi mutasi + `addPaymentToTransaction` + `settleMergedDebt`) — revert-verified 2x (sisaAfter formula & guard baris rincian catatan-murni). 16 test migrasi lama (v7-v31) diperbarui: seluruhnya diupgrade sampai schemaVersion terkini (32), 9 fixture yg belum punya tabel `transaction_payments` (dibutuhkan migrasi v32) ditambahkan, assersi versi akhir disesuaikan.

## 2026-08-11 (lanjutan lagi — chart interaktif)

- `5115832` — fix: ganti chart tren produk jadi LineChart interaktif, tahan rentang panjang. Bug dilaporkan user (screenshot): rentang tanggal panjang (mis. setahun, ~365 titik harian) di statistik produk membuat label sumbu-X pecah jadi tumpukan karakter vertikal. Akar: `StatsDailyBarChart` me-layout satu `Expanded` per titik data — makin banyak titik makin sempit tiap kolom sampai teks tidak muat & tiap karakter pindah baris sendiri. Diganti `StatsTrendChart` berbasis fl_chart `LineChart` (dependency yang sudah ada, sebelumnya cuma dipakai utk `PieChart`): label sumbu-X dipasang lewat `interval` fl_chart (dihitung dari skala X sungguhan, bukan dibagi rata per titik) — jumlah titik berapa pun, jumlah label tetap terbatas. `trend_aggregation.dart` (fungsi murni) mengelompokkan titik harian jadi mingguan/bulanan begitu melebihi ambang 60 titik, nilai per kelompok DIJUMLAH (bukan dirata-rata). Interaktif ala trading (permintaan user): `LineTouchData` bawaan fl_chart — tap/drag di garis memunculkan tooltip tanggal+nilai + indikator garis putus-putus & titik mengikuti sentuhan. Scope sengaja HANYA `StatsTrendChart` (ProductStatsScreen) — tidak menyentuh chart pengeluaran/ringkasan/arus kas lain. Test `trend_aggregation_test.dart` (8, murni Dart) + `stats_trend_chart_test.dart` (6, termasuk kasus 365 titik yg dulu overflow) — revert-verified.

## 2026-08-11 (lanjutan — batch fitur & sync)

- `d300b16` — feat: sinkronkan setting toko, metode bayar & pegawai antar device. Temuan audit: `app_settings`/`payment_methods`/`employees` TIDAK PERNAH ikut sync. Paling berbahaya: aturan poin loyalti (`loyalty_points_per`/`loyalty_point_threshold`) dibaca SAAT CHECKOUT → nota bernilai sama dapat poin BERBEDA tergantung device, padahal `loyalty_point_ledger` ikut tersync (inkonsistensi masuk data bersama). Juga `allow_negative_stock`, identitas toko di struk (`store_name` selama ini cuma ikut SEKALI saat pairing), tampilan struk, metode bayar & pegawai. `app_settings` TIDAK di-dump bulat-bulat — bercampur identitas/state device (`store_uuid`/`store_key`/`device_code`/`device_role`/watermark/`last_archive_date`); dipakai ALLOWLIST `AppDatabase.syncableSettingKeys` yang disaring di DUA sisi (dump host + `mergeRows` penerima, guard penerima berdiri sendiri). Tetap SATU ARAH host→klien. Kategori approval "Pengaturan Toko" baru. Test `settings_sync_test.dart` (8) — revert-verified (tanpa guard, `store_key`/`device_code` benar² ikut tertulis).
- `ba0a6fc` — feat: Penerimaan Barang via tempel teks + kamus belajar tersinkron (schemaVersion 31). Output tools cek-stok HTML eksternal user bisa langsung ditempel; setelah contoh file ditinjau disepakati ini PENERIMAAN (qty MENAMBAH stok), BUKAN opname (MENIMPA) — jadi alur tersendiri. Parser `receive_text_parser.dart` (`qty satuan nama`, header tanggal diabaikan, koma/titik desimal; baris tanpa satuan sengaja meninggalkan satuan KOSONG supaya kata pertama nama tidak salah terbaca sbg satuan; baris gagal-parse ditampilkan, tidak dibuang diam-diam). Pencocokan PERSIS saja — TIDAK ada fuzzy (konsisten larangan Levenshtein di CLAUDE.md); urutan: kamus kunci ber-satuan → kamus fallback tanpa satuan → nama produk persis (hanya bila tunggal). Ambigu diselesaikan lewat dropdown BERPENCARIAN, pilihannya diingat. Kamus = tabel baru `product_aliases` kunci 2 tingkat (pola `nkLearn` tools user), **tersinkron DUA ARAH** lewat konsep `LanSyncService.sharedTables` baru (dump selalu disertakan termasuk klien→host, guard host dilonggarkan KHUSUS tabel ini, merge LWW `updated_at`). Layar `receive_goods_screen.dart` + kelola kamus, rute `/produk/penerimaan`, tombol di AppBar Cek Stok. Test: `receive_goods_test.dart` (16), `receive_goods_screen_test.dart` (4), `migration_v31_test.dart`; 15 test migrasi lama diupdate 30→31.
- `7c89ca1` — feat: tab Arus Kas — uang yang benar-benar berpindah. Sebelumnya cuma ada "Selisih Kas Operasional" (Omzet − Pengeluaran) yang bukan arus kas: omzet memuat nota TEMPO belum dibayar + transfer/QRIS, dan pelunasan hutang nota lama tidak jatuh di tanggal uang diterima. Query baru memakai `transaction_payments` (`paid_at`) sbg sumber kas masuk: `getCashInByMethod` (NET dari `change_given`, lewati `voided`, refund negatif otomatis terhitung, method `tempo` dikecualikan), `getCashFlowDaily`, `getCashFlowSummary`. Kas keluar pakai SEMUA jenis expenses (bukan subset `netProfitExpenseTypes`) — ini "ke mana uang mengalir", bukan P&L. Tab ditaruh PALING AKHIR supaya index 0-3 `ReportTab.values[index]` utk ekspor tetap sama. Test `cash_flow_query_test.dart` (9) + `arus_kas_tab_test.dart` (3) — revert-verified.
- `352911b` — feat: statistik detail per produk & per pelanggan (drill-down + filter tanggal). Tab Produk & Pelanggan di Laporan sebelumnya BUNTU (`onTap` nihil di kedua tab). Query baru tanpa migrasi DB (indeks yang dibutuhkan sudah ada semua): `getProductStatsSummary` (jumlah NOTA pakai DISTINCT transaction_id, bukan jumlah baris), `getProductDailySales`, `getProductTopBuyers`, `getCustomerStatsSummary`, `getCustomerTopProducts`, `getCustomerTransactions`. Layar baru `features/laporan/stats/` + `stats_common.dart` yang dipakai bersama. Statistik pelanggan bisa dibuka dari DUA pintu masuk (tab Pelanggan di Laporan + tombol "Statistik Belanja" di detail pelanggan). Pelanggan umum/ad-hoc DIABAIKAN (keputusan user). Rentang tanggal LOKAL per layar (bukan `dateRangeProvider` global) + `ValueKey(_range)` supaya ganti rentang benar² memuat ulang. Test `detail_stats_query_test.dart` (10) + `detail_stats_screen_test.dart` (5) — revert-verified (3 pintu masuk gagal saat dicabut).
- `c1f639f` — feat: nilai rupiah selisih di riwayat Stock Opname. Riwayat dulu cuma menampilkan QTY, padahal yang dipakai untuk pertanggungjawaban adalah nilai rupiahnya. `OpnameSessionSummary.valueChange` + `OpnameSessionDetailRow.costPrice`/`valueChange`, dihitung dari HPP (`price_tiers.cost_price` tier `min_qty=1`, pola sama `getInventoryRows`); helper `_costPriceByUnitIds` ambil seluruh HPP dalam SATU query (hindari N+1). UI: kartu "Nilai selisih (susut/lebih)" + nominal per baris & per sesi, warna mengikuti pola kartu laba/pengeluaran Ringkasan. Keterbatasan disengaja & didokumentasikan: HPP dibaca SAAT QUERY (bukan snapshot) — ubah HPP ikut mengubah nilai riwayat lama. Test `opname_value_change_test.dart` (4) — revert-verified.
- `d7420b2` — fix: kelebihan bayar retur/hapus item nota belum lunas jadi kembalian nyata. Dilaporkan user: pelanggan hutang tempo lalu meretur barang senilai LEBIH dari sisa hutang — kelebihan jadi hak pelanggan tapi tidak pernah muncul di layar manapun. Akar: `returnUnpaidTransactionItems`/`editUnpaidTransactionItem` cuma menyisipkan baris penanda `amount: 0` dgn `changeGiven` 0; kelebihannya terhitung TAPI cuma mendarat di `transactions.changeAmount` — kolom yang TIDAK PERNAH dirender (struk membaca `changeGiven` pembayaran TERAKHIR via `_latestPayment`). Fix sesuai keputusan user "samakan dgn UI/UX kembalian yang sudah ada": kelebihan ditulis sbg `changeGiven` di baris penanda lewat `_computePaymentChangeGiven` yang SAMA (`_overpaymentAfterUnpaidItemChange` + `_sumItemSubtotals` baru) — seluruh UX kembalian langsung berlaku tanpa kode tampilan baru (baris "Kembalian", centang `changeTaken`, `netRemainingOwed` jadi 0 bukan negatif). Jalur HAPUS/EDIT item ditutup sekalian. Test `unpaid_return_overpayment_test.dart` (4) — revert-verified.

## 2026-08-11

- `49b2834` — fix: perbaiki 4 temuan audit efisiensi storage (schemaVersion 30). **(1) KRITIS**: Tutup Buku bisa gagal total ("FOREIGN KEY constraint failed") kalau ada nota dalam periode yang diarsipkan yang masih punya baris Laci Meja (`left_behind_items`/`borrowed_items`/`preorder_entries`, FK ke `transactions` TANPA cascade, `PRAGMA foreign_keys = ON` aktif) — 3 tabel ini sebelumnya tidak pernah disentuh `TutupBukuService.execute`. Fix: blokir eksekusi kalau ada baris yang BELUM SELESAI (titip/pinjaman/pre-order aktif — jangan diam-diam buang tugas operasional aktif), hapus baris yang SUDAH SELESAI bersama notanya (riwayat tetap ada di file arsip yang sudah disalin duluan). (2) Indeks baru utk tabel yang sebelumnya tidak terindeks sama sekali: `product_units.product_id`, `price_tiers.product_unit_id`, `alt_prices.product_unit_id`, `product_barcodes.product_unit_id`, `loyalty_point_ledger.customer_id`, plus `transaction_id` di 3 tabel Laci Meja (dipakai guard baru poin 1). Migrasi step-nya dibuat defensif thd tabel/kolom yang belum ada (`_createIndexesIfTableExists`) — fixture test migrasi lama sengaja minimal, bukan replika skema penuh; di DB produksi nyata semua tabel targetnya sudah ada sejak v1. (3) `PRAGMA auto_vacuum = INCREMENTAL` — sebelumnya `NONE`, ruang bekas baris yang dihapus di luar Tutup Buku (mis. void transaksi) tidak pernah dikembalikan ke OS; DB baru langsung aktif, DB lama aktif otomatis setelah `VACUUM` berikutnya (Tutup Buku tahunan). (4) `CatalogStore.add()` dibatasi 30 katalog tersimpan terbaru — blob JSON `saved_catalogs` sebelumnya tidak punya batas jumlah sama sekali. Test baru: `tutup_buku_laci_meja_guard_test.dart` (5), `migration_v30_indexes_test.dart` (1), `catalog_store_cap_test.dart` (2) — semua revert-verified. 15 test migrasi lama diupdate assersi `PRAGMA user_version` 29→30.

## 2026-08-09

- `19c635e` — chore: cabut instrumentasi diagnostik sementara Item 55. Bug "Tempel Pesanan pegawai tidak dapat produk" tidak lagi terjadi menurut laporan user, kemungkinan besar tertutup sbg efek samping salah satu fix sync sesi yang sama (Item 58/60/61.1) — bukan oleh logging ini sendiri, yang murni instrumentasi pasif tanpa efek pada logic (dikonfirmasi baca ulang diff commit `0919f0d`). Karena tidak ada log yang sempat ditangkap utk konfirmasi akar pastinya, dan instrumentasi ini memang didesain sementara (WAJIB dicabut begitu tidak diperlukan lagi), dicabut total: `OrderParseDiagnostics`, titik `.add()` di `parse()`, `ParseDiagnosticsScreen`, tombol debug di `PasteOrderSheet`, dan test-nya sendiri.
- `0becd9c` — fix: Riwayat Transaksi tampilkan Sisa mentah, beda dari struk. Dilaporkan user (tangkapan layar): nota `A1-20260809-0030` tampil "Sisa -Rp 300" di Riwayat Transaksi, tapi struk-nya sendiri bilang "Sisa Tagihan Rp 6.600" untuk nota yang sama. Akar sama dgn Item 56 (Buku Hutang): `tx_history_sheet.dart` pakai `total - paid` MENTAH, padahal `paid` boleh melebihi `total` saat kembalian dipakai ulang sbg pembayaran baru (lihat dok `netRemainingOwed` di `receipt_screen.dart`). Fix: `AppDatabase.getNetSisaForTxIds` baru (pola SQL sama `getDebtBook`/`getCustomerOutstandingDebt`, batched hindari N+1) dipakai di SEMUA titik `tx_history_sheet.dart` yang sebelumnya pakai raw `total-paid`: ringkasan multi-pilih, baris daftar, detail baris, dan `_lunasi` (fetch fresh dari DB, bukan provider yang bisa basi/race, krn jumlah ini yang benar-benar tercatat sbg pembayaran). Test baru (`tx_history_net_sisa_test.dart`, 6 test: 4 DB + 2 widget) — revert-verified.

## 2026-08-08

- `fb0acd5` — feat: QR Share handoff antar-device bawa keterangan item, bukan cuma kode (PLAN.md Item 54). `OrderParserService.encodeHandoff` dapat param baru `storeName` (opsional, backward-compatible) — kalau diisi, tampilkan daftar item (induk+varian dikelompokkan, format sama `buildOrderText()` katalog HTML) + Total DI DEPAN baris kode mesin `#PSN:`/meta, supaya penerima bisa baca isi pesanan di preview chat sebelum scan. `_buildItemLines`/`_handoffTotal` baru. Parser (`parse()`) TIDAK perlu diubah — regex per-baris sudah mentolerir baris tambahan apa pun. `cart_sheet.dart:_showHandoffQr` kirim `device.storeName`. Test baru (3) + `kasir_handoff_qr_test.dart` diupdate — revert-verified.
- `0919f0d` — feat: pasang logging diagnostik SEMENTARA utk bug Tempel Pesanan pegawai (PLAN.md Item 55). Bug "pegawai non `terima_pembayaran` tempel QR/teks owner, tidak ada produk masuk keranjang, padahal asisten yang terima teks PERSIS SAMA berhasil" sudah mentok di 4 ronde investigasi kode statis. `OrderParseDiagnostics` (in-memory, maks 200 entry, BUKAN Downloads) baru + titik `.add()` di `OrderParserService.parse()` (teks mentah + per-pasangan unitId lookup product_units→products→kesimpulan) + `ParseDiagnosticsScreen` (akses via tombol sementara di `PasteOrderSheet`). Test baru `order_parse_diagnostics_test.dart` (verifikasi instrumentasi sendiri, bukan regresi bug utama). WAJIB dicabut total setelah root cause ketemu.
- `b3d1588` — fix: 5 temuan sync menengah dari audit (PLAN.md Item 61): (61.1) `resetDownloadWatermark` baru, "Sync Ulang Penuh" reset KEDUA watermark sekaligus — dulu tidak ada cara reset watermark download, selisih jam device bisa bikin window download macet permanen; (61.2) `_reconcileTransactionTotals` param `guardEmptyItems` (default false, diaktifkan HANYA di `reconcileTransactionsByIds`/path pasca-sync) — item susulan yang di-skip via sync (FK gagal) dulu bikin total ditimpa 0, tapi guard blanket sempat merusak flow lokal legitimate (`returnUnpaidTransactionItems` dkk) makanya di-scope ketat; (61.3) `rebuildStockAfterForUnits` tie-break `rowid` (bukan id/UUID acak), samakan dgn `_rawBaseStock` — baris di detik sama dulu bisa beda "terakhir" antar host/client; (61.4) checkbox "Stok" di dialog approve sync WAJIB ikut tercentang & disabled selama "Transaksi" tercentang; (61.5) `Expenses.deletedAt` baru (migrasi v28→v29), `deleteExpense` soft-delete, `dumpSince`/`mergeRows` expenses ditangani khusus supaya penghapusan propagate ke device lain (append-only, dulu hard DELETE tidak pernah tersinkron). Test baru `item61_sync_medium_findings_test.dart` (8 test) — semua revert-verified. 15 test migrasi lama diupdate utk fixture `expenses` + assertion versi 29.
- `b76b923` — fix: pembayaran/item susulan ke transaksi yang sudah sync TIDAK PERNAH sampai ke host (PLAN.md Item 57). Akar: pelunasan/cicilan/item susulan ("Tambah Belanjaan") ke transaksi yang HEADER-nya (`transactions`) sudah lebih dulu tersinkron tidak pernah kirim ulang header itu (`dumpSince` filter `created_at`) — payload sync bisa HANYA berisi `transaction_payments`/`transaction_items` baru. `sync_screen.dart:_approve()` dulu hitung jumlah kategori "Transaksi" cuma dari tabel PERTAMA (`tables.first`), kalau itu kosong kategori dianggap tidak ada data sama sekali → `rejectSync` PERMANEN dgn pesan salah. Fix: hitung count dari SEMUA tabel dalam kategori, diekstrak jadi fungsi murni `computeAvailableSyncCategories` (testable tanpa widget, `approveSync` terbukti rawan hang di widget test krn banyak round-trip async NativeDatabase). Test baru (`sync_payment_only_category_test.dart`) — revert-verified.
- `5b2029f` — fix: Buku Hutang `total-paid` mentah bikin nota tempo hilang dari laporan (PLAN.md Item 56). Akar: `getDebtBook`/`getUnpaidTxDetails`/`getCustomerOutstandingDebt`/`settleMergedDebt` pakai `total-paid` MENTAH — padahal `paid` SENGAJA boleh melebihi `total` (kembalian dipakai ulang, lihat dok `netRemainingOwed` di `receipt_screen.dart`). `getDebtBook` `GROUP BY customer HAVING SUM(total-paid) > 0` — nota lain milik customer yg sama yg overpay begitu bisa bikin SUM jadi negatif, menutupi nota tempo asli, SELURUH pelanggan hilang dari Buku Hutang walau tiap nota individual statusnya tetap benar (cart bar tidak kena krn gerbangnya `COUNT(*)`, bukan `SUM`). Fix: terapkan pola `netRemainingOwed` (net dari `change_given`, LEFT JOIN subquery) ke keempat fungsi + status hasil pelunasan `settleMergedDebt`. Test baru (`debt_book_net_change_given_test.dart` + tambahan di `transaction_lifecycle_test.dart`) — revert-verified.
- `219bf7f` — fix: poin loyalti client bisa hilang, tidak pernah benar-benar tersinkron (PLAN.md Item 60, KRITIS). Akar: SEMUA 7 tempat tulis `customers.loyalty_points` pakai UPDATE RELATIF mentah ke DB lokal, TIDAK menyentuh `updated_at` — krn `customers` master data LWW berdasar `updated_at`, poin yg baru didapat client bisa langsung KETIMPA BALIK begitu host push versi lama pelanggan itu, walau `loyalty_point_ledger`-nya sendiri (append-only) sudah sync benar. Tidak ada mekanisme rebuild dari ledger (beda dari stok yg sudah punya `rebuildStockAfterForUnits`). Fix: `AppDatabase.rebuildLoyaltyPointsForCustomers` baru (pola persis `rebuildStockAfterForUnits`) — `customers.loyalty_points = SUM(loyalty_point_ledger.points)` per customer, dipanggil setelah merge di `approveSync` (host) & `syncToHost` (client), via `_collectLoyaltyCustomerIds` baru. Test baru (`lan_sync_loyalty_points_rebuild_test.dart`, 2 device beri poin independen lalu sync 2 arah) — revert-verified.
- `3de2358` — fix: Tutup Buku bisa hancurkan saldo stok saat sync pertama pasca-arsip (PLAN.md Item 59, KRITIS). Akar: `tutup_buku_service.dart` skip insert baris carry-forward saldo kalau MASIH ada baris `stock_ledger` tersisa utk unit itu — benar utk pembaca (`_rawBaseStock`, ambil baris TERAKHIR apa adanya), TAPI `rebuildStockAfterForUnits` (jalan otomatis di host & client tiap sync yg sentuh `stock_ledger`) menjumlah ulang `stock_after` dari NOL memakai baris yang TERSISA saja — kontribusi baris yg sudah diarsip hilang dari total, saldo rusak permanen begitu device lain sync. Fix: hitung `deletedSum` (qty_change baris yg akan dihapus) + `priorBalance` (saldo SEBELUM periode) per satuan sebelum delete, SELALU sisipkan 1 baris carry-forward `deletedSum != 0`, ditanggal PAS di batas akhir periode (bukan `DateTime.now()`) supaya urutan kronologis rebuild benar. Test baru (`tutup_buku_stock_carry_forward_test.dart`, riwayat CAMPURAN sebelum+sesudah periode) — revert-verified; test lama di `db_fixes_test.dart` yg menegaskan asumsi lama (salah) diupdate.
- `8897298` — fix: sync kedua dari client sebelum owner approve/reject sync pertama bisa MENGHAPUS PERMANEN batch upload sebelumnya (PLAN.md Item 58, KRITIS). Akar: `enqueueSyncUpload` DELETE+INSERT slot pengirim tanpa cek apakah payload baru superset dari yang lama — sejak watermark upload client dimajukan begitu HTTP 200 diterima (bukan setelah owner approve, Item 17 Fase 2), sync kedua yang menyusul cepat cuma bawa delta kecil/kosong, menimpa & menghilangkan batch pertama tanpa jejak. Fix: `LanSyncService._handleRequest` sekarang GABUNGKAN (union, dedup by `id`) payload baru dgn item antrian lama yang masih menunggu approve utk slot yg sama (`AppDatabase.getSyncUploadQueueItemForSlot` baru + `_unionSyncTables`/`_decodeTablesJson` helper), sebelum diteruskan ke `enqueueSyncUpload`. 1 test baru (`lan_sync_upload_queue_test.dart`, 2x `syncToHost` berurutan sebelum approve) — revert-verified.

## 2026-08-06

- `f0c5ea5` — fix: pre-order/titip/pinjaman yang SUDAH SELESAI (Dipenuhi/Dibatalkan/Diambil/Dikembalikan) tidak lagi terus diusulkan ulang ke owner — bug dilaporkan user (pola sama bug produk Item 40 yang sudah diperbaiki `filterUnchangedProposals`, tapi Laci Meja tidak pernah dapat perbaikan setara). Akar: `dumpLaciMejaProposals` cuma filter `locally_modified=1` klien, tanpa banding ke data host — flag itu cuma reset kalau baris resmi host ter-merge balik ke klien, jadi sebelum itu terjadi baris identik terus dikirim ulang sbg "usulan baru". Fix: `filterUnchangedLaciMejaProposals` (pola sama produk, lebih sederhana — record datar) dipanggil host sebelum masuk antrian, buang baris identik dgn host (kecuali `locally_modified`/`updated_at`). 5 test baru (3 unit DB + 2 end-to-end HTTP) — revert-verified.
- `d99e4b1` — feat: hapus jendela waktu tetap 1.5 detik di konfirmasi minus stepper — susulan LANGSUNG dari `5740203`, user kadang butuh tekan minus berkali-kali beruntun tanpa pindah jempol (spt stepper biasa), tapi timer bikin tap terlambat (walau jempol MASIH di stepper) dianggap tap pertama lagi. Ganti mekanisme "bersenjata": bukan berlaku selama Timer, tapi SELAMA stepper baris ini masih membesar (`AddControl.activeStepper`, mekanisme "pijakan jempol" yang sudah ada) — lepas HANYA kalau stepper baris LAIN jadi aktif/scroll, tidak pernah krn lewat waktu. Tap beruntun selama bersenjata langsung mengurangi qty tiap kali. `GlobalKey<State<AddControl>>` per baris dibandingkan via `identical()` thd `activeStepper.value`. Timer+konstanta jendela dihapus total. Test diganti — revert-verified.
- `5740203` — feat: ganti mekanisme toggle "Konfirmasi sebelum kurangi qty" dari dialog jadi getar+tap-lagi — susulan LANGSUNG dari `cdde036`, user sadar dialog kurang pas (jempol biasanya sudah di atas tombol minus, warning yang cuma di stepper tak kentara). Sekarang: tap PERTAMA bikin SELURUH baris item bergetar (warning, TIDAK mengurangi qty), tap KEDUA dalam ~1.5 detik baru benar-benar mengurangi qty; lewat jendela itu kembali netral. `_CartItemTile` diubah `ConsumerWidget`→`ConsumerStatefulWidget` (AnimationController+Timer per-item, `ValueKey` per `productUnitId` mencegah state getar bocor antar-baris saat urutan berubah). 5 test baru menggantikan test dialog lama — revert-verified.
- `cdde036` — feat: toggle "Konfirmasi sebelum kurangi qty" di dialog "Pengaturan Keranjang" (satu tempat dgn opsi posisi checkbox verifikasi) — mencegah qty berkurang tanpa sengaja (missclick) saat tap tombol minus stepper. `cartMinusConfirmProvider` (SharedPreferences, default OFF/opt-in). Saat aktif, tap minus munculkan dialog "Kurangi Qty?" — qty baru berkurang setelah user konfirmasi. 4 test baru — revert-verified.
- `70089e7` — fix: harga TIDAK ikut dibawa lewat handoff QR dari device TANPA izin `terima_pembayaran` — susulan langsung dari fix sebelumnya (`4d5c170`), user menyadari konsekuensinya: pegawai tak berizin bisa menyetel Harga Lain/override manual di keranjangnya sendiri (tidak digerbang izin apa pun di `item_entry_sheet.dart`), dan kalau harga itu ikut dibawa apa adanya ke owner tanpa validasi ulang, itu celah. `encodeHandoff()` dapat parameter `trustPrices` (default true) — `cart_sheet.dart` memanggil dgn `trustPrices: !needsGate`, jadi utk pengirim tanpa izin bayar, flag harga (p=/o=/k=/v=) tidak disertakan sama sekali → `parse()` jatuh ke resolve fresh dari DB penerima (perilaku lama). Atribut non-harga (checklist, status pre-order, catatan) TETAP dibawa apa pun izinnya. Test baru + 1 test existing disesuaikan — revert-verified.
- `3ef3019` — feat: tombol "Pengaturan Keranjang" (ikon gerigi) di samping ikon "Tempel Pesanan" (header `CartSheet`) — dialog 4 opsi letak checkbox verifikasi (`CartCheckboxPosition`, tersimpan persisten via SharedPreferences pola sama `fontScaleProvider`): depan qty/paling kiri (default), belakang stepper/paling kanan, kiri stepper minus, kanan nama item (menempel PAS setelah nama — baris nama `Expanded`→`Flexible` supaya nama pendek tidak memaksa checkbox terdorong ke ujung kanan). Jawaban permanen atas posisi checkbox yang sudah 2x dibalik bolak-balik di sesi-sesi sebelumnya — sekarang user bisa pilih sendiri. 4 test baru — revert-verified (3/4 terbukti gagal sensible saat posisi dipaksa default).
- `4d5c170` — fix: handoff QR keranjang antar device (`OrderParserService.encodeHandoff`/`parse`) sekarang membawa SEMUA atribut cart item ke penerima — harga override, `originalPrice`, HPP override, `priceOverridden`, `checked` (checklist verifikasi), `isPreorder`/`preorderPaid`, `depositQty`. Dulu penerima selalu resolve harga fresh dari DB, seolah-olah transfer ini pesanan katalog HTML biasa. Fix: segmen opsional `|key=value` setelah qty tiap item, HANYA diisi `encodeHandoff` (transfer antar device toko sendiri) — kode dari katalog HTML pelanggan tetap tanpa segmen ini, jadi perilaku lama (selalu resolve fresh) tidak berubah sama sekali. 2 test baru — revert-verified. `flutter analyze` 0 issue, full suite hijau (2 kegagalan `proposal_unchanged_end_to_end_test.dart` flaky pre-existing/port-conflict, tidak terkait).
- `62d739d` — fix: usulan Laci Meja (client->host) tidak lagi gagal total dgn `SqliteException 787 FOREIGN KEY constraint failed` saat transaksi terkaitnya sendiri belum tersync ke host (dilaporkan user via screenshot) — usulan Laci Meja & usulan sync "Transaksi" adalah dua antrian independen tanpa jaminan urutan. `applyLaciMejaProposals` sekarang cek dulu keberadaan `transaction_id` di host sebelum insert; kalau belum ada, baris DILEWATI (bukan menggagalkan seluruh batch) dgn alasan ditampilkan ke owner — baris itu otomatis diusulkan lagi begitu transaksinya tersync. Test end-to-end HTTP loopback asli (FK enforcement ON) mereproduksi persis bug di screenshot — revert-verified.
- `bd2ee9b` — fix: kembalikan checkbox verifikasi baris keranjang (`_CartItemTile`, `cart_sheet.dart`) ke paling KIRI baris — membalik keputusan sesi sebelumnya (sempat dipindah ke kanan, kiri tombol minus stepper, atas permintaan user saat itu). Checkbox sekarang jadi child PERTAMA di `Row`, sebelum badge qty & nama produk. Test `cart_item_layout_checkbox_right_test.dart` di-rename `_checkbox_left_test.dart`, 2 assersi posisi disesuaikan — revert-verified. Test lain (highlight, checklist checkedItemIds, jarak tepi, posisi nominal, stabilitas stepper) tidak diubah, tetap hijau. `flutter analyze` 0 issue, full suite 925 hijau.

## 2026-08-05

- `0577fef` — feat: redesain keranjang katalog HTML (`order_page_service.dart`) — mockup Playwright JPG dikonfirmasi user dulu (termasuk kompatibilitas dark mode) sebelum eksekusi. (1) Tiap baris keranjang dapat ikon 🗑 hapus TERPISAH dari stepper qty (dulu satu-satunya cara hapus: tekan − berulang sampai 0, tanpa konfirmasi). (2) Modal konfirmasi custom (`showConfirm`/`hideConfirm`, ikut tema app — ganti `confirm()` bawaan browser) utk KEDUA tindakan destruktif: hapus satu barang & kosongkan semua (jumlah item disebutkan). (3) Tombol "Kosongkan" (teks polos bergaris bawah) diganti pill ikon tempat sampah + "Kosongkan Keranjang", aksen merah, tetap di kanan-atas header sheet. Token `--danger`/`--danger-bg` ditambah ke `:root` + varian `data-theme="dark"`/`"light"`, pola sama dgn `--ok`/`--warn` yg sudah ada. Test baru — revert-verified. `flutter analyze` 0 issue, full suite 925 hijau.
- `4fa815d` — fix: `scripts/license-generator.html` — pesan error jelas saat kamera scan QR diblokir browser. User laporkan "Permission denied" tanpa konteks. Diduga awalnya soal "secure context" (`file://` vs https/localhost), TERNYATA SALAH setelah diverifikasi via Playwright (`isSecureContext == true` utk `file://` di Chromium). Akar sebenarnya: banyak browser (terutama Chrome Android) menolak izin kamera OTOMATIS tanpa dialog sama sekali kalau halaman dibuka langsung dari file manager/Downloads — muncul sbg `NotAllowedError`/`NotFoundError`, persis "Permission denied" yg dilaporkan. Fix: deteksi error itu di `startScan()`, tampilkan panduan konkret (jalankan `python3 -m http.server`/`npx serve .`, buka via `http://localhost`) alih-alih toast generik. Diverifikasi via Playwright: BarcodeDetector palsu + `getUserMedia` yg reject `NotAllowedError` (persis skenario Android Chrome via `file://`) → hint tampil dgn benar. Alur normal (generate/backup/build kode/QR) dites ulang, tidak berubah.
- `ead32f8` — fix: 5 penyesuaian About/Lisensi dari feedback user setelah koreksi mockup sebelumnya. (1) Ikon About: `filterQuality: FilterQuality.high` + `isAntiAlias` — sumber 192x192 diperbesar ke 178 logis pixel jadi >300px fisik di DPI tinggi, filter default (low) bikin tepi bulat patah-patah. (2) Jarak chip Lisensi ke AppBar direnggangkan. (3) Seksi "Segera Hadir" di `DeviceLicenseScreen` dihapus total (permintaan user) — parameter `dimmed` di `_InfoRow` ikut dihapus krn tak ada lagi pemakainya. (4) Nomor serial sekarang SPOILER (pola titik-titik ala mockup awal utk chip ID) sampai diketuk — QR di atasnya sengaja TIDAK ikut disamarkan (sudah seperti kebisingan visual bagi mata telanjang). 3 test baru (reveal/hide spoiler, seksi "Segera Hadir" benar-benar hilang). `flutter analyze` 0 issue, full suite 923 hijau.
- `0f70fc8` — fix/feat: ganti ikon About dengan file emoji resmi 512x512 ber-alpha (sebelumnya 192x192 mipmap-xxxhdpi — batas resolusi tertinggi yg ada di repo, pecah saat ditampilkan besar); backdrop peachy `#FFC896` ditambahkan di belakang krn sumber baru transparan (bukan squircle solid spt mipmap Android) supaya bentuknya tetap sama dgn ikon launcher asli. Tutorial dilengkapi 6 bab baru (Printer Bluetooth, Backup & Restore + Alihkan Owner, Poin Loyalitas, Retur & Edit Transaksi Lunas, Tutup Kasir vs Tutup Buku, Katalog Pesanan) — semua Pro Tip digrounding ke perilaku kode nyata, bukan tebakan. Bug visual dilaporkan user (garis tipis di atas konten `ExpansionTile` saat expanded, nempel aneh di dalam `Card`) diperbaiki via `shape`/`collapsedShape: const Border()`. 3 test baru — revert-verified. `flutter analyze` 0 issue, full suite 925 hijau.

## 2026-08-04

- `09927d1` — ci: beri retry+backoff pada langkah "Create dev pre-release" di `build-apk.yml`. Run `3485f42` GAGAL padahal SELURUH build sukses (APK 45MB sudah jadi & terupload sbg artifact) — yang jatuh cuma langkah terakhir, `gh release create` balas `error checking for existing release: HTTP 503` (hiccup API GitHub, bukan masalah kode). Langkah itu tadinya tanpa retry sama sekali, padahal jalan di SETIAP push branch. Sekarang 4 percobaan (jeda 0/5/15/30 detik); kalau `create` gagal TAPI rilisnya ternyata sudah terbuat duluan, jatuh ke `gh release upload --clobber` supaya tidak ada rilis dobel. API yang benar-benar down terus TETAP menggagalkan build (bukan lolos diam-diam). Logika retry diuji lokal dgn `gh` palsu untuk 4 skenario (sukses langsung / 503 sekali lalu sukses / fallback upload / down permanen).
- `dcf7bf8` — fix: samakan `AboutScreen` & `DeviceLicenseScreen` dengan mockup yang sudah dikonfirmasi (versi `b97a0e8` dibangun tanpa membuka ulang mockup-nya, melenceng jauh). About: AppBar TANPA judul + tombol "?" BULAT ber-outline di kanan; urutan hero dibalik ke mockup (wordmark 38px w800 DULU, BARU ikon) dan ikon dibesarkan 108px → **178px** ber-radius 40 + shadow (dulu ikon sama sekali tidak dominan, padahal itu justru permintaan intinya); footer "Versi X (build N)"; kartu "Info Lisensi & Serial" di tengah halaman diganti **chip kecil rata-kanan** di bawah AppBar (mewarisi posisi & bentuk chip "ID device" di mockup). Lisensi: dirombak ke bahasa desain yang sama (section label uppercase, panel kartu, baris ikon+label+nilai — dulu `ListTile` Material generik), baris "Sisa waktu" disembunyikan utk lisensi "selamanya" (dulu mengulang kata yang sama 2x). **BUG NYATA ikut ketemu & diperbaiki**: `DateFormat(..., 'id_ID')` di `DeviceLicenseScreen` membuat SELURUH layar gagal render (`LocaleDataException`) di app asli — app ini TIDAK PERNAH memanggil `initializeDateFormatting` (sudah didokumentasikan di `expenses_screen.dart`, terlewat saat menulis layar ini); nama bulan dibentuk manual. Entry point Pengaturan dipindah dari Diagnostik ke seksi "Lainnya" sendiri dgn tint aksen (sesuai baris highlight mockup). 2 test baru — regresi locale revert-verified. `flutter analyze` 0 issue, full suite 921 hijau.
- `b97a0e8` — feat: layar baru "Tentang Aplikasi" (`AboutScreen`, `/pengaturan/tentang`) — wordmark "The POS" + ikon launcher asli (disalin ke `assets/icon/app_icon.png`, belum ada sumber ikon Flutter-asset sebelumnya), versi via `package_info_plus` (dependency baru), teks "made with ♥️ by Dre". Tombol "?" di AppBar buka **"Panduan & Tips"** (`TutorialListScreen`, `/pengaturan/tentang/tutorial`) — daftar bab yang bisa dicari by keyword, tiap bab punya satu/lebih "Pro Tip" (fitur yang kurang kentara: tempel-pesanan-merge-ke-cart-aktif, QR handoff merge, sync harga antar toko barcode-matching, dll — bukan cuma satu contoh). "Info Lisensi & Serial" (`DeviceLicenseScreen`, sebelumnya di kartu "Device Ini" Pengaturan) **DIPINDAH** ke `AboutScreen` (permintaan user) — ListTile & navigasinya dihapus dari `pengaturan_screen.dart`, entry point baru "Tentang Aplikasi" ditambahkan di kartu Diagnostik. 6 test baru (`about_screen_test.dart`, `tutorial_list_screen_test.dart`) — revert-verified. `flutter analyze` 0 issue, full suite hijau (1 kegagalan `proposal_unchanged_end_to_end_test.dart` flaky pre-existing/port-conflict, lolos sendirian).
- `673119e` — feat: redesain total `scripts/license-generator.html` (alat generator lisensi offline developer) mengikuti referensi desain "Toko Berkah — All-in-One" (palet ivory/charcoal + aksen crail-orange `#D97757`, TANPA font CDN — tetap 100% offline satu file). Tambahan: (1) QR encode kode aktivasi yang barusan dibuat (vendored `qrcode.js` — Kazuhiko Arase, MIT — disematkan inline, bukan CDN), bisa langsung discan dari layar pelanggan; (2) kartu "Scan Serial dari HP Pelanggan" pakai `BarcodeDetector` native browser (mesin ML Kit yang sama dgn `mobile_scanner` Flutter di Chrome Android) untuk isi kolom sidik jari otomatis tanpa ketik manual, gagal-diam ke input manual kalau browser tidak dukung. TIDAK ada integrasi GitHub API (scope dikecualikan eksplisit oleh user) — remote-terminate device tetap manual lewat `revoked.json` seperti sebelumnya. Seluruh fungsi lama (generate/import/export keypair, build+copy kode) tidak berubah perilakunya, diverifikasi ulang via Playwright headless (generate keypair → build kode → render QR non-kosong).
- `18d1e64` — feat: POS bisa tampilkan sidik jari device sendiri sbg QR code, bukan cuma teks — di layar `AktivasiScreen` (gerbang aktivasi/welcome device baru) DAN halaman baru "Info Lisensi & Serial" (`DeviceLicenseScreen`, `/pengaturan/lisensi`, dibuka dari kartu "Device Ini" di Pengaturan). Halaman baru juga menampilkan tanggal diaktifkan (`LicenseState.activatedAt`, SharedPreferences key baru `license_activated_at` — dicatat SEKALI saat aktivasi pertama, tidak tertimpa saat renewal berikutnya), tanggal berlaku sampai, dan placeholder "Segera Hadir" (perpanjang via scan, riwayat perpanjangan) utk fitur upcoming yang terkait alat scanner lisensi di atas. `flutter analyze` 0 issue, test screen existing (`aktivasi_screen_test.dart`) tetap hijau.
- `c786ee8` — chore: naikkan versi ke 2.10.0+15. MINOR bump: 4 fitur baru sejak 2.9.1+14 (Tempel Pesanan langsung dari keranjang + diaktifkan di mode Tambah Belanjaan + QR scan merge ke keranjang aktif; usulan sync pelanggan dari device non-owner; highlight soft item keranjang tercentang; posisi scroll keranjang dipulihkan saat sheet dibuka ulang), tidak ada breaking change (schemaVersion 28, additive).
- `eefb4b0` — fix: layar review usulan produk (`ProductProposalReviewScreen`) tampilkan SEMUA perubahan terdeteksi, bukan cuma harga tier dasar — dulu produk yang diubah satuannya (atau nama/isi-rasio/barcode/harga alternatif/jumlah tier grosir/satuan ditambah-dihapus) tanpa perubahan harga tetap tampil "Tidak ada perubahan harga" yang menyesatkan, nyaris bikin owner dismiss usulan asli krn dikira glitch. `_diffProduct` baru membandingkan proposal vs data host per-aspek (satuan dicocokkan by id — stabil lintas edit; tier/alt-harga/barcode dibandingkan sbg SET nilai — id-nya diregenerasi tiap simpan). Tampilan harga satuan dasar (RichText strikethrough) TIDAK diubah. `filterUnchangedProposals` di host sudah benar sejak awal — bug murni di tampilan review. Tidak ada bump versi tersendiri (menyatu dgn bump 2.10.0 di bawah). 5 test baru — revert-verified. Full suite hijau, `flutter analyze` 0 issue.
- `512aae9` — test: buktikan produk baru client aman dari sync host & deaktivasi owner tetap propagasi ke client, dalam satu skenario sekaligus (`product_new_client_survives_host_sync_test.dart`). Bukan trade-off yang perlu dipilih salah satu — sudah terpenuhi keduanya oleh desain sync yang ada (`mergeRows` cuma memproses baris yang benar-benar ada di payload host). Tidak ada perubahan kode produksi. Full suite hijau, `flutter analyze` 0 issue.
- `ef18cdd` — feat: pulihkan posisi scroll `CartSheet` saat sheet dibuka ulang (`_cartScrollMemory`, top-level map per-`cartId` di `cart_sheet.dart`, listener pada scroll controller + `jumpTo` saat dibuka lagi, kalah dari `scrollToBottom` eksplisit). Sebelum ini sheet selalu mulai dari atas tiap dibuka, merepotkan saat keranjang panjang & sering misclick tap item (konsekuensi yang sudah diterima, debounce sempat dicoba lalu direvert krn bikin stepper kurang sensitif). Tidak ada bump versi (permintaan eksplisit user). 2 test baru — revert-verified. Full suite 908 hijau, `flutter analyze` 0 issue.
- `ddbe65c` — docs: catat rencana percepatan input harga modal/stok dari nota supplier (Item 53 PLAN.md, PENDING atas permintaan eksplisit user — insight didiskusikan, belum dieksekusi).

## 2026-08-03

- `f606428` — feat: highlight soft (`scheme.primary` opacity 0.08) untuk baris item keranjang yang sudah dicentang (`_CartItemTile`, dibungkus `Container` baru) — sekilas membedakan barang yang sudah/belum diverifikasi. Tidak ada bump versi (permintaan eksplisit user). 2 test baru — revert-verified. Full suite 905 hijau, `flutter analyze` 0 issue.
- `d196ccd` — feat: usulan sync pelanggan dari device non-owner (schemaVersion 28). Pola sama persis dgn usulan harga/produk (Item 40) & Laci Meja (Item 52): kolom baru `customers.locally_modified` (migrasi additive), `markCustomerLocallyModified`/`dumpLocalCustomerProposals`/`applyCustomerProposals` di `AppDatabase`, dipanggil dari `pelanggan_form_screen.dart` (gated `!device.isOwner`). Antrian usulan PARALEL di `LanSyncService` (`PendingCustomerProposal`), field payload `customerProposals` terpisah. Layar review baru `CustomerProposalReviewScreen` + kartu "Usulan Pelanggan" di `SyncScreen`. Migrasi test lama (v7-v26) yang synthetic schema-nya belum punya tabel `customers` diberi tabel minimal (pola sama dgn gotcha `product_units` yang sudah didokumentasikan). Sebelum ini, pelanggan yang ditambah/diubah di device kasir/asisten TIDAK PERNAH sampai ke host sama sekali. Tidak ada bump versi (permintaan eksplisit user). 12 test baru (DB murni, sync end-to-end HTTP loopback asli, gating form per-role, review screen) — revert-verified. Full suite hijau, `flutter analyze` 0 issue.
- `c690329` — feat: tombol "Tempel Pesanan" langsung dari `CartSheet` (ikon di header, selalu aktif walau keranjang kosong) — berguna kalau ada pesanan tambahan dari pelanggan (katalog HTML) atau pegawai tanpa izin `terima_pembayaran`, sebelum keranjang di-checkout; `PasteOrderSheet` sudah generik per-`cartId` & sudah merge ke keranjang yang ada, tidak disentuh. Tombol yang sama diaktifkan juga di mode "Tambah Belanjaan" (`kasir_screen.dart`, dulu sengaja disembunyikan). Scan QR handoff (Transfer via QR) sekarang cek dulu keranjang aktif device ini — kalau sudah berisi item, item hasil scan di-merge LANGSUNG ke situ (pola sama dgn `PasteOrderSheet`), bukan selalu masuk antrian `held_orders` terpisah seperti sebelumnya. Tidak ada bump versi (permintaan eksplisit user). 7 test baru — revert-verified. Full suite hijau, `flutter analyze` 0 issue.
- `31c9b26` — fix: ganti tombol "Sudah Dikirim, Kosongkan Keranjang" di sheet QR handoff (`_HandoffQrSheet`, `cart_sheet.dart`) dengan tombol "Share Pesanan" (`Share.share`). Tombol lama persis di atas "Tutup" rawan ke-misclick, menghapus keranjang yang sebenarnya belum terkirim. Mengosongkan keranjang tetap tersedia lewat ikon tempat sampah di header `CartSheet` (dialog konfirmasi sudah ada, tidak berubah). `onDone` callback & plumbing terkait dihapus (tidak ada lagi pemanggil). Test `kasir_handoff_qr_test.dart` diperbarui. Tidak ada bump versi (permintaan eksplisit user). Full suite 884 hijau, `flutter analyze` 0 issue.

## 2026-08-01 (lanjutan 4)

- `5266dcd` — fix: baris keranjang terlalu mepet ke tepi layar di kedua sisi — jarak ke tepi diperlebar (8/4px -> 16/16px, varian 32px -> 40px), memberi ruang jari lebih leluasa di sekitar checkbox/stepper (sisi kanan) & nama/nominal (sisi kiri). Tidak ada bump versi (permintaan eksplisit user). 1 test baru — revert-verified. Full suite 883 hijau (1 kegagalan `proposal_unchanged_end_to_end_test.dart` dikonfirmasi flaky pre-existing/tidak terkait — port conflict, lolos 100% saat dijalankan sendirian), `flutter analyze` 0 issue.

## 2026-08-01 (lanjutan 3)

- `c006f44` — chore: naikkan versi ke 2.9.1+14. PATCH: melengkapi jalur sync utk fitur yg baru dirilis di commit sebelumnya, bukan fitur baru.
- `c006f44` — fix: saklar "Ikut harga satuan dasar" varian sekarang ikut tersambung ke jalur approve usulan sync (`applyProductProposals`), bukan cuma `saveProduct` (form Edit Produk biasa). User bertanya eksplisit "Apakah bisa dibuat sync juga?" menindaklanjuti gap yang dicatat di PLAN.md Item 53. Selama memproses `product_units` & `price_tiers` dari usulan yang di-approve, dikumpulkan (productId -> satuan dasarnya + harga tier barunya kalau ikut ter-approve); SETELAH `transaction()` commit, dipanggil `_cascadeVariantPricesForUnit` yang sama persis dgn yang dipakai `saveProduct` — scope ketat per `parentProductId` (usulan produk lain yg diapprove bersamaan tidak ikut mencascade). Cascade cuma terpicu kalau tier dasar (`minQty=1`) satuan dasar produk itu benar-benar ada di batch usulan yang diterapkan (bukan setiap approve produk apa pun). 3 test baru (`variant_follow_price_proposal_sync_test.dart`) — revert-verified. Full suite 883 hijau, `flutter analyze` 0 issue.

## 2026-08-01 (lanjutan 2)

- `9ec58d5` — chore: naikkan versi ke 2.9.0+13 (schemaVersion 26->27, additive/backward-compatible). MINOR: fitur baru (saklar "ikut harga satuan dasar" varian, harga varian bisa diketik manual + chip Harga Lain langsung), tidak ada breaking change.
- `9ec58d5` — feat: Item 53 — saklar "Ikut harga satuan dasar" untuk satuan jual varian + revisi UX harga varian di modal Tambah Item. Menjawab 2 usulan susulan user setelah keputusan final "pertahankan skema varian sekarang" (jadi arsitektur varian-sebagai-entity TIDAK direvisi, hanya diperkaya): (1) **Saklar ikut harga** — kolom baru `product_units.follows_parent_price` (migrasi v26->v27, additive `addColumn`, default false — varian lama tidak terpengaruh). Saat aktif, harga dasar (tier `minQty=1`) satuan JUAL varian otomatis mengikuti harga × isi-per-satuan setiap kali harga satuan dasar produk INDUK (yg jenisnya sama dgn jangkar varian) disimpan ulang lewat `saveProduct` — helper baru `AppDatabase._cascadeVariantPricesForUnit` (scoped ketat per `parentProductId`, tidak pernah lintas produk). Dialog Tambah/Edit Varian (`produk_form_screen.dart`) dapat `SwitchListTile` baru "Ikut harga satuan dasar" — field Harga jadi read-only + terisi pratinjau (harga induk × isi) selama saklar aktif, mencegah nilai manual yg bisa menyimpang diam-diam dari hasil hitung. **Belum** disambungkan ke `applyProductProposals` (jalur approve usulan sync) — dicatat di PLAN.md sbg gap yg diketahui. (2) **UX harga varian di kasir** — ikon popup "Pilih harga" (dari commit sebelumnya) DIGANTI: field harga bisa diketik manual langsung + chip "Harga dasar"/Harga Lain tampil langsung (persis pola "Pilih harga" produk utama, widget baru `_MiniPriceChip` versi ringkas), TAPI keduanya HANYA muncul saat varian itu qty>0 (sedang dibeli) — supaya produk dgn banyak varian (mis. 8 rasa) tidak bikin modal jadi penuh sesak/scroll panjang kalau semua baris menampilkan kontrol harga sekaligus. Ketik manual di field ini = override SEKALI PAKAI utk transaksi ini saja (konsisten dgn override harga produk utama), TIDAK menyentuh data varian tersimpan. Kedua fitur ini dirancang bareng user via mockup HTML+Playwright (dua putaran revisi visual) sebelum implementasi kode sungguhan dimulai — sesuai permintaan eksplisit "takutnya salah lagi". Gotcha migrasi (sudah didokumentasikan di CLAUDE.md, kena LAGI persis sesuai prediksi): step migrasi baru yg `addColumn` ke `product_units` TANPA syarat versi mematahkan 3 test migrasi lama (`migration_v23/v24/v26_test.dart`) yg skema sintetisnya sengaja tidak menyertakan tabel itu — fix: tambahkan `CREATE TABLE product_units` (skema PERSIS versi pra-v27) ke setup sintetis ketiganya, plus update semua assertion `PRAGMA user_version` dari 26 ke 27 di seluruh file migrasi lama. 12 test baru (`variant_follow_parent_price_test.dart` x5, `produk_form_variant_follow_price_ui_test.dart` x4, revisi total `item_entry_variant_alt_price_test.dart` x4) — semua revert-verified. Full suite 880 hijau, `flutter analyze` 0 issue, codegen diverifikasi dari NOL (`app_database.g.dart` byte-identik).

## 2026-08-01 (lanjutan)

- `ecf3bdd` — chore: naikkan versi ke 2.8.1+12. PATCH: koreksi posisi UI, bukan fitur baru.
- `ecf3bdd` — fix: posisi nominal baris keranjang & jarak checkbox-stepper. Dua percobaan sebelumnya (di bawah qty badge kiri, lalu di bawah stepper kanan) SALAH — user klarifikasi lewat mockup HTML+Playwright (dibuat sengaja sebelum sentuh kode Flutter, supaya tidak salah arah 3x): nominal seharusnya PERSIS di bawah baris "satuan · harga" (mis. "Karung · Rp 65.000"), rata kiri sejajar nama produk — bukan di blok kanan (stepper) sama sekali. `_CartItemTile` (`cart_sheet.dart`): `Text(formatRupiah(subtotal))` dipindah ke dalam kolom kiri (setelah Row satuan+harga, sebelum itemNote), blok kanan disederhanakan jadi cuma `Checkbox` + `AddControl` langsung di `Row` utama (tidak perlu dibungkus `Column` lagi krn tidak ada lagi teks yang ditumpuk di situ). Jarak checkbox->stepper diperlebar dari 4px ke 14px sesuai permintaan ("agak renggang"). Desain `AddControl` (stepper) sendiri sama sekali tidak disentuh. 1 test lama disesuaikan total (`cart_item_layout_checkbox_right_test.dart`, 4 kasus) — revert-verified. Full suite hijau, `flutter analyze` 0 issue.

## 2026-08-01

- `6c0463d` — chore: naikkan versi ke 2.8.0+11 utk rilis resmi berikutnya. MINOR bump: fitur baru (satuan jual + isi per satuan untuk varian) + 4 penyesuaian UI/bugfix, tidak ada breaking change/migrasi schema.
- `6c0463d` — feat: satuan jual + isi per satuan untuk varian, plus 4 penyesuaian UI dari user. (1) **Satuan varian** — menjawab "jangkar ke satuan mana" yang menggantung di Item 52 PLAN.md: varian TETAP jangkar ke satuan dasar (pemegang stok — seluruh `stock_ledger` app ini ditulis dalam satuan dasar, lihat `_baseUnitOf`), tapi sekarang boleh DIJUAL dalam satuan lain berisi N satuan dasar (mis. Renteng isi 10). Diwakili SATU baris `product_units` non-dasar tambahan; helper baru `AppDatabase.variantSaleUnit(units)` jadi satu-satunya aturan pemilihan ("prefer non-dasar") dan dipakai SEMUA konsumen (`ItemEntrySheet._load`, `_adjustVariantStock`, `_VariantStockLabel`, `_editVariant`). Harga/barcode/Harga Lain menempel di satuan jual; `currentStock` yang sudah ada otomatis membagi rasio. TANPA migrasi schema — varian lama (satu satuan) otomatis jatuh ke satuan dasarnya. `createVariant` dapat param `baseUnitTypeId`+`contentPerUnit`, `updateVariant` dapat `unitTypeId`+`contentPerUnit` (satuan jual dibuat MALAS saat isi pertama kali != 1, lalu harga/barcode/Harga Lain DIPINDAH dari satuan dasar; setelah ada TIDAK PERNAH dihapus walau isi dikembalikan ke 1 — `transaction_items` nota lama menunjuk ke id satuan itu). Dialog Tambah/Edit Varian dapat dropdown "Jenis Satuan" + field "Isi per Satuan". (2) Baris keranjang: nominal subtotal PINDAH ke bawah baris qty — dulu sebaris di kanan stepper, jadi lebar teks rupiah yang berubah tiap qty diketuk MENGGESER stepper di bawah jari. (3) Checkbox verifikasi pindah dari kiri baris ke kanan, persis di kiri tombol minus. Untuk (2)+(3), `_CartItemTile` TIDAK lagi memakai `ListTile` — `ListTile` mengunci tinggi leading/trailing ke 48px (dense) apa pun tinggi barisnya, jadi subtotal mustahil ditumpuk di bawah stepper tanpa overflow (kena persis saat dikerjakan: "RenderFlex overflowed by 14 pixels"); diganti `InkWell`+`Row` manual supaya blok kanan bebas tinggi & aman saat skala font sistem dibesarkan. (4) Ikon tab Ringkasan `grid_view` → `note_alt` (kertas+pensil). (5) Bugfix: status centang keranjang HILANG di jalur "Tambah Belanjaan" — `_confirmPayment` (nota baru) sudah lama meneruskannya ke `transactions.checkedItemIds` tapi `_confirmAddItems` sama sekali tidak, jadi barang yang sudah dicentang kasir muncul TANPA centang di struk in-app; `addItemsToTransaction` dapat param `checkedItemIds` yang DIGABUNG (union) dgn yang sudah ada, bukan menimpa (centang nota lama tidak boleh ikut hilang). 15 test baru (`variant_sale_unit_test.dart` x5, `produk_form_variant_unit_ui_test.dart` x3, `cart_item_layout_checkbox_right_test.dart` x3, `ringkasan_tab_icon_test.dart` x1, `tambah_belanjaan_checked_carry_test.dart` x3) — semua revert-verified. Full suite hijau, `flutter analyze` 0 issue.

## 2026-07-31 (lanjutan 2)

- `3958b48` — chore: naikkan versi ke 2.7.0+10 utk rilis resmi berikutnya. MINOR bump: 2 fitur baru (sesuaikan stok varian, pakai Harga Lain varian saat jual), tidak ada breaking change/migrasi schema.
- `3958b48` — feat: sesuaikan stok varian + Harga Lain varian bisa dipakai saat jual. Susulan langsung dari commit sebelumnya (`b7744d7`) — waktu itu stok varian & Harga Lain varian baru bisa DILIHAT/DISIMPAN, belum bisa DIPAKAI. (1) Stok varian: ikon "Sesuaikan stok varian" baru di tiap baris varian (`produk_form_screen.dart`) membuka dialog "Sesuaikan Stok" yang sama persis dgn punya satuan produk utama (`_adjustStockDialog`, cukup diberi `unitId`+label varian) — sebelumnya stok varian mustahil diubah dari layar mana pun kecuali lewat Stok Opname/transaksi. Varian non-stok (`isNonStock`) ditolak dgn banner info, bukan dialog kosong. (2) Harga Lain varian: `_VariantOption` (`item_entry_sheet.dart`) diberi field `altPrices` (di-fetch dari `db.getAltPrices` di `_load()`), `_VariantRow` diberi ikon popup kecil (`Icons.sell_outlined`, muncul HANYA bila varian punya Harga Lain) berisi "Harga dasar" + tiap Harga Lain — memilihnya mengubah harga yang benar-benar tersimpan ke `CartItem` saat "Tambah ke Keranjang" (state baru `_variantPriceOverride`, dipakai `_variantTotal` & `_submit`), bukan cuma harga dasar mentah spt sebelumnya. 4 test baru (`produk_form_variant_stock_adjust_test.dart` x2, `item_entry_variant_alt_price_test.dart` x2) — semua revert-verified. Full suite hijau, `flutter analyze` 0 issue.

## 2026-07-31 (lanjutan)

- `b7744d7` — chore: naikkan versi ke 2.6.0+9 utk rilis resmi berikutnya. MINOR bump: 2 fitur baru (Harga Lain varian, status stok varian), tidak ada breaking change/migrasi schema.
- `b7744d7` — feat: Harga Lain per varian + tampilkan status stok varian. Usulan user (poin 2 & 3 dari diskusi sebelumnya): (1) "Harga Lain" — sudah bisa tanpa migrasi (`AltPrices` sudah di-key per `productUnitId`, varian sudah punya `ProductUnits` sendiri) — `createVariant`/`updateVariant` diberi param `altPrices` (pola identik `saveProduct`: null = tidak disentuh, list termasuk kosong = selalu ganti seluruh baris), dialog Tambah/Edit Varian diberi UI ringkas (label+harga, tanpa drag-reorder spt produk utama). (2) Status stok varian — SUDAH dilacak di DB (tiap varian punya `stock_ledger` sendiri via unitId-nya) tapi TIDAK PERNAH ditampilkan di mana pun (kasir bisa jual varian kosong tanpa peringatan). Fix: `_VariantOption`/`_VariantRow` di `ItemEntrySheet` diberi field stok + badge "Habis" (merah) dan teks "Stok N"/"Non-stok"; widget baru `_VariantStockLabel` di `produk_form_screen.dart` (Edit Produk, daftar Varian) menampilkan hal yang sama via `FutureBuilder`. Gotcha selama implementasi: draft awal subtitle non-stok TIDAK benar-benar menulis teks "Non-stok" (cuma menghilangkan info stok) — ketahuan dari test yang gagal, bukan review manual. 6 test baru (`variant_alt_prices_test.dart` x4, `item_entry_variant_stock_test.dart` x2, `produk_form_variant_stock_label_test.dart`, `produk_form_variant_alt_price_ui_test.dart`) — semua revert-verified. Full suite 849/849 hijau, `flutter analyze` 0 issue.

## 2026-07-31

- `8e35500` — chore: naikkan versi ke 2.5.0+8 utk rilis resmi berikutnya. MINOR bump: ada fitur baru (qty varian bisa diketik langsung) sejak `v2.4.1`, tidak ada breaking change (`schemaVersion` 25->26 backward-compatible via `alterTable`, data lama aman).
- `1d2b627` — fix: hapus FK `customer_id -> customers` dari `left_behind_items`/`borrowed_items` (schemaVersion 25->26). Bug nyata dilaporkan user via screenshot: "Gagal menerapkan usulan: SqliteException(787): FOREIGN KEY constraint failed" saat owner menerapkan usulan Laci Meja (Titip/Ketinggalan "Sedap Soto" ditaut pelanggan "Devi"). Akar: `customers` adalah master data yang HANYA mengalir host->klien (lihat `dumpSince`) — pelanggan AD-HOC yang dibuat di device kasir TIDAK PERNAH tersinkron balik ke host. `LeftBehindItems.customerId`/`BorrowedItems.customerId` (beda dari `Transactions.customerId` & `PreorderEntries` yang SENGAJA tanpa FK persis krn alasan ini) tetap FK ke `Customers` — begitu owner coba terapkan usulan yg menaut pelanggan ad-hoc semacam itu, INSERT OR REPLACE gagal PERMANEN, tidak pernah bisa diterapkan sama sekali. Fix: `m.alterTable(TableMigration(...))` merekonstruksi kedua tabel dari definisi Dart terkini (sudah tanpa FK itu) — data lama disalin apa adanya. `transactionId` FK TETAP dipertahankan (transaksi selalu tersinkron client->host via `dumpSince` appendOnly, beda sifat dari customer). Test baru: `migration_v26_test.dart` (FK hilang via `PRAGMA foreign_key_list`, data lama utuh, revert-verified via hapus migration step sementara) + `laci_meja_adhoc_customer_proposal_test.dart` (reproduksi persis skenario bug lewat `applyLaciMejaProposals` — WAJIB `PRAGMA foreign_keys = ON` manual krn `NativeDatabase.memory()` polos tidak mengaktifkannya, gotcha yg sama dgn `lan_sync_transaction_items_repro_test.dart`; revert-verified: throw SqliteException 787 sebelum fix, sukses sesudah). Full suite 841/841 hijau, `flutter analyze` 0 issue, codegen diverifikasi dari NOL (`app_database.g.dart` byte-identik).
- `8e35500` — feat: qty varian bisa diketik langsung (desimal), samakan format dgn Titip/Ketinggalan. Usulan user: stepper +/-1 varian di `ItemEntrySheet`/`_VariantRow` sebelumnya tidak punya field ketik langsung (beda dari qty produk utama & Titip/Ketinggalan yg sudah punya `TextField` keyboard desimal) — bikin qty varian desimal (mis. varian produk timbang) tidak bisa dicapai. Fix: `_VariantRow` diberi `TextEditingController` per varian (pola identik `_qtyCtrl` qty utama), stepper +/-1 tetap ada utk kenyamanan qty bulat & disinkronkan ke teks. 2 test baru `item_entry_variant_decimal_qty_test.dart` (ketik 2.5 tersimpan persis ke cart; stepper +/-1 tetap berfungsi) — revert-verified.

## 2026-07-30

- `61f00ec` — chore: naikkan versi ke 2.4.1+7 utk rilis resmi berikutnya. PATCH bump (bukan MINOR) krn perubahan sesi ini murni bugfix (retur desimal + overshoot stepper) — bukan fitur baru, tidak ada breaking change (`schemaVersion` tetap 25). Build number lanjut urutan (`+6` → `+7`).
- `61f00ec` — fix: dialog Retur bisa qty DESIMAL (mis. retur 2.5 dari beli 4.5 minyak kelapa) + fix bug overshoot stepper. User tanya "retur tidak bisa desimal ya?" — dicek: `returnQty` sudah `Map<String, double>` & layer DB (`returnPaidTransactionItems`/`returnUnpaidTransactionItems`) sudah full support desimal (refund = `priceAtSale * q`, otomatis proporsional) — TAPI UI di `_showReturSheet` cuma punya stepper +/-1 polos, tidak ada TextField, jadi qty desimal sembarang (mis. 2.5) mustahil dicapai. Bonus bug ditemukan: tombol `+` lama TIDAK meng-klem ke qty maksimum (beda dari dialog Titip/Ketinggalan yg sudah benar) — bisa menampilkan angka MELEBIHI batas di layar (mis. "5" padahal maks 4.5), walau DB tetap meng-klem otomatis jadi TIDAK ada data rusak, cuma tampilan yg membingungkan kasir. Fix: qty diganti `TextField` keyboard desimal (pola identik `_showLeftBehindDialog`), stepper `+` diklem persis ke `maxQty`. Test baru `receipt_retur_decimal_qty_test.dart` (ketik 2.5 dari maks 4.5 + assert refund proporsional; stepper `+` mendarat pas 4.5 bukan 5) — keduanya revert-verified. Full suite 837/837 hijau, `flutter analyze` 0 issue.
- `cce591e` — chore: naikkan versi ke 2.4.0+6 utk rilis resmi berikutnya. MINOR bump (bukan PATCH) krn ada 1 fitur baru (opsi "barang lain" Titip/Ketinggalan) sejak `v2.3.0`, MINOR bukan MAJOR krn tidak ada breaking change (`schemaVersion` tetap 25, tidak ada migrasi baru). Build number lanjut urutan (`+5` → `+6`). `flutter analyze` 0 issue.
- `734ebcc` — feat: Titip/Ketinggalan — opsi "barang lain" (diketik bebas, di luar nota) + toggle jenis dipindah ke atas. Usulan user: (1) kadang barang pelanggan yang tertinggal/dititip BUKAN barang yang dibeli di toko ini (mis. barang milik sendiri yang tertinggal, atau sengaja dititipkan) — centang produk nota TETAP fitur utama (tidak dihilangkan), ditambah field "Nama barang"+"Jml" diketik bebas (pola identik `_showBorrowedDialog`/Pinjaman) sbg pelengkap, disimpan dgn `transactionItemId: null`. (2) toggle `SegmentedButton` Titip/Ketinggalan dipindah dari BAWAH daftar produk ke ATAS — sebelumnya kalau nota banyak produk, staf harus scroll dulu lewati semua baris sebelum ketemu toggle-nya. Ditambah: method DB baru `getLeftBehindWithoutLineForTransaction` + section "Titip/Ketinggalan (di luar nota)" baru di struk in-app (pola sama section "Pinjaman Barang") supaya entri tanpa `transactionItemId` tetap kelihatan sbg "rujukan kebenaran", bukan cuma snackbar sekali lewat. Gotcha selama implementasi: field qty "barang lain" awalnya default `'1'` spt Pinjaman — collision dgn stepper qty checklist yg SAMA-SAMA nampilkan '1' dalam SATU dialog yg sama (finder ambigu di test) — diganti default KOSONG (wajib diisi eksplisit). 2 test baru (posisi toggle via `getTopLeft` dy comparison, entri "barang lain" tanpa centang produk apa pun) — keduanya revert-verified (gagal sblm fix: toggle Y lebih besar dari heading, field "Nama barang" tidak ditemukan sama sekali). Full suite 833/834 hijau (1 gagal flaky pre-existing `proposal_unchanged_end_to_end_test.dart`, tidak terkait), `flutter analyze` 0 issue.

## 2026-07-28

- `89e0587` — fix: backup penuh & Alihkan Owner sekarang menyertakan 3 tabel Laci Meja (`left_behind_items`, `borrowed_items`, `preorder_entries`). User tanya "apakah state laci ikut backup/alihkan owner?" — dicek ternyata TIDAK: `db.dumpAllTables()`/`restoreFromDump()` (dipakai `DbExportService.exportPortable`/`exportOwnerTransfer`/`restore`) berpatok ke list hardcoded `_allTables` yang 3 tabel ini terlewat dimasukkan — beda dari sync LAN (`dumpSince`/`dumpLaciMejaProposals`) yang sudah benar sejak awal. Akibatnya: restore backup / terima Alihkan Owner diam-diam MENGHAPUS semua catatan titip/ketinggalan belum diambil, pinjaman belum kembali, & pre-order/jaminan belum terpenuhi (setting produk `requiresDeposit` di `product_units` TIDAK kena — tabel itu memang sudah ada di list, cuma catatan transaksionalnya yang hilang). Fix: tambah 3 tabel itu ke `_allTables`, ditaruh setelah `transactions`/`customers` (parent via FK) supaya urutan delete/insert `restoreFromDump` tetap benar. Test baru `db_export_laci_meja_test.dart` (dump+restore ke DB baru, assert 3 tabel ikut) — revert-verified (gagal sebelum fix dgn `[]` vs `hasLength(1)`, hijau sesudah). Full suite 833/833 hijau, `flutter analyze` 0 issue.

- `593c99c` — fix: pindah `revoked.json` ke GitHub Gist terpisah, bukan lagi di repo `The-POS`. Ditemukan pas user tanya soal rencana private-kan repo: `license_provider.dart` sebelumnya fetch daftar revoke via `raw.githubusercontent.com/Ikanuri/The-POS/main/license/revoked.json` — unauthenticated raw fetch itu SELALU butuh repo public, 404 kalau repo private. Celah lebih dalam: device BARU (belum pernah fetch berhasil) default `cachedRevoked: false`, jadi tetap lolos aktivasi meski fetch gagal — fail-safe di `activate()` cuma melindungi device yang SUDAH pernah ke-cache revoked, bukan device baru. Fix: `_revokedListUrl` sekarang nunjuk ke Gist public terpisah (`gist.githubusercontent.com/Ikanuri/.../raw/revoked.json`, TANPA hash revisi di URL supaya selalu ambil versi terbaru bukan snapshot beku) — visibility Gist independen dari status private/public repo kode, jadi repo boleh private permanen tanpa mematikan kill-switch. Hapus `license/revoked.json` dari repo (bukan lagi sumber kebenaran, cegah drift dua-sumber). Full suite test lisensi (`license_service_test.dart`, `aktivasi_screen_test.dart`, `license_remaining_label_test.dart`) hijau, `flutter analyze` 0 issue.
- `334efd8` — chore: samakan versi `pubspec.yaml` dengan tag rilis resmi yang benar-benar dipublish (`3.0.0+5` → `2.3.0+5`). Ternyata rilis dipublish sbg `v2.3.0` (bukan `v3.0.0` yg dibahas awalnya) setelah ditinjau ulang lewat semantic versioning: tidak ada breaking change di rilis ini (semua migrasi `schemaVersion` 21→25 backward-compatible, tidak ada fitur/API yg dihapus) — jadi MINOR bump (nambah fungsi cara backward-compatible) yang tepat, bukan MAJOR. Update `docs/HANDOFF.md` menyesuaikan (nomor versi, cara publish tag yg tervalidasi bekerja utk sesi non-lokal: web UI Releases, bukan `git push` tag yg diblokir proxy sesi ini, ditambah catatan risiko `revoked.json` — fetch-nya butuh repo PUBLIC, jadi kill-switch lisensi tidak berfungsi selama repo di-private-kan, meski fail-safe designnya mencegah app crash/nge-block salah).

## 2026-07-27

- `18ed4c7` — chore: naikkan versi ke 3.0.0+5 + bersih-bersih menjelang rilis resmi. (1) Hapus 3 dependency tidak terpakai dari `pubspec.yaml`: `riverpod_annotation`+`riverpod_generator` (tidak ada `@riverpod`/`.g.dart` riverpod sama sekali di codebase — semua state pakai `StateNotifierProvider` manual) dan `printing` (0 import — fitur ekspor PDF/Excel sudah lama pindah ke `FilePicker.saveFile`, lihat gotcha CLAUDE.md). (2) Hapus `preview/index.html` & `preview/phase6.html` — mockup desain awal (pre-implementasi), tidak direferensikan kode/dokumen mana pun, app sungguhan sudah lama menggantikannya. (3) Verifikasi codegen bersih: `rm -rf .dart_tool/build` lalu `dart run build_runner build --delete-conflicting-outputs` dari NOL — `app_database.g.dart` yang dihasilkan cocok BYTE-PER-BYTE dgn yang sudah di-commit (tidak ada drift codegen yg terlewat). (4) Update `README.md` (fitur Laci Meja + sinkron harga antar toko + gerbang lisensi belum tercantum, tabel paket masih sebut `riverpod_annotation`/`printing` yg sudah dihapus, skema disebut "v4" padahal sudah v25, struktur folder blm sebut `laci_meja`/`aktivasi`) dan `CLAUDE.md` (schemaVersion hardcode "21" diganti jadi pointer ke kode, bukan angka statis yg cepat basi). Full suite 831/832 test hijau (1 gagal `proposal_unchanged_end_to_end_test.dart` "Address already in use" — flaky port bentrok antar test LAN-sync paralel, TERKONFIRMASI lolos saat dijalankan sendirian, TIDAK terkait perubahan sesi ini), `flutter analyze` 0 issue.
- `e9167b4` — fix: AKAR SESUNGGUHNYA nama pelanggan terpotong di cart bar — `TextPainter` `_MarqueeText` mengabaikan `fontFamily` ambient. User laporan ke-3 (build terbaru, dikonfirmasi diam SELAMANYA bukan cuma jeda): 2 fix sebelumnya (`cfebc31` textScaler, `99f1e88` istirahat-ulang) BELUM menyentuh akarnya. Bukti kunci ada di screenshot sendiri: "Buk Khotimah" tampil "Buk" + RUANG KOSONG LEBAR sebelum tombol ×. Kalau marquee aktif lalu ter-clip krn sempit, yg tampil pasti "Buk Khotim…" mengisi PENUH sampai batas — kata kedua hilang di *word boundary* + sisa ruang kosong = ciri text WRAPPING, bukan clipping. Akar: `widget.style` (`TextStyle(fontSize: 12.5, ...)` dari `_MetaChip`) SENGAJA tidak menyebut `fontFamily`, jadi `Text` mewarisi **Hanken Grotesk** dari `DefaultTextStyle` tema (`GoogleFonts.hankenGroteskTextTheme` di `app_theme.dart`) sementara `TextPainter` yg diberi TextSpan style mentah memakai **font default engine (Roboto)** — Hanken Grotesk lebih LEBAR, jadi painter SELALU under-measure, `overflow <= 0`, jatuh ke cabang `Text` biasa yg (waktu itu) TIDAK punya `softWrap: false` -> teks WRAP di spasi -> `maxLines: 1` menyisakan kata pertama saja. Fix: (1) painter mengukur pakai `DefaultTextStyle.of(context).style.merge(widget.style)` (style yg PERSIS dirender), (2) `softWrap: false` di cabang non-marquee sbg jaring pengaman — kalau pengukuran masih meleset tipis, teks terpotong MEPET (nyaris tak kelihatan) bukan runtuh jadi kata pertama. 2 test baru di `cart_bar_bayar_button_test.dart` — keduanya revert-verified. Test `textScaler` lama dibuat ROBUST (batas dicari empiris, bukan konstanta hardcode "Karti" yg jadi basi begitu pengukuran diperbaiki). Full suite hijau, `flutter analyze` 0 issue.
- `63dd058` — feat: qty desimal di dialog Catat Titip/Ketinggalan (susulan langsung). User: "bagaimana jika barang yang ketinggalan itu bentuk desimal? Misal Filma 4.5kg?" — stepper +/-1 murni (fitur sebelumnya) TIDAK BISA mencapai nilai desimal sembarang (loncat kelipatan 1 dari qty penuh, mis. mulai 4.5 turun jadi 3.5/2.5/1.5, tidak akan pernah pas di angka bulat "2"). Fix: angka qty di `receipt_screen.dart` diganti `TextField` (keyboard `numberWithOptions(decimal: true)`) yang bisa diketik BEBAS, stepper +/-1 tetap ada utk kenyamanan qty bulat & disinkronkan ke teks via `Map<String, TextEditingController>` per item. Sekalian perbaiki bug SUDAH ADA di stepper +1: `qty >= item.qty ? null : qty + 1` bisa MELEBIHI batas kalau sisa < 1 (mis. maks 4.5, qty skrg 4 -> "+1" jadi 5, padahal harusnya klem ke 4.5 pas) — fix `qty + 1 > item.qty ? item.qty : qty + 1`. Gotcha: dispose eksplisit `TextEditingController` tepat setelah `showDialog` selesai menyebabkan "TextEditingController was used after being disposed" (dialog masih animasi keluar) — dibiarkan tak di-dispose eksplisit, konsisten dgn `customerController` yg juga tak pernah di-dispose di fungsi yg sama. Test baru `receipt_catat_laci_meja_test.dart` (produk qty 4.5, ketik ulang & verifikasi klem +1 presisi) — revert-verified. Full suite hijau, `flutter analyze` 0 issue.
- `3d3b843` — feat: qty SEBAGIAN utk Titip/Ketinggalan (permintaan user). User: "kadang yang ketinggalan hanya sebagian (tidak semua)" — checklist barang polos (dari fase sebelumnya) selalu menyiratkan SELURUH qty baris nota. Skema: `LeftBehindItems.qty` baru (`real().nullable()`, migrasi v24->v25, guard `from>=22` pola identik v23/v24 — nullable krn entri LAMA = seluruh qty baris nota, entri BARU selalu mengisi eksplisit). `addLeftBehindItem` & `getLeftBehindMarksForTransaction` (app_database.dart) diperluas bawa qty. Dialog "Catat Titip/Ketinggalan" (`receipt_screen.dart`): `CheckboxListTile` polos diganti `ListTile` + `Checkbox` + stepper +/- (pola SAMA PERSIS `_showReturnSheet`, clamp 1..qty baris nota, default qty PENUH saat dicentang). Penanda struk in-app ("· Dititip"/"· Ketinggalan") & tile dashboard Laci Meja sekarang ikut menampilkan qty SEBAGIAN (bukan qty penuh nota) kalau ada. Test baru: `receipt_catat_laci_meja_test.dart` (stepper qty end-to-end + clamp batas atas), `laci_meja_marks_and_reminder_test.dart` (qty di penanda struk), `laci_meja_dashboard_grouping_test.dart` (qty sebagian di tile dashboard) — semua revert-verified. Migrasi test lama (`migration_v7`..`v24`) disesuaikan assersi versi akhir 24->25; `migration_v24_test.dart` juga dapat tabel `left_behind_items` sintetis (sebelumnya tak dibuat, cocok dgn skema v23 nyata) supaya migrasi v24->v25 (qty) tidak gagal "no such table" saat isolasi test hanya menyentuh `borrowed_items`. Full suite hijau, `flutter analyze` 0 issue.
- `99f1e88` — fix: `_MarqueeText` berhenti PERMANEN setelah 4 putaran, kelihatan spt kepotong lagi. Susulan langsung dari fix textScaler — user kirim screenshot LAIN: nama "Bu Khotimah" tampil "Bu" statis, BUKAN karena overflow-nya salah ukur (sudah diperbaiki commit sebelumnya), tapi krn desain `_maxCycles=4` SENGAJA berhenti selamanya di posisi awal nama setelah 4 putaran (~8-32 detik tergantung panjang nama) — kalau kasir baru lihat layar SESUDAH itu (wajar, tidak nonton terus-menerus), nama terlihat identik dgn bug pemotongan yg sudah "diperbaiki". Fix: `_startCycle` sekarang menjadwalkan `_restTimer` (3 detik) setelah tiap putaran-nyala 4-kali selesai, lalu memanggil dirinya sendiri lagi — bukan berhenti sekali lalu diam selamanya. Tetap DIBATASI per putaran-nyala (bukan `repeat()` tanpa henti sama sekali) krn alasan baterai & `pumpAndSettle` yg sama seperti sebelumnya. Test baru di `cart_bar_bayar_button_test.dart` — polling offset per 100ms mencari pola "bergerak -> diam PANJANG (>=1.5 detik, dibedakan dari jeda baca normal ~0.72 detik di titik balik `repeat(reverse:true)`) -> bergerak LAGI"; pakai nama overflow MINIMAL (bukan nama sangat panjang) supaya durasi 1 putaran diklem ke MINIMUM (2 detik) agar jeda baca normal jauh lebih pendek drpd `_restPause` — revert-verified (gagal sensible "Expected true, Actual false" saat balik ke perilaku berhenti-permanen). Full suite hijau, `flutter analyze` 0 issue.
- `cbfe073` — fix: `_MarqueeText` (nama pelanggan di cart bar) terpotong permanen di skala font besar. User kirim screenshot: nama pelanggan "Buk..." kepotong di kata kedua, bukan bergeser seperti seharusnya. Akar: `main.dart` menerapkan pengali skala font global (`fontScaleProvider` x faktor ukuran layar) via `MediaQuery.textScaler`, tapi `TextPainter` internal `_MarqueeText` (dipakai mengukur apakah nama overflow) tidak menyertakan `textScaler` itu — mengukur di skala 1.0 sementara `Text` sungguhan dirender di skala lebih besar, jadi kadang keliru simpul "muat" padahal SEBENARNYA overflow, dan kode jatuh ke cabang `Text` statis ber-`overflow: TextOverflow.clip` permanen (marquee tak pernah aktif). Fix: tambah `textScaler: MediaQuery.textScalerOf(context)` di `TextPainter` pengukur. Test baru `cart_bar_bayar_button_test.dart` — pakai `pumpKasir` yg sekarang bisa terima `textScale` opsional (meniru pengali `main.dart`), penanda "marquee aktif" pakai ancestor `OverflowBox` (BUKAN `Transform` polos — halaman ini jg punya `Transform` lain dari animasi transisi rute Material 3), nama pendek "Karti" (5 huruf, ditentukan empiris thd widget sungguhan) yg TIDAK overflow di skala normal tapi HARUS overflow di skala 1.4x — revert-verified (gagal sensible "Expected true, Actual false" saat fix dicabut). Full suite hijau, `flutter analyze` 0 issue.
- `81f3b66` — fix: keterangan jaminan pre-order di struk jadi temporary (dibalik dari keputusan sebelumnya). User laporan screenshot: struk masih menampilkan "Titip 10" walau pre-order-nya sudah "Dipenuhi" di dashboard Laci Meja, lalu setelah dikonfirmasi ("itu jaminan preorder btw") user minta pola disamakan dgn Titip/Ketinggalan — TEMPORARY, hilang begitu terpenuhi, BUKAN bukti historis permanen lagi (ini membalik keputusan sesi sebelumnya yg SENGAJA membuatnya permanen atas permintaan user yg sama). `getPreorderDepositForTransaction` (`app_database.dart`) sekarang menambah filter `fulfilledAt.isNull() & cancelledAt.isNull()` — dulu sengaja TIDAK memfilter apa pun. Test `receipt_borrowed_section_test.dart` dibalik assersinya (dulu `findsOneWidget` "jaminan WAJIB tetap ada", sekarang `findsNothing` "jaminan HARUS hilang") — revert-verified. Full suite hijau, `flutter analyze` 0 issue.
- `82af404` — style: bold qty+nama produk di rincian item & rincian jaminan Pre-order. Permintaan user: "produk serta qty bold" di 2 tempat — (1) baris rincian item per-produk di kartu Pre-order (`_preorderTile`, format `"[qty] [produk] - [jaminan]"`) sekarang `Text.rich` dgn span `"$qtyStr $productName"` bold, sisanya (jaminan, status bayar) normal; (2) baris rincian jaminan per produk di `_StatTile` ("Total jaminan", mis. "LPG: 20 jaminan") sekarang `Text.rich` dgn nama produk & qty bold, ": " dan " jaminan" normal — `_StatTile.breakdown` diganti type dari `List<String>` jadi `List<({String name, String qty})>` biar bisa disusun jadi span terpisah. Kedua perubahan `Text`->`Text.rich` memaksa 2 assertion lama disesuaikan `findRichText: true`. Test baru: verifikasi bold via traversal `TextSpan` (helper `findBoldableSpan`) di `laci_meja_dashboard_grouping_test.dart` — revert-verified (2 kasus, masing2 gagal sensible saat bold dicabut). Full suite 825 test hijau, `flutter analyze` 0 issue.
- `df2877f` — feat: statistik Pre-order — "wadah" -> "jaminan" + rincian jaminan per produk. (1) Istilah "wadah dititip" di `_StatTile` sub-label diganti "jaminan dititip" (permintaan user, konsistensi bahasa). (2) Kartu "Total jaminan" sekarang menampilkan rincian per produk di bawah angka total (mis. "LPG: 20 jaminan") — dihitung dari `filtered` (entri hasil pencarian, sama seperti totalDeposit) dgn `depositQty > 0`, di-map ke nama produk via `labels[productUnitId]`. `_StatTile` dapat parameter baru `breakdown` (list baris opsional, default kosong — tidak mengubah tampilan "Total produk"). Test baru di `laci_meja_dashboard_grouping_test.dart` — revert-verified.
- `78bc29a` — fix: padding `ListView` Ringkasan dikembalikan `EdgeInsets.all(16)` seragam. Percobaan ke-3 dari saga gap Ringkasan (sebelum akar sungguhan `TabAlignment.startOffset` ketemu) menekan top padding ke 0px sebagai tebakan buta — sekarang tidak relevan lagi & terlihat janggal (kartu KPI mepet penuh ke TabBar tanpa alasan) setelah fix sungguhannya (`1f7f69e`) sudah menyelesaikan keluhan aslinya. Test `ringkasan_kpi_card_margin_test.dart` disesuaikan. Full suite 822 test hijau, `flutter analyze` 0 issue.
- `0e8f80c` — feat/redesign: cart bar (baris Laci Meja per-kategori + hutang + teks berjalan), pinjaman balik plain text, pencarian & statistik Pre-order. (1) **Pinjaman kembali PLAIN TEXT** (permintaan user, dikonfirmasi via AskUserQuestion): dialog "Catat Pinjaman Barang" balik ke input nama bebas ketik — alasan kuat: yang dipinjamkan biasanya WADAH (galon/tabung kosong) yang justru BUKAN baris di nota, jadi checklist barang-nota (percobaan putaran lalu) bikin barang yg sebenarnya dipinjam tidak bisa dicatat. Konsekuensinya penanda per-baris di struk mustahil (tak ada `transactionItemId`) → diganti SECTION "Pinjaman Barang" tersendiri di struk in-app (`getBorrowedForTransaction`), tetap memenuhi maksud "rujukan kebenaran". Kolom `BorrowedItems.transactionItemId` (migrasi v24) DIBIARKAN ada tapi tak dipakai lagi — schemaVersion tidak diturunkan krn build user sudah terlanjur v24. (2) Jaminan pre-order di nota dipastikan TIDAK hilang setelah "Penuhi" — `getPreorderDepositForTransaction` sengaja tidak memfilter `fulfilledAt` (nota = bukti historis permanen), dikunci test regresi. (3) **Redesain cart bar**: pengingat Laci Meja jadi SATU BARIS PER KATEGORI (`LaciMejaReminder.linesOf`, dulu satu string digabung " · ") — baris pre-order menyebut nama produk + qty + jaminan (diringkas "+N lagi" bila >2 produk); pengingat HUTANG akumulatif (total rupiah + jumlah nota) muncul di BAWAH nominal Total (`cartCustomerDebtProvider`, warna error merah — beda dari dusty rose Laci Meja); baris meta chip TIDAK LAGI melipat (`Wrap`→`Row` ber-`Expanded(flex:)` tetap, chip Pelanggan porsi terbesar) dan nama panjang ditangani `_MarqueeText` (teks berjalan kiri↔kanan, dibatasi 4 putaran lalu diam — animasi abadi membakar baterai POS yang menyala seharian DAN bikin `pumpAndSettle` 10 test kasir timeout). (4) Tab Pre-order dashboard dapat kotak pencarian (cocokkan nama pelanggan ATAU nama produk) + statistik akumulatif total produk & total jaminan sbg dua angka TERPISAH (satuannya beda maknanya), keduanya ikut tersaring hasil pencarian. (5) `getLaciMejaPendingForCustomer`/`ForName` disatukan jadi `getLaciMejaPending({customerId, customerName})` — sekaligus menutup bug lama: jalur `customerId` dulu SELALU mengembalikan `preorder: 0`, jadi pre-order milik pelanggan TERDAFTAR tidak pernah muncul di pengingat mana pun. Test baru: `cart_bar_reminder_lines_test.dart` (3), `receipt_borrowed_section_test.dart` (4), +4 test pencarian/statistik di `laci_meja_dashboard_grouping_test.dart`, +2 marquee di `cart_bar_bayar_button_test.dart`, +3 di `laci_meja_marks_and_reminder_test.dart` — semua revert-verified. Full suite 822 test hijau, `flutter analyze` 0 issue.

- `1f7f69e` — fix: akar gap Ringkasan akhirnya ketemu (TabAlignment.startOffset) + grouping Pre-order/Pinjaman + penanda Pinjaman di struk. (1) **Gap Ringkasan, akar SESUNGGUHNYA**: user beranotasi panah kedua kalinya menunjuk KIRI tab "Ringkasan" (bukan area kartu, 2 percobaan sebelumnya salah sasaran total) — `TabBar(isScrollable: true)` Material 3 default `tabAlignment: TabAlignment.startOffset` menambah inset ~52dp di depan tab pertama (dirancang utk sejajar leading icon, tidak relevan di `LaporanScreen` yg AppBar-nya tanpa leading icon). Fix: `tabAlignment: TabAlignment.start`. Test baru `laporan_tab_left_align_test.dart` — revert-verified (gap sungguhan 68px sebelum fix). (2) Dashboard Pre-order — barang dari NOTA SAMA dikumpulkan jadi 1 Card (pola sama Titip/Ketinggalan), header = NAMA PELANGGAN (bold, sekali per grup, bukan diulang), tiap baris format ringkas `"[qty] [produk] - [qty jaminan]"` (butuh join baru `getProductUnitLabelsFor`). (3) Dashboard Pinjaman — di-group PER-PELANGGAN (bukan per-nota, beda dari 2 kategori lain) — satu pelanggan bisa punya pinjaman dari BEBERAPA nota berbeda, semua kelihatan jadi satu grup, tiap baris tetap tertaut `transactionId` MILIKNYA sendiri. (4) Struk in-app — penanda "Pinjaman" baru di samping nama barang (rujukan kebenaran, tampil TERLEPAS status kembali) — butuh kolom BARU `BorrowedItems.transactionItemId` (schemaVersion 23->24, pola identik `LeftBehindItems.transactionItemId`), yang berarti dialog "Catat Pinjaman Barang" DIROMBAK dari TextField nama bebas jadi checklist barang nyata di nota (pola sama Titip/Ketinggalan) — perlu utk tautan presisi. Test baru: `laporan_tab_left_align_test.dart`, `migration_v24_test.dart`, 6 test tambahan di `laci_meja_dashboard_grouping_test.dart`, `receipt_borrowed_marker_test.dart` (3) — semua revert-verified; `receipt_catat_laci_meja_test.dart` disesuaikan ke checklist Pinjaman baru. Full suite 809 test hijau, `flutter analyze` 0 issue.

- `f19700d` — redesign: Pre-order nyambung ke keranjang/nota (ganti total jalur lama) + gap Ringkasan mepet penuh + label Laci Meja didekatkan ke nama. (1) **Gap TabBar Ringkasan**: user konfirmasi build sudah update tapi gap masih terasa (2 percobaan sebelumnya, 16px lalu 8px, belum cukup) — top padding `ListView` dibuat MEPET PENUH (0), kartu KPI langsung menempel ke TabBar. Test `ringkasan_kpi_card_margin_test.dart` diperbarui — revert-verified. (2) **Redesain Pre-order TOTAL** (permintaan eksplisit user, konfirmasi via AskUserQuestion: ganti 2 jalur lama sepenuhnya): dulu "+ Antri" (pencarian Kasir) & "Catat Pre-order" (Cek Stok) menulis `PreorderEntries` LEPAS dari transaksi (`transactionId` sering null) lewat dialog terpisah `preorder_entry_dialog.dart` — SEKARANG kartu "Pre-order?" baru di `ItemEntrySheet` (modal tap item), muncul HANYA saat produk `markedOutOfStock`: toggle Ya/Tidak (default Tidak) -> toggle "DP?" (Ya = harga penuh & dibayar lunas sekarang; Tidak/default = harga 0, bayar nanti) -> field "Jumlah jaminan dititip" (HANYA muncul bila satuan `requiresDeposit`, default = qty pesanan). `CartItem` diperluas 3 field baru (`isPreorder`, `preorderPaid`, `depositQty`). Checkout (`payment_screen.dart`, KEDUA jalur: transaksi baru & tambah-belanjaan) sekarang menulis `PreorderEntries` LANGSUNG dgn `transactionId` OTOMATIS terisi + item pre-order DIKECUALIKAN dari pengurangan stok (barangnya belum ada fisik — user konfirmasi via AskUserQuestion). Jalur lama ("+ Antri"/"Catat Pre-order", file `preorder_entry_dialog.dart`) DIHAPUS TOTAL, termasuk 2 test lamanya (`kasir_preorder_entry_test.dart`, `cek_stok_preorder_entry_test.dart`, sudah menguji jalur yg dihapus). Label "Titip [qty]" (jaminan) ditambahkan di keranjang & struk in-app, menyatu ke text run nama produk (`Text.rich`, bukan `Text` terpisah+`SizedBox` gap) — permintaan user: posisi persis pola badge "Habis" di katalog kasir. Perubahan struktur `Text`->`Text.rich` ini juga memaksa beberapa test lama (`receipt_item_name_bold_test.dart`, `receipt_qty_unit_bold_test.dart`, `laci_meja_marks_and_reminder_test.dart`) disesuaikan ke `find.textContaining(..., findRichText: true)` / traversal `TextSpan.children`. Test baru: `item_entry_preorder_test.dart` (6), `payment_preorder_checkout_test.dart` (2), `cart_sheet_preorder_deposit_label_test.dart` (1) — semua revert-verified. Full suite 801 test hijau, `flutter analyze` 0 issue.

- `ce1ffc3` — fix: gap TabBar->kartu KPI RingkasanTab, akar ketemu via screenshot beranotasi panah user. Fix margin Card KPI (`673d045`) TERNYATA belum cukup — user konfirmasi build sudah update tapi gap masih terlihat, lalu kirim screenshot dgn panah menunjuk PERSIS jarak antara underline tab "Ringkasan" dan kartu "Omzet". Akar sesungguhnya: `ListView`'s `padding: EdgeInsets.all(16)` sama rata semua sisi — top 16px terasa lebih lebar drpd sisi krn TIDAK ADA elemen visual lain (spt di tab lain) yg mengisi ruang itu. Fix: `EdgeInsets.fromLTRB(16, 8, 16, 16)`, top dipersempit jadi 8. Test baru di `ringkasan_kpi_card_margin_test.dart` — revert-verified.
- `a24156f` — redesign: animasi menu cepat, grouping frame Titip/Ketinggalan, tombol Ambil minimalis. Putaran kedua redesign menu Kasir/Laci Meja: (1) `_QuickMenuPopup` baru (`main_shell.dart`) membungkus menu dgn `FadeTransition`+`ScaleTransition` (durasi 160ms, `Curves.easeOutBack` utk scale, alignment `bottomCenter` biar terasa "muncul dari tab") — sebelumnya nongol/hilang instan tanpa transisi (rigid). Tutup (tap luar/pilih item) menunggu `_controller.reverse()` selesai dulu baru navigasi, biar animasi keluar juga smooth. Test baru cek opacity < 1.0 di tengah animasi lalu 1.0 setelah settle (key `quickMenuFade` dipasang krn `Tooltip` bawaan Flutter JUGA punya `FadeTransition` sendiri — `find.byType().first` tanpa key salah tangkap widget Tooltip) — revert-verified (duration Zero -> opacity langsung 1.0, gagal sensible). (2) Dashboard Laci Meja (`laci_meja_dashboard_screen.dart`), tab Titip/Ketinggalan — barang dari NOTA YANG SAMA sekarang dikumpulkan jadi SATU `Card` (frame), bukan baris rata terpisah spt laporan screenshot user (5 baris nyaris identik sulit dibedakan mana yg satu nota). Tiap baris juga menampilkan qty+satuan produk (join baru `getQtyUnitForTransactionItems` di `app_database.dart`, provider `leftBehindQtyUnitProvider`) — entri lama tanpa `transactionItemId` tetap tampil apa adanya tanpa qty. Test baru `laci_meja_dashboard_grouping_test.dart` (3) — revert-verified. (3) Tombol "Sudah Diambil" diganti `_CollectButton` — pill `StadiumBorder` kecil ikon centang + label "Ambil", minimalis tapi tidak kotak-persegi rigid.
- `673d045` — redesign: menu cepat Kasir/Laci Meja (posisi+ikon+rounded+delay) & 3 perbaikan UI lain. (1) Menu tekan-tahan tab Kasir dirombak total dari `showMenu` (PopupMenuItem teks) jadi `OverlayEntry` custom: muncul DI ATAS bottom bar (posisi dihitung dari `_bottomBarKey`, bukan dari titik jari yg bisa nongol ke samping), HANYA ikon (tanpa label "Buka Kasir"/"Buka Laci Meja"), sudut `ClipRRect` rounded 20, dan delay tekan-tahan dipercepat dari 500ms bawaan Flutter ke 250ms (`RawGestureDetector` + `LongPressGestureRecognizer(duration:)`, `GestureDetector` polos tidak bisa custom durasi). Test lama `laci_meja_bottom_nav_gesture_test.dart` disesuaikan (cari lewat `find.byTooltip`, bukan `find.text`) + 3 test baru (icon-only, posisi+rounded, delay 300ms) — revert-verified. (2) `RingkasanTab` — `Card` KPI diberi `margin: EdgeInsets.zero`; margin bawaan Material 3 (4px) bertumpuk dgn `SizedBox(height:12)` eksplisit antar baris jadi jarak sesungguhnya 20px, tidak konsisten (laporan user "agak ada renggang"). Test baru `ringkasan_kpi_card_margin_test.dart` — revert-verified. (3) `_ReceiptPaper` (struk share/gambar) — baris qty+satuan+harga tidak lagi bold (dulu w600); hanya nama produk tetap bold. Test baru `receipt_paper_qty_not_bold_test.dart` — revert-verified.
- `9d238fc` — fix: cart bar Bayar tetap di kanan saat 2 baris + keterangan Laci Meja bedakan titip/ketinggalan. Dua penyesuaian susulan dari user: (1) `_CartMetaTab` — segmen "Bayar" dikeluarkan dari `Wrap` chip Pelanggan/Pegawai/Tahan (`Expanded(Wrap(...))` di kiri, Bayar jadi elemen `Row` terakhir) supaya tetap menempel kanan berapa pun baris chip di kirinya melipat; test baru `cart_bar_bayar_button_test.dart` (nama pelanggan sangat panjang, verifikasi tepi kanan tombol Bayar tidak bergeser) — revert-verified. (2) `LaciMejaReminder`/`getLaciMejaPendingForCustomer`/`getLaciMejaPendingForName` — record `titipKetinggalan` gabungan dipecah jadi `titip`+`ketinggalan` terpisah (dihitung per `jenis` baris `LeftBehindItems`), supaya keterangan modal checkout/cart bar menulis "N barang ketinggalan" utk barang yang memang `jenis='ketinggalan'`, bukan selalu "dititip". Test baru di `laci_meja_marks_and_reminder_test.dart` (2) — revert-verified.
- `45ede18` — fix: bugfix checkout QR + 4 perbaikan UI Laci Meja. **BUGFIX KRITIS**: checkout gagal total setelah transfer QR bolak-balik (owner->asisten->owner) dgn "UNIQUE constraint failed: transactions.local_id" — transaksi tidak bisa diselesaikan sama sekali. Akar: nomor nota bawaan QR cuma disimpan di `CartMeta.reservedLocalId` TANPA dicatat ke `reserved_order_numbers` device penerima, jadi `reserveLocalId` berikutnya membagikannya lagi ke keranjang lain; keranjang itu checkout duluan & nomornya jadi milik `transactions`. Fix dua lapis: `adoptReservedLocalId` di KEDUA jalur terima (scan QR ke antrian & Tempel Pesanan), + penjaga di checkout (nomor reservasi basi jatuh ke nomor bebas berikutnya, tidak menggagalkan penjualan). UI: (1) cart bar chip Pelanggan/Pegawai/Tahan/Bayar jadi `Wrap` — nama panjang dulu mendorong tombol Bayar keluar layar; baris ringkasan boleh 2 baris; (2) nama pelanggan di kartu Laci Meja ber-aksen terracotta, dibedakan dari warna umur; (3) penanda "Dititip"/"Ketinggalan" per-barang di struk in-app (pola badge "Habis"), butuh kolom `LeftBehindItems.transactionItemId` (schemaVersion 22->23) utk tautan PRESISI ke baris nota — cocok-nama salah tandai kalau produk sama muncul beberapa kali dgn satuan berbeda (revert-verified); (4) `LaciMejaReminder` di modal checkout & cart bar, meniru pengingat hutang tapi warna dusty rose supaya tidak tertukar. Test baru: handoff_reserved_local_id_collision_test.dart (4), migration_v23_test.dart, laci_meja_marks_and_reminder_test.dart (7).
- `3c7df58` — fix: Laci Meja ikut pola Buku Hutang — pindah ke dalam shell + nama dari nota. Dua laporan user dari device asli: (1) **redirect ke nota menampilkan halaman BLANK** — versi awal menaruh `/laci-meja` di LUAR `ShellRoute` sedangkan `/kasir/struk/:txId` bersarang DI DALAM shell; push lintas batas shell memunculkan halaman kosong di device (shell baru ter-mount, body tidak pernah terisi). Mengikuti arahan user "bandingkan dgn pendekatan laporan hutang": Buku Hutang (`/laporan` -> HutangTab -> push `/kasir/struk/:txId`) selalu push ANTAR-RUTE DI DALAM SATU shell — dashboard Laci Meja dipindah ke dalam shell sbg `/kasir/laci-meja` (anak `/kasir`, sebelah `/kasir/struk/:txId`) jadi jalurnya identik; bottom nav ikut tampil, konsisten dgn layar Struk. Pelajaran: test navigasi dgn router TIRUAN (tanpa ShellRoute) TIDAK PERNAH bisa menangkap kelas bug batas-shell ini — wajib pakai `routerProvider` asli. (2) **nama pelanggan tidak tampil di kartu dashboard** — field pelanggan dulu dikosongkan & harus diketik manual; sekarang DIWARISI dari nota (dialog pra-isi nama pelanggan nota + `customerId` nota ikut disimpan, bukan cuma teks), nota "Umum" tetap bisa diisi manual. Berlaku utk Titip/Ketinggalan & Pinjaman Barang. Test `receipt_catat_laci_meja_test.dart` +1; path rute disesuaikan di 2 test navigasi.
- `35495aa` — fix: Laci Meja — Titip/Ketinggalan ditaut ke produk struk, dashboard redirect ke nota. Koreksi user thd 2 kesalahpahaman desain awal: (1) dialog "Catat Titip/Ketinggalan" diganti dari TextField nama bebas jadi checklist produk NYATA yang ada di nota (qty>0, retur dikecualikan) — toggle centang 1+ barang, tiap barang jadi satu baris `LeftBehindItem` terpisah dgn `itemName` = nama produk asli; (2) tap kartu di dashboard Laci Meja sekarang redirect ke struk terkait (mekanisme sama persis `HutangTab` -> `/kasir/struk/:txId`) — Titip/Ketinggalan & Pinjaman selalu redirect (transactionId NOT NULL), Pre-order hanya kalau transactionId ada (kasus titip wadah tanpa beli apa pun sengaja tidak navigasi, bukan crash). Test `receipt_catat_laci_meja_test.dart` diperbarui, `laci_meja_dashboard_redirect_test.dart` baru (3 test) — revert-verified.
- `d25b8f2` — feat: Laci Meja (Item 52 susulan) — layar review usulan client->host. Bagian yang sengaja ditunda saat eksekusi awal Item 52: `LaciMejaProposalReviewScreen` (PARALEL dari `ProductProposalReviewScreen` Item 40, antrian & UI terpisah) — daftar per-kategori (Titip/Ketinggalan/Pinjaman/Pre-order) dgn checkbox default tercentang, "Terapkan" memanggil `LanSyncService.applyLaciMejaProposal`. `SyncState`/`SyncStateNotifier` diperluas dgn `laciMejaProposals` (wired ke `onLaciMejaProposalsChanged`). Kartu "Usulan Laci Meja (N)" baru di `SyncScreen`, banner status sync ikut menghitung antrian ini. Test `laci_meja_proposal_review_test.dart` (2 test, pola sama `sync_screen_proposal_layout_test.dart`: seam `debugAddLaciMejaProposal`, tanpa host/HTTP sungguhan) — revert-verified. Dengan ini seluruh scope Item 52 SELESAI TOTAL.
- `ec7257e` — test: perbarui test migrasi lama utk schemaVersion 22 (Item 52). Item 52 menaikkan schemaVersion 21->22; 11 test migrasi lama (v7-v21) men-assert `PRAGMA user_version` akhir yang hardcode 21, sekarang 22. 7 di antaranya (v13/v14/v15/v16/v17/v18/v21) sempat gagal dgn error lebih serius sebelum itu disadari ("no such table: product_units") krn sintetis DB mereka tidak mendeklarasikan tabel yang disentuh migrasi v22 — ditambahkan `CREATE TABLE product_units` minimal, mengikuti konvensi "diperlukan agar migrasi vNN tak gagal" yang sudah ada di file-file itu. Bukan bug produksi, murni penyesuaian test sintetis.
- `8f9e667` — feat: Laci Meja (Item 52) fase 7 — toggle "Butuh Jaminan Fisik saat Antri" di form Edit Produk (per kartu satuan, sejajar "Lacak stok"), menulis ke `ProductUnits.requiresDeposit`. Default OFF. Test `produk_form_requires_deposit_toggle_test.dart`
- `e5fe487` — feat: Laci Meja (Item 52) fase 6 — dua jalur entry Pre-order: "+ Antri" inline di baris pencarian Kasir (tampilan list, muncul hanya saat stok habis, transactionId null) & "Catat Pre-order" di layar Cek Stok (baris terpisah dari stepper qty order restock, hindari overflow HP 360px). Dialog dibagikan (`preorder_entry_dialog.dart`) dgn validasi wajib: produk `requiresDeposit=true` tidak bisa disimpan tanpa jumlah wadah dititip minimal 1. `CatalogDetail`/`StockOverviewRow` diperluas dgn `requiresDeposit`. Test `kasir_preorder_entry_test.dart`, `cek_stok_preorder_entry_test.dart` — revert-verified
- `50d778f` — feat: Laci Meja (Item 52) fase 5 — tombol "+ Catat" (satu ikon gabungan, bukan dua) di app bar Struk membuka bottom sheet pilihan Titip/Ketinggalan atau Pinjaman Barang, keduanya menumpang transactionId nota yang sedang dibuka (bukan nota terpisah). `locallyModified` mengikuti pola usulan produk Item 40 (true kalau device bukan owner). Test `receipt_catat_laci_meja_test.dart` — termasuk catatan gotcha `db.watchXxx().first` yg bikin widget test hang saat drain, diganti one-shot select
- `955551c` — feat: Laci Meja (Item 52) fase 4+8 — dashboard `/laci-meja` (3 kartu ringkasan tappable sekaligus filter, list FIFO dgn warna umur) & gesture tekan-tahan tab "Kasir" di bottom nav (ala Telegram) membuka menu "Buka Kasir"/"Buka Laci Meja" — tap singkat tetap navigasi normal. Badge jumlah gabungan selalu tampil di ikon Kasir. Warna baru `AppTheme.laciFg/laciBg` (dusty rose). Test `laci_meja_bottom_nav_gesture_test.dart` (5 test, termasuk regresi eksplisit overlay-menelan-tap) — revert-verified
- `2411e17` — feat: Laci Meja (Item 52) fase 1-3 — skema (schemaVersion 21->22: `LeftBehindItems`/`BorrowedItems`/`PreorderEntries` + `ProductUnits.requiresDeposit`), DB layer CRUD dasar (FIFO murni by createdAt, `paid` hanya informatif), sync host->klien auto-merge (pola products/customers) & klien->host via antrian usulan PARALEL (tidak menyentuh alur usulan produk Item 40). Bug nyata ketemu & diperbaiki selama build: `applyLaciMejaProposals` awalnya `customInsert` tanpa param `updates:` — data tertulis benar tapi `.watch()` tidak refresh (gotcha yg sama sudah didokumentasikan CLAUDE.md, ternyata masih lolos ke kode baru). Test `migration_v22_test.dart`, `laci_meja_db_test.dart`, `laci_meja_sync_test.dart` — revert-verified

## 2026-07-26

- `69b7ffc` — fix: codegen Drift tidak pernah memperbarui `app_database.g.dart`. Sejak `dd4bad3` (17 Juli) blok anotasi `@DriftDatabase(tables: [...])` tidak lagi menempel di `class AppDatabase`: beberapa `typedef` (`StockOverviewRow`, `OpnameSessionSummary`, dst) + `class BarcodeConflictException` tersisip DI ANTARA blok anotasi dan class-nya, jadi anotasinya diam-diam menempel ke `typedef StockOverviewRow`. Dart menganggap ini sah, jadi bug-nya sepenuhnya senyap: `flutter analyze` 0 issue, semua test hijau, `build_runner` tetap lapor "Succeeded" dgn ribuan output, tanpa satu pun pesan error — padahal drift_dev tidak menemukan database sama sekali (`elements` kosong di `app_database.dart.drift_elements.json`) sehingga `app_database.g.dart` tidak pernah ditulis ulang, dan setiap penambahan tabel/kolom baru diam-diam tidak ikut ter-generate. Sempat salah didiagnosis sbg bug alat/sandbox. Fix: blok `@DriftDatabase` dipindah langsung di atas `class AppDatabase`, typedef/class lain digeser ke atasnya — tanpa perubahan skema/perilaku. `app_database.g.dart` diregenerasi (drift_dev 2.23.1); selisihnya murni formatting + propagasi doc-comment, diverifikasi tidak ada kolom/tabel hilang atau bertambah (103 nama kolom & seluruh class `$...Table` identik). Alat diagnostik tercepat utk kelas bug ini: `dart run drift_dev identify-databases` (output kosong = anotasi tidak terbaca). Pagar regresi `drift_codegen_in_sync_test.dart` — revert-verified
- `256f61c` — fix: kembalian terakhir di Ringkasan struk in-app di-bold. Usulan user: nominal kembalian TERAKHIR (baris Ringkasan atas, yang harus benar-benar diserahkan ke pelanggan sekarang) perlu menonjol dari baris kembalian per-pembayaran di kartu Riwayat Pembayaran (cuma catatan historis). `_ChangeTakenRow` dapat parameter `bold` baru (default false, tidak mengubah baris Riwayat Pembayaran), dipakai `true` hanya di baris Ringkasan. Test baru `receipt_change_taken_bold_test.dart` — revert-verified
- `a64f1bf` — feat: produk utama bisa diset non-stok + jeda pelacakan stok semua produk. User tanya: fitur non-stok cuma ada di varian (`_variantDialog` "Lacak stok varian"), produk utama tidak bisa. Tambah toggle "Lacak stok" yang sama per kartu satuan di form Edit Produk (`_UnitCard`) — berlaku utk satuan dasar maupun satuan tambahan, ditulis ke `ProductUnits.isNonStock` persis spt varian. Susulan permintaan user: "set semua produk ke non stok untuk sementara" — toggle baru "Jeda Pelacakan Stok" di Pengaturan > Manajemen Data, set SEMUA satuan yang masih dilacak jadi non-stok sekaligus, dgn snapshot id yang diubah disimpan di `app_settings` supaya bisa dipulihkan PERSIS; satuan yang MEMANG sudah non-stok sebelumnya (mis. varian jasa) sengaja tidak ikut tersentuh baik saat dijeda maupun dipulihkan. Reversibel & idempoten. Test baru `produk_form_non_stock_toggle_test.dart`, `stock_pause_db_test.dart`, `pengaturan_stock_pause_toggle_test.dart` — revert-verified
- `ce2c427` — fix: pembayaran dibatalkan tidak lagi ikut tercetak/ter-share di struk. Dilaporkan user via foto struk kertas: pembayaran yang dibatalkan (mis. salah input nominal, dibatalkan lalu diulang) ikut tercetak sbg baris "Tunai" biasa di struk fisik & struk gambar (share) — kertas termal tidak bisa menampilkan coretan "Dibatalkan" spt kartu Riwayat Pembayaran in-app, jadi pelanggan melihat beberapa baris Tunai identik tanpa tahu sebagian sudah batal, seolah dibayar berkali-kali. Akar: `_visiblePayments` (`receipt_screen.dart`, dipakai share) & `visiblePayments` (`printer_service.dart`, dipakai cetak fisik) sudah filter method 'edit'/'retur' tapi lupa filter `voided` — beda dari `_refundTotal`/`_refundMethod` di file yang sama yang sudah benar. In-app (kartu Riwayat Pembayaran) sengaja tetap tampilkan semua dgn coretan, jalur itu tidak disentuh. Test baru `receipt_paper_voided_payment_hidden_test.dart` — revert-verified

## 2026-07-25

- `1176d97` — feat: layar Cek Duplikat Data (Pengaturan > Diagnostik). Dipicu temuan nyata user: produk "Amplop" punya 2 barcode berlabel Primer sekaligus di device HOST — ditelusuri sampai `restoreFromDump` (fitur restore backup), yang menimpa SELURUH DB lokal verbatim dari file backup TANPA cek invarian apa pun; host pernah restore backup FULL dari device klien yang `product_barcodes`-nya sudah basi akibat bug orphan-cleanup sinkron (`6455b9a`), jadi duplikatnya ikut terbawa mentah-mentah. `AppDatabase.findMasterDataDuplicates()` memindai `product_barcodes` (>1 baris `isPrimary` per satuan), `price_tiers` (min_qty dobel per satuan), `alt_prices` (label dobel per satuan) — 3 tabel yang sama-sama full-dump tanpa `updated_at` sehingga rentan kelas masalah yang sama. Layar baru (owner-only, Pengaturan > Diagnostik) melaporkan produk yang kena + tautan ke Edit Produk. Sengaja HANYA melapor, TIDAK menghapus otomatis — baris mana yang benar tidak bisa ditentukan cuma dari data lokal (mis. barcode mana yang labelnya sudah tercetak), owner yang tinjau & simpan ulang (logika delete-lama-insert-baru bawaan `saveProduct` otomatis merapikan jadi 1 baris). Test baru `master_data_duplicate_detection_test.dart` (5 kasus DB-level) + `duplicate_data_screen_test.dart` (2 kasus widget) — revert-verified
- `6455b9a` — fix: barcode/tier grosir/Harga Lain lama tidak dihapus di klien setelah sync (dilaporkan user: "owner edit barcode, tapi setelah sync ke klien barcode itu tidak ikut berubah"). Akar: `saveProduct` hapus baris lama + insert baris baru (id UUID baru) saat barcode/tier/Harga Lain diedit, bukan update in-place — dan `product_barcodes`/`price_tiers`/`alt_prices` selalu full-dump tanpa `updated_at` (lihat `dumpSince`), jadi `INSERT OR REPLACE` di `mergeRows` tidak pernah menghapus baris lokal yang sudah tak ada di payload; persis pola bug yang sudah pernah terjadi & diperbaiki di `product_group_tags` (luput ditutup di sini). Fix: sweep orphan-cleanup yang sama (hapus baris lokal yang id-nya tak ada di payload full-dump — payload full-dump = kebenaran LENGKAP host) diterapkan ke ketiga tabel, tetap hormat `protectedUnitIds` (skip unit yang `locally_modified`, usulan blm di-approve owner) agar edit lokal yang belum direview tidak ikut kehapus. `product_units` sendiri (unit/satuan yang di-hard-delete) SENGAJA belum ikut fix ini — anak-anaknya (price_tiers/alt_prices/product_barcodes/customer_group_prices) referensi FK RESTRICT ke situ, urutan hapus perlu lebih hati-hati, didokumentasikan sbg item terpisah di HANDOFF. Test baru `orphan_master_data_sync_test.dart` (3 kasus: barcode diganti, tier dihapus, guard proteksi usulan) — revert-verified
- `d3e5e6f` — fix: memilih chip Harga Lain mematikan highlight satuan yang sedang aktif (dilaporkan user: "tidak tahu satuan mana yang sedang dipilih"). Akar masalah SUDAH ADA sejak `6564852` jauh sebelum sesi ini — chip satuan pakai `selected: i == _selectedIdx && !_priceOverridden`, begitu chip Harga Lain dipilih (`_priceOverridden=true`) highlight satuan ikut mati walau satuannya sendiri tidak berubah; baru KETARA sekarang krn chip Harga Lain sesi ini selalu tampil (dulu tersembunyi popup). Fix: `selected: i == _selectedIdx` saja, satuan aktif independen dari harga yg dipakai. Test baru di `item_entry_price_menu_test.dart` (baca warna `Container.decoration.color` sebelum/sesudah tap, harus identik) — revert-verified: `Color(0x1fc96442)` accent vs `Color(0xffebe8e0)` netral saat bug direproduksi
- `1dfd159` — fix: `setMarkedOutOfStock` tidak mencap `updated_at` (host->klien) — kelas bug SAMA PERSIS dgn `deactivateProduct`/`applyProductProposals` (tercatat 2x di CLAUDE.md), tanpa cap ulang `dumpSince` (filter `WHERE updated_at >= since`) tidak pernah lagi menyertakan produk yg ditandai habis owner begitu watermark klien lewat dari edit terakhirnya. Fix ini HANYA menutup arah host->klien; investigasi arah client->host (ditanyakan user) didokumentasikan terpisah di HANDOFF krn ternyata BUKAN bug watermark — celah arsitektur: `syncToHost` kirim `dumpSince(includeMasterData:false)` (products = master data, sengaja tak diunggah klien) & `setMarkedOutOfStock` tidak memanggil `markProductLocallyModified` shg juga tak ikut jalur usulan Item 40; perlu keputusan desain, bukan cuma tambahan kode. Test baru `marked_out_of_stock_sync_test.dart` (2 kasus, meniru `product_deactivate_sync_test.dart`) revert-verified
- `28267c0` — feat: Harga Lain di kasir jadi chip langsung terlihat, bukan popup menu (klarifikasi user via screenshot kedua — bukan minta balik ke chip menumpuk lama yg Item 19 sengaja hindari, tapi tiap opsi harga tampil sbg chip sendiri yg langsung kelihatan semua, persis "Pilih satuan"). `_PriceChip` (sudah ada, dipakai chip satuan) dipakai ulang apa adanya. `_buildPriceMenuButton`/`PopupMenuButton<int>`/`_selectedPriceLabel` dihapus total. Baris baru "Pilih harga" (horizontal scroll semua `_priceOptions()`, termasuk "Harga dasar" pertama, hanya muncul kalau >1 opsi) dipindah jadi baris tersendiri selebar penuh setelah Qty & Harga (bukan lagi mepet kolom Harga yg cuma setengah lebar). `item_entry_price_menu_test.dart` ditulis ulang total (bukan disesuaikan) — revert-verified vs git HEAD: 2/3 gagal di versi lama
- `7e5acec` — feat: Opsi A stepper qty (mockup) + kecualikan kategori dari output Cek Stok (2 permintaan user). Redesain visual: satu jalur menyatu ber-latar token `field` app (`inputDecorationTheme.fillColor`, otomatis benar 2 mode), qty pakai `AppTheme.numStyle` (Newsreader+tabular figures, token numerik wajib app yg dulu diabaikan di layar ini), satuan jadi `PopupMenuButton` bukan `DropdownButton`. Sekalian: kartu tercentang dulu memakai warna keparahan stok (badgeBg/badgeFg) utk "terpilih" (produk kritis dicentang jadi merah-di-atas-merah) -> sekarang selalu accent terracotta, badge stok tetap independen. BUG NYATA ditemukan+diperbaiki saat implementasi (ketahuan sebelum sempat commit): tombol minus nonaktif sempat `onTap: null` utk meniru "beku", tapi `InkWell(onTap: null)` tidak menyerap gesture -> tap menembus ke `CheckboxListTile` pembungkus & MEMBATALKAN CENTANG seluruh baris; fix `_StepGlyph.onTap` non-nullable selamanya, `enabled` cuma pengaruhi opacity. Fitur baru: chip ✓/✕ per kategori (Key `outcat-\$id`, label sama persis dgn chip filter atas jadi perlu Key utk dibedakan) muncul di atas kotak teks kalau ≥2 kategori bernama, meniru `skCatExcluded`/`sk-outcat-bar` acuan; disimpan blob JSON di `app_settings` key `cek_stok_excluded_output_groups` (pola `saved_catalogs`, tanpa migrasi); produk kategori dikecualikan TETAP tercentang & tampil normal, hanya tidak ikut teks maupun parser dua-arah (baris di-`continue` penuh). Visibilitas PANEL sengaja pakai centang MENTAH bukan yg sudah disaring kategori, supaya produk yg semua kategorinya kebetulan dikecualikan tidak bikin panel & chip sertakan-baliknya hilang total. Test baru: 2 kasus di `cek_stok_order_qty_test.dart` (regresi tombol minus, kartu tercentang harus identik lepas dari keparahan stok) + `cek_stok_output_category_exclude_test.dart` (6 kasus) — semua revert-verified
- `a866ff1` — feat: filter Semua/Dicentang/Belum di Cek Stok (usulan user), TEGAK LURUS dgn filter kategori shg keduanya bisa dipakai bersamaan (mis. Sembako + belum dicentang). Dua hal yang butuh perhatian & sudah ditutup test: (1) filter status HANYA menyaring DAFTAR yang tampil, TIDAK teks order — `_lastRows` (dipakai penyusun teks & parser dua-arah) sengaja diisi nilai stream MENTAH, penyaringan lokal di dalam `data:` builder; kalau teks ikut disaring, memilih "Belum" bikin teksnya kosong dan parser dua-arah akan MEMBATALKAN SEMUA centang yang sudah dikumpulkan user. (2) Barisnya `Row` + `Expanded`, BUKAN ListView horizontal spt baris kategori: versi ListView terukur mendorong chip terakhir sampai R520 di layar 430px (~90px di luar layar, hit test MELESET, opsi ketiga tidak bisa ditekan sama sekali) dgn label berangka, dan masih lewat 3,75px di 360px walau label sudah dipendekkan; krn opsinya tetap 3, membaginya rata membuat ketiganya pasti muat di lebar apa pun. Hitungan angka dipindah ke judul panel (`Teks Order Restock — N produk`), tempat yg lebih berguna & tidak memakan lebar chip. Test baru 5 kasus di `cek_stok_order_qty_test.dart` termasuk test layout di surface 360x800 (`getRect(...).right <= 360` + tap yang harus benar2 mengenai) — test tanpa surface sempit TIDAK menangkap kelas bug ini; revert-verified dgn menjalankan file test melawan `cek_stok_screen.dart` versi git HEAD: 7 test qty tetap lolos, tepat 5 test filter gagal. `cek_stok_screen_test.dart` disesuaikan ke `find.textContaining` krn judul panel kini memuat jumlah produk
- `6fa4886` — fix: rombak Order Restock di Cek Stok — jumlah diisi owner, satuan selalu ada, teks dua arah. User membandingkan dgn HTML acuan yang dia kirim & menemukan versi pertama Item 4 SALAH KONSEP: qty diambil dari STOK dibagi rasio & tidak bisa di-set sama sekali (data nyata user stok minus -> `-104 Pres Lawet Ijo` / `0 Pres Lawet Ijo`), plus produk bersatuan tunggal jatuh ke `- Nama` polos padahal di acuan SETIAP item tercentang selalu punya qty+satuan. Sesudah membaca HTML-nya (fitur "Order ke Karyawan"): tiap produk tercentang dapat baris `[−] [qty] [+] [satuan ▾]`, qty awal 1 (bukan stok), minus MEMBEKU di 1 & tidak pernah desimal (sama `skAdjustQty`), angka diketuk utk mengetik lewat dialog (setara `openCalc`, field-nya select-all), dan teks order jadi textarea EDITABLE DUA ARAH (`skOnOutputChange`): tiap baris di-parse jadi centang+qty+satuan, baris dihapus = produknya ter-uncheck, format `{qty} {satuan} {nama}` dgn fallback seluruh-baris-sbg-nama (qty 1) & bentuk lama `- Nama` tetap dimengerti. Beda dari acuan atas keputusan user: satuan dari satuan MILIK produk dulu lalu daftar umum `unit_types` (bukan 13 nama hardcode acuan) supaya tetap nyambung ke master data — efeknya produk bersatuan tunggal pun dapat pemilih satuan. Parser di-debounce 600ms krn tiap baris cocok = tulis `markedOutOfStock` ke DB & tiap tulis memicu stream emit ulang (tanpa debounce: 1 ketikan = beberapa tulis DB + rebuild yang berebut dgn ketikan user); teks tidak ditimpa selama fokus di textarea kecuali saat fokus dilepas. Konversi rasio tidak dipakai lagi di layar ini. Sekalian: warna nama satuan dulu dipaksa `Colors.black87` -> nyaris tak terbaca di mode gelap, sekarang ikut tema. `cek_stok_unit_output_test.dart` DIHAPUS (2 dari 3 test-nya mengunci desain yang user batalkan), diganti `cek_stok_order_qty_test.dart` (7 kasus) — revert-verified dgn menjalankan test baru melawan file versi git HEAD: 7/7 gagal di versi lama, 7/7 lolos di versi baru
- `e8bf216` — fix: pemilih satuan Cek Stok tidak muncul utk produk yang SUDAH tercentang (dilaporkan user lewat screenshot: produk tercentang tapi "satuan tidak ada", teks order turun jadi `- Gajah Baru` polos) + tuntaskan tie-break Item 38 di 6 query raw SQL. (1) Satuan HANYA dimuat di `_toggle` (saat checkbox ditekan), jadi produk yang `markedOutOfStock`-nya sudah true sejak layar dibuka tidak pernah lewat situ -> `_unitsByProduct` kosong -> dropdown tidak dirender & `_buildOrderText` jatuh ke cabang `- {nama}`; diprobe dulu (produk Pcs/Dus pre-checked = 0 `DropdownButton`). Fix: `_ensureUnitsForChecked` dibaca dari nilai stream yang sudah di-`watch` (`WidgetRef.listen` TIDAK punya `fireImmediately` shg emisi PERTAMA — yang justru berisi produk tercentang — malah terlewat), memuat semua produk tercentang dlm SATU query batch via `getUnitsWithTypeNamesFor`; `_ensureUnitsLoaded` lama mendelegasi ke jalur batch yang sama (satu code path). (2) Ketemu saat menelusuri (1): fix Item 38 sebelumnya TIDAK LENGKAP — hanya `_rawBaseStock` yang diperbaiki, sedangkan 6 query raw SQL lain masih `id DESC`, termasuk `watchStockOverview` yang MEMASOK layar Cek Stok & Stock Opname, plus `getInventoryRows` & jalur saldo non-base; akibatnya `currentStock` (sudah benar) dan angka yang TAMPIL di layar bisa BERBEDA utk produk dgn 2 penulisan stok di detik yang sama. Keenam situs kini pakai `rowid`. Test: kasus pre-checked di `cek_stok_unit_output_test.dart` (revert = 0 `DropdownButton`) & kasus `currentStock` vs `watchStockOverview` di `stock_ledger_tiebreak_test.dart` (revert = Expected 100, Actual 5.0) — revert-verified
- `f3f71e8` — feat: nama produk di banner bentrok barcode bisa diketuk -> redirect ke produk itu (usulan user). `InlineBanner` dapat 2 param opsional `linkText`/`onLinkTap` — potongan pesan yang cocok dirender sbg `TextSpan` ber-`TapGestureRecognizer` (accent + bold + underline); kalau `linkText` tidak ditemukan di `message`, otomatis jatuh ke teks biasa (tidak pernah kosong/error), dan 20 pemakai `InlineBanner` lain tidak tersentuh krn kedua param opsional. Dua konsekuensi yang dituntut fiturnya: (a) selama `onLinkTap` di-set banner TIDAK auto-dismiss — 4 detik tidak cukup utk membaca lalu mengetuk & banner yang hilang sendiri bikin aksinya mustahil diraih (banner tanpa aksi tetap auto-dismiss, ada test regresinya); (b) recognizer-nya SATU objek yang `onTap`-nya diganti tiap build lalu di-dispose di `dispose()` (bikin baru di build = bocor). `BarcodeConflictException` dapat field `productId` supaya UI bisa `context.push`; navigasinya PUSH di atas form shg isian yang belum tersimpan tetap utuh saat kembali (alur: ketuk nama -> bebaskan barcode di produk itu -> kembali -> simpan). Sekalian diperiksa `createVariant` juga insert polos tanpa pre-delete spt `updateVariant`, jadi `saveProduct` benar2 satu-satunya jalur yang punya bug curi-barcode. Test baru `barcode_conflict_banner_link_test.dart` (5 kasus, termasuk end-to-end lewat router asli yang membuktikan ketukan benar2 membuka form produk pemegang barcode) revert-verified: fitur dimatikan -> 3 test gagal, 2 test regresi tetap lolos; revert-verify itu sekalian mengungkap cacat di test auto-dismiss versi pertama (lolos walau fitur mati krn `message` sama di kedua `pumpWidget` shg timer tak pernah di-arm) — sudah diperbaiki
- `b277190` — fix: barcode ganda lintas produk diam-diam MENCURI barcode produk lain (dilaporkan user: "dua produk barcode sama lolos"). Diprobe: produk kedua mengambil barcode dari yang pertama (A punya 0 barcode, B punya 1, `lookupBarcode` -> B) tanpa error — produk lama jadi tak bisa di-scan & scan kode itu menagih produk SALAH di kasir. Akar masalah: `saveProduct` menjalankan `DELETE ... WHERE barcode = value` polos semata-mata utk menghindari `UNIQUE(barcode)`, termasuk kalau baris itu milik produk lain. Fix: helper `_claimBarcodeFor` — bentrok lintas-produk yang pemegangnya masih AKTIF dilempar sbg `BarcodeConflictException` baru (transaksi rollback total, barcode produk lain tidak tersentuh, pesan menyebut nama produk pemegangnya); `produk_form_screen.dart` menangkapnya lewat `on BarcodeConflictException` + `_friendlyBarcodeError` diperluas. Kasus reuse SAH sengaja tetap jalan (ada test masing2): produk pemegang sudah dinonaktifkan (`_releaseBarcodesForProduct` me-rename `RELEASED:...`), barcode dipegang produk ITU SENDIRI sbg alias `isPrimary=false` dari sinkron harga lalu dipromosikan, dan simpan-ulang produk yang sama. Jalur lain diperiksa & bersih: `updateVariant` (update/insert tanpa pre-delete -> gagal keras, tanpa kehilangan data), sinkron LAN tidak lewat `saveProduct`, impor CSV sudah try/catch per baris shg baris bentrok kini dilaporkan sbg baris gagal. Test baru `barcode_cross_product_conflict_test.dart` (5 kasus) revert-verified: DELETE polos dikembalikan -> 2 test bentrok gagal, 3 test kasus sah tetap lolos
- `6993b7d` — fix: dua temuan audit pra-rilis. (1) `stock_opname_screen.dart` `_loadUnits()` N+1 BERLAPIS (`getProductUnits` per produk + select `unit_types` per satuan) padahal layar "Hitung Fisik" menahan SELURUH barisnya di balik `CircularProgressIndicator` sampai loop itu habis — terukur 1000 produk = 3000 query/441ms di SQLite memori x86 (jauh lebih parah di HP kelas bawah: SQLCipher mendekripsi tiap page + I/O file), diganti satu query JOIN lewat `AppDatabase.getUnitsWithTypeNamesFor` baru; `coalesce(unit_type_id, 1)` & urutan `rowid` sengaja MEMPERTAHANKAN perilaku lama `u.unitTypeId ?? 1` + urutan insert apa adanya (indeks dropdown & `indexWhere(isBaseUnit)` bergantung padanya) supaya refactor ini murni soal jumlah query; fitur sebelahnya (`cek_stok_screen.dart`) sudah lazy per-produk sejak awal jadi tidak diubah. (2) Laporan user "nol pra-cursor tidak terhapus": dialog "Sesuaikan Stok" (`produk_form_screen.dart`) field-nya `autofocus` TAPI sudah terisi stok sekarang shg cursor mendarat di UJUNG angka — mengetik MENEMPEL bukan mengganti; untuk stok non-nol ini bahaya data nyata (stok 5 + ketik "12" = "512" tersimpan), dan angka di field itu redundan krn "Stok saat ini" sudah tertulis di atasnya; fix select-all seluruh angka (pola sama dgn dialog Ubah Nama Kategori). Field "Poin Loyalitas" (`pelanggan_form_screen.dart`) berhenti di-prefill '0' literal (pelanggan baru maupun lama berpoin 0) — jalur simpan sudah `int.tryParse(...) ?? 0` jadi kosong aman; `_minStockCtrl` SENGAJA tidak disentuh krn `null` vs `0` di situ TIDAK ekuivalen (`baseStock > min` vs SQL `stock < min_stock`) shg blanking akan menggeser produk mana yg dianggap "menipis". Test baru: `stock_opname_unit_load_batch_test.dart` (menghitung query lewat `QueryInterceptor` Drift — kelas bug ini tak terdeteksi test tier DB/widget biasa krn hasil akhirnya identik, yg beda hanya JUMLAH query; revert = 121 query vs ambang 5) & `numeric_field_zero_prefill_test.dart` (revert = `TextSelection.collapsed(offset: 1)` alih-alih select-all, dan `'0'` alih-alih kosong) — semua revert-verified
- `ba9fe6a` — docs: perbaiki klaim basi `schemaVersion = 9` di CLAUDE.md (kode sudah 21) — utang D.4 di Item 41 PLAN.md, ditemukan lewat audit pra-rilis; klaim stack lain (Riverpod 2.5, GoRouter 14) diverifikasi masih akurat thd pubspec, jadi hanya baris ini yang diubah; D.4 dihapus dari PLAN.md
- `fec3639` — chore: naikkan versi ke 2.2.0+4 untuk rilis fitur satuan berjenjang (Stock Opname/Cek Stok) + fix sync
- `d7b8851` — fix: `sync_upload_queue` rentan tabrakan per-IP sama seperti `_pendingProposals` (dua device beda berbagi IP saling menimpa antrian sync) — fix identik: kolom baru `device_code` nullable di `SyncUploadQueue` (migrasi v20->v21, guard `from >= 18` krn `createTable` di step v18 sudah pakai definisi tabel terkini), `enqueueSyncUpload` sekarang kunci slot "1 per pengirim" pakai `deviceCode` kalau klien mengirimkannya, fallback `fromIp` utk klien lama; `lan_sync_service.dart` parsing `rawDeviceCode` dipindah lebih awal & dipakai ulang utk kedua antrian (proposal + upload queue). Sekalian fix Item 38 (PLAN.md, ditemukan tak sengaja lewat investigasi flake test Stock Opname 25 Juli, TERBUKTI berdampak nyata bukan cuma teoretis): `_rawBaseStock` tie-break `ORDER BY created_at DESC, id DESC` bisa salah pilih baris `stock_ledger` kalau 2 penulisan stok jatuh di detik yang sama persis (`created_at` presisi detik, `id` UUID v4 acak tak berkorelasi urutan insert) — fix pakai `rowid` SQLite built-in sbg tie-break kedua (monoton sesuai urutan insert, tanpa migrasi kolom baru). Test baru: `migration_v21_test.dart`, `sync_upload_queue_device_slot_key_test.dart` (real HTTP round-trip), `stock_ledger_tiebreak_test.dart` (deterministic DB-level) — semua revert-verified
- `ed6ff36` — feat: 4 penyesuaian/fitur sekaligus atas permintaan user. (1) `cart_sheet.dart`: nama produk panjang di keranjang boleh tumbuh sampai 2 baris (dulu terpotong 1 baris ellipsis) — `maxLines: 1 -> 2`, `ListTile` otomatis menyesuaikan tinggi baris tanpa restrukturisasi leading/trailing. (2) `stock_opname_screen.dart`: produk bersatuan berjenjang (>1 satuan, mis. Pcs/Dus) sekarang boleh dihitung dalam satuan yang lebih nyaman (mis. "10 dus"), dikonversi ke satuan dasar via `ratio_to_base` sebelum dibandingkan/disimpan (`commitOpname` tidak berubah — tetap terima base unit + qty base yang sudah dikonversi); produk bersatuan tunggal (mayoritas toko) sama sekali tidak berubah tampilannya; layar review menampilkan nilai APA ADANYA yang diketik user ("diketik: 10 Dus") di samping hasil konversinya. (3) `cek_stok_screen.dart`: produk berjenjang yang dicentang utk restock dapat pemilih satuan per-produk (dimuat malas saat dicentang); teks "Order Restock" yang sudah ada diperluas dari "- {nama}" polos jadi "{qty} {satuan} {nama}" (format sama dgn contoh HTML yang diberikan user) memakai satuan terpilih & stok terkini terkonversi; produk bersatuan tunggal tetap format lama. (4) Fix bug nyata ditemukan saat investigasi pertanyaan user: kategori produk (create/rename/delete/reorder KATEGORI ITU SENDIRI, beda dari penugasan produk ke kategori yang sudah benar via `products.productGroupId`/`product_group_tags`) tidak pernah tersinkron ke device kasir/asisten sama sekali — `product_groups` lupa dimasukkan ke `dumpSince`'s `masterData` list & `clientMergeableTables` di `lan_sync_service.dart` sejak awal; full-dump tiap sync (tabel ini tidak punya kolom `updated_at`) aman krn baris kategori tidak pernah benar-benar dihapus (`deleteProductGroup` menombstone `name=null`, bukan DELETE, slot id dipakai ulang). Test baru: `cart_item_name_two_lines_test.dart`, `stock_opname_unit_conversion_test.dart`, `cek_stok_unit_output_test.dart`, `product_group_sync_test.dart` (real HTTP round-trip) — semua revert-verified

## 2026-07-24

- `d4b17b9` — fix: restore backup gagal total dgn "FOREIGN KEY constraint failed ... DELETE FROM product_groups" (kode 787) utk toko mana pun yg pernah pakai kategori-tambahan (Item 54) — akar masalah: `_allTables` (dipakai `dumpAllTables`/`restoreFromDump`, `app_database.dart`) tidak pernah diperbarui saat `product_group_tags` & `reserved_order_numbers` ditambah ke skema, jadi baris lama `product_group_tags` tidak pernah ikut dihapus di awal restore & masih menunjuk ke `product_groups` lama saat `DELETE FROM "product_groups"` dijalankan; sekalian dampak diam-diam: kedua tabel itu tidak pernah ikut ter-backup sama sekali. Fix: tambahkan keduanya ke `_allTables` (posisi sesuai dependensi FK). Sekalian fix bug terpisah dilaporkan user: produk baru yang diusulkan asisten via sync LAN kadang hilang dari antrian owner tanpa jejak (bahkan tanpa owner pindah layar) — `_pendingProposals` dikunci "satu slot per alamat IP", 2 device BEDA yang kebetulan tersambung dari IP sama (lazim di hotspot HP, pool DHCP kecil) saling menimpa slot sebelum owner sempat meninjau; fix: kunci slot sekarang preferensi `deviceCode` (dikirim klien via `syncToHost`) drpd IP mentah. Test baru `backup_restore_bug_test.dart` (kasus `product_group_tags`) & `proposal_device_slot_key_test.dart` (2 device beda IP sama vs device sama sync ulang) — revert-verified

## 2026-07-23

- `22601be` — fix: sheet "Verifikasi Pesanan" (Item 24b, centang tiap barang sebelum lanjut bayar) dihapus dari alur transfer transaksi via QR — tap kartu antrian handoff pegawai (`awaitingPayment`) sekarang langsung resume ke keranjang aktif, persis sama seperti pesanan ditahan biasa (permintaan user: penerima tidak perlu mengecek ulang barang yang sudah disusun pengirim); `_VerifyOrderSheet`, `_toggle`, dan field `checked` di payload `held_orders` dihapus sebagai dead code; `kasir_verify_order_test.dart` & satu test terkait di `kasir_scan_order_code_test.dart` disesuaikan/dihapus
- `d9e971a` — feat: tombol Bayar di cart bar, transfer QR bebas + nomor nota reservasi (Item 55/56/57) — segmen "Bayar" terracotta baru di tab meta cart bar (sejajar "Tahan") utk owner/asisten/pegawai berizin `terima_pembayaran`, tap langsung ke layar Pembayaran tanpa lewat sheet keranjang; gerbang izin dipusatkan ke provider baru `handoff_gate_provider.dart` (`needsPaymentGateProvider`), dipakai jg oleh `cart_sheet.dart` (menghapus provider privat duplikat sebelumnya); `CartSheet` dapat tombol "Transfer via QR" (ikon `qr_code_2`+panah kecil) utk transfer transaksi BEBAS ke device lain (owner/asisten/pegawai berizin) — terpisah dari jalur handoff pegawai TANPA izin yg sudah ada ("Kirim ke Owner/Asisten"); teks "Kosongkan" diganti ikon tempat sampah, dialog konfirmasi tetap ada. Nomor nota (`local_id`) sekarang **direservasi sejak item pertama masuk keranjang** (tabel baru `reserved_order_numbers`, `schemaVersion` 19->20, `AppDatabase.reserveLocalId`/`releaseLocalId`) — bukan cuma di-generate saat checkout — tampil sbg `#<segmen terakhir>` (mis. `#17`) di cart bar & kartu pesanan tertahan, nomornya STABIL sepanjang siklus hidup keranjang termasuk lewat transfer QR (tidak reservasi baru di penerima); dilepas saat keranjang dikosongkan/transfer selesai, dikonsumsi (`releaseLocalId`) begitu checkout tersimpan. `OrderParserService.encodeHandoff`/`parse` menambah baris `PelangganId:`/`Nota:` — pelanggan non-umum ikut terbawa ke penerima (auto-resolve `customerId` kalau pelanggan itu tersync lokal, diam-diam fallback ke `customerName` polos kalau belum), nomor nota yg sudah direservasi pengirim dibawa apa adanya. Sekalian fix: sheet "Tempel Pesanan" — tombol konfirmasi tertutup keyboard krn `DraggableScrollableSheet` tidak reaktif thd `viewInsets`, diganti pola `Padding+LayoutBuilder+Column(mainAxisSize.min)` yg sudah terbukti aman dipakai sheet lain di app ini. Migration test v7-v18 diperbarui ekspektasi `schemaVersion` ke 20; test baru: `cart_bar_bayar_button_test.dart`, `cart_sheet_transfer_icon_test.dart`, `reserve_local_id_test.dart`, `order_parser_customer_id_test.dart` — semua revert-verified
- `4e0fbf3` — feat: kategori multi-tag + chip kategori di Kasir (Item 54) — lanjutan Item 52: `Products.productGroupId` tetap jadi kategori UTAMA (katalog cetak/HTML, avatar warna, CSV import tidak disentuh), tabel baru `product_group_tags` (many-to-many) menampung kategori TAMBAHAN, produk sekarang bisa ada di lebih dari satu kategori tanpa kehilangan kategori lamanya; `category_assign_products_screen.dart` dirombak dari checkbox+"Terapkan" batch-overwrite jadi live-toggle (`AppDatabase.setProductGroupMembership`), tampil qty/harga per produk + "Juga ada di: ..." kalau sudah di kategori lain; tab Kasir dapat row chip kategori (single-select union kategori utama+tag, hold-and-reorder ke kolom baru `ProductGroups.sortOrder`) di atas `SyncStatusBanner`/`InlineBanner` yang sudah ada; `schemaVersion` 18->19; `product_group_tags` disinkron host->klien full-dump dgn cleanup baris yatim di `mergeRows` saat untag; sekalian menutup Item 53 (`deleteProductGroup` tidak cap ulang `updated_at`) + tambah pembersihan tag yatim saat kategori dihapus; migration test lama (v7-v18) diperbarui fixture-nya (tabel `product_groups` + ekspektasi versi 19); test baru: `category_assign_products_test.dart` (ditulis ulang), `category_assign_products_nav_test.dart`, `product_group_tags_sync_test.dart`, `product_group_reorder_test.dart`, `kasir_category_chip_test.dart` — semua revert-verified
- `1ce4ef1` — feat: bulk assign produk ke kategori (Item 52) — dari layar Kelola Kategori, tap kategori (di luar mode pilih-utk-hapus) buka `CategoryAssignProductsScreen` baru: cari & pilih banyak produk sekaligus, Terapkan menugaskan semuanya ke kategori itu; produk yang sudah punya kategori lain tetap muncul & boleh ditimpa (keputusan eksplisit user); DB: `assignProductsToGroup` (typed update massal + cap ulang `updated_at`, pola sama spt `deactivateProduct`); route baru `/produk/kategori/:id/pilih-produk`; test baru `category_assign_products_test.dart` (DB-tier) & `category_assign_products_nav_test.dart` (end-to-end via routerProvider asli), keduanya revert-verified

## 2026-07-22

- `4aea663` — fix: `mergeRows` (jalur sync) menulis via raw `customInsert`/`customStatement` TANPA param `updates:` — Drift tidak tahu tabel `products`/dll berubah, jadi `StreamProvider`/`.watch()` (`watchProducts()` di `produk_list_screen.dart` & katalog `kasir_screen.dart`) tidak auto-refresh, data DB klien sudah benar tapi UI terlihat "tidak berubah" sampai dipaksa reload manual; pola bug & fix ini sudah ada & terdokumentasi di `restoreFromDump` (param `updates:`), cuma belum pernah diterapkan ke `mergeRows`; fix: resolve `TableInfo` dari nama tabel string, thread `updates: {table}` ke INSERT utama & DELETE dedup `price_tiers`; test baru `product_deactivate_sync_reactive_test.dart` mendengarkan `watchProducts()` STREAM LIVE (bukan one-shot spt test sebelumnya, yang tidak menangkap kelas bug ini)
- `e66cfd2` — fix: label cetak produk (`PrinterService._buildLabelBytes`) tidak menampilkan kode batang sama sekali utk barcode yang bukan persis 12/13 digit EAN-13 (mis. kode "asal tempel angka" 8-digit yang UMUM dipakai toko ini utk produk non-barcode resmi) — sebelumnya jatuh ke fallback teks polos tanpa grafis apa pun; tambah fallback `Barcode.code128` (dukung panjang berapa pun) supaya selalu ada kode batang yang bisa discan
- `7f20d38` — fix: produk yang dinonaktifkan owner (`deactivateProduct`, tombol "Nonaktifkan" di form produk) tidak pernah mencap ulang `updated_at` — beda dari `deleteVariant` yang sudah benar — sehingga `dumpSince` (filter `WHERE updated_at >= since`) tidak pernah lagi menyertakan baris itu ke klien yang watermark-nya sudah lewat, produk "hantu" tetap muncul selamanya di HP kasir/asisten; akar masalah sama dgn bug `applyProductProposals` yang sudah pernah diperbaiki; fix: `deactivateProduct` sekarang mencap `updatedAt: Value(DateTime.now())`; test baru `product_deactivate_sync_test.dart` (unit + end-to-end host→klien via `dumpSince`/`mergeRows` sungguhan)
- `005c68b` — fix: "Generate Barcode" dipindah dari layar Barcode/Cetak Label ke field input Barcode di form Edit Produk (tombol baru sejajar tombol Scan yang sudah ada, per satuan) — klarifikasi user Item 51; `barcode_screen.dart` sekarang murni utk cetak label satuan yang sudah punya barcode, satuan kosong cuma tampil pesan info arahan; ukuran font nama produk di label cetak diperbesar (`PosStyles(height: PosTextSize.size2)`, sedikit lebih kecil dari baris Total di struk yang pakai size2/size2 penuh)
- `ed81fb9` — fix: layar Barcode & Cetak Label (route `/produk/:id/barcode`) tidak bisa dijangkau dari UI sama sekali — route sudah terdaftar sejak commit `c818324` tapi tidak ada tombol apa pun yang menavigasi ke situ; tambah tombol ikon "Barcode & Cetak Label" di AppBar layar Edit Produk; test baru via router sungguhan (bukan `pumpWithFakeApp` tanpa router) supaya kelas bug "route yatim" ini bisa terdeteksi
- `c818324` — feat: generator barcode internal (EAN-13 prefix `29`, reserved GS1 "Restricted Circulation Number" khusus pemakaian internal toko — tidak pernah bentrok dgn barcode manufaktur resmi) di `lib/core/utils/internal_barcode.dart`, uniqueness dicek langsung ke `product_barcodes`; cetak label per satuan/varian via printer thermal 58/80mm yang sudah terintegrasi (`PrinterService.printProductLabel`, ESC/POS command barcode EAN-13 native — bukan raster capture widget); UI di `barcode_screen.dart`: satuan tanpa barcode dapat tombol "Generate Barcode" terpisah dari "Cetak Label" (2 aksi independen); kolom `is_generated` (sudah ada di skema, belum pernah dipakai) mulai diisi; test baru + revert-verify nemukan & fix bug nyata overflow lebar tombol `FilledButton` (gotcha `minimumSize` yg sudah tercatat CLAUDE.md)
- `2472533` — feat: redesain sinkron harga induk-cabang (Item 50) — fuzzy-matching (Levenshtein) dihapus total dari `PriceMatchService` (terbukti dari data nyata 2 toko menyebabkan tabrakan false-positive pada produk varian ukuran/nama mirip, akar penyebab "harga oscillating"); algoritma baru 4 tingkat murni deterministik (barcode > kode_produk unik-2-sisi dgn override konflik-barcode-resmi > nama+satuan persis kandidat tunggal/ganda); mekanisme lock-in: begitu owner konfirmasi pasangan Tingkat 2/3/4, barcode katalog ditulis sbg alias permanen non-primary ke `product_unit` lokal shg sync berikutnya utk produk sama langsung Tingkat 1, tidak pernah ditinjau ulang; tambah ekspor/impor katalog harga terenkripsi (`.berkahpos` magic BPRC1) di `DbExportService`/`price_sync_screen.dart` utk toko yg tidak selalu satu WiFi, cara simpan/bagikan sama persis fitur Backup (`saveOrShareExport`), sengaja dipisah dari `decrypt()`/`restore()` generik krn shape payload beda

## 2026-07-21

- `c1ff649` — fix: `SyncStatusBanner` masih dibungkus `SafeArea(bottom:false)` peninggalan desain lama (dulu di atas MainShell) — sekarang selalu di bawah AppBar/toolbar shg jadi inset ganda (celah kosong aneh), dihapus; `ClientSyncPhase.waitingApproval` dikeluarkan dari `clientSyncing` — protokol sync connectionless, klien TIDAK PUNYA kanal utk tahu kapan/apakah owner memutuskan, jadi banner klien nampilkan spinner "menunggu persetujuan owner..." SELAMANYA walau permintaan sudah selesai teknis; diganti konfirmasi sekali-tampil "Terkirim — menunggu peninjauan owner"
- `d281a28` — fix: `SyncStatusBanner` dipindah dari `MainShell` (mengambang di atas SETIAP layar tab) ke masing-masing 6 layar tab (Ringkasan/Kasir/Produk/Pelanggan/Laporan/Pengaturan), tepat di bawah AppBar/toolbar — user klarifikasi "inline" yg dimaksud adalah POSISI (sejajar `InlineBanner` yg sudah ada, mis. banner "Pesanan ditahan" di Kasir), bukan cuma gaya kartu; param `hideOnSyncScreen` dihapus (tak relevan lagi)
- `eb7cc1b` — fix: usulan produk (Item 40) yang isinya sudah identik dgn data owner tidak lagi menumpuk di antrian review — `AppDatabase.filterUnchangedProposals` (baru) bandingkan payload usulan (nama/satuan/tier harga/harga alternatif/barcode) thd data LIVE host sebelum masuk `_pendingProposals`, produk identik dibuang & kalau semua identik tidak ada usulan yg dibuat sama sekali; ketemu & diperbaiki sekalian bug nyata di fix ini sendiri sebelum sempat commit — Dart `Set`/`List` bandingkan by-identity bukan isi, jadi perbandingan awal SELALU `!=` walau isi persis sama (fix: bandingkan sbg string kanonik ter-sort)
- `6b4366d` — feat: `SyncStatusBanner` (shell) diubah jadi kartu notifikasi inline (accent bar + ikon, konsisten dgn `InlineBanner`) — TIDAK lagi tampil hanya krn `hostRunning` semata (dulu "Host aktif" menetap selamanya walau antrian sudah kosong, laporan nyata user); tambah `SyncState.transientMessage`/`SyncStateNotifier._showTransient` (konfirmasi sekali-tampil dgn auto-dismiss timer) dipasang di approve/tolak/Sync Ulang Penuh; kalau antrian lain masih menunggu SAAT konfirmasi tampil, antrian itu tertumpuk sbg garis aksen tipis "Compact Strip" di belakang kartu konfirmasi (bukan hilang)
- `d691e49` — fix: antrian sync tampak hilang di layar Sync setelah app di-force-stop/clear RAM — `LanSyncService._db` (static, RAM) reset null saat proses mati & `SyncStateNotifier._refreshQueue()` mengosongkan antrian kalau host belum direstart owner; `LanSyncService.attachDb()` sekarang dipanggil segera saat provider dibuat (bukan cuma via `startHost()`), antrian selalu dimuat dari DB terlepas status host, tidak lagi dikosongkan saat host di-stop
- `456bf45` — feat: Item 17 Fase 2 — antrian approval sync sisi host dipindah dari in-memory ke tabel DB persisten `sync_upload_queue` (schemaVersion 17->18); klien beralih dari selalu full-dump sejak epoch ke watermark upload incremental per device (`last_sync_upload_confirmed_at`, terpisah dari watermark download); tolak (reject) sekarang PERMANEN dgn dialog konfirmasi wajib + tombol baru "Sync Ulang Penuh" sbg escape hatch manual reset watermark

## 2026-07-20

- `cab92dc` — fix: `mergeRows` skip `price_tiers`/`product_units`/`alt_prices`/`product_barcodes` yang unit-nya milik produk masih `locally_modified=true` (usulan belum di-review owner) — 4 tabel ini disinkron full-dump tanpa `updated_at` sama sekali, jadi edit lokal asisten yang belum approved bisa tertimpa balik/terduplikasi oleh sync APA PUN yang lewat; sekalian rapikan alignment baris "Produk: N" di struk cetak thermal (gen.row 2-kolom, sejajar dgn "Pegawai:")
- `a0b20e6` — feat: Item 21 Fase 1 — state sync (host/antrian/progres klien) diangkat ke `syncStateProvider` global; `SyncScreen.dispose()` tidak lagi mematikan host saat owner pindah tab; banner status sync persisten baru di level shell (`SyncStatusBanner`); seam test-only `LanSyncService.debugHostRunningOverride` (testWidgets + HttpServer sungguhan terbukti hang)
- `1d47b2a` — fix: `mergeRows` (`app_database.dart`) isolasi kegagalan per-baris untuk tabel append-only (transaction_items/transaction_payments/dll) — SQLite "INSERT OR IGNORE" tidak menekan pelanggaran FOREIGN KEY, jadi satu baris yatim dulu bisa menggagalkan seluruh batch sync (bug "riwayat kosong" di sisi owner). `applyProductProposals` sekarang mencap `updated_at` ke saat approve, bukan mempertahankan timestamp lama usulan klien — memperbaiki usulan harga yang sudah diterapkan tapi terus muncul lagi di sync berikutnya.
- `ed177ac` — fix: `dibayarDisplay()` di `receipt_screen.dart` diubah jadi terima param `kembalian` eksplisit (bukan hitung ulang internal via `latestChangeGiven`) — full-suite run menemukan regresi di `receipt_dibayar_net_test.dart` (skenario kembalian lama dipakai ulang sbg pembayaran baru via Tambah Belanjaan): in-app pakai definisi kembalian `_latestPayment` (pembayaran PALING AKHIR apa pun nilainya), share/cetak pakai `latestChangeGiven` (pembayaran PALING AKHIR yg changeGiven>0) — dua definisi ini sudah beda sejak lama, caller sekarang WAJIB kirim nilai yg konsisten dgn baris Kembalian yg benar2 dirender
- `cec17f5` — fix: baris "Dibayar"/"Bayar"/"Terbayar" di struk (in-app/share/cetak/gabungan) salah menampilkan `netPaidDisplay` (= Total persis) alih-alih Total+Kembalian saat nota lunas dgn kembalian — pembaca tak bisa merekonsiliasi kenapa ada Kembalian kalau Dibayar sudah = Total (bug nyata dilaporkan user via screenshot, cicilan 4-pembayaran 250.000 tapi Dibayar tampil 231.200)
- `f14e06e` — feat: Item 49b/49d/49f/49g — struk ringkasan 3-baris (Total/Dibayar/Sisa-Kembalian, hapus "Uang Diterima"), tab Laporan Pengeluaran baru (KPI+donut+grafik harian), filter baris audit 'edit'/'retur' dari struk share/cetak, retur & edit item transaksi lunas kini update nota yang sama (bukan bikin nota baru) via kolom `returnedAt` (migrasi v16->v17) + pembatas "Retur HH:MM" + ringkasan Total awal/Retur/Akhir/Refund
- `257bdf8` — feat: Item 49e — Tambah Satuan langsung scroll-into-view + autofocus field harga
- `df7cd02` — fix: Item 49c — catatan struk cetak (itemNote/strukNote/receiptFooter) rusak kalau multi-baris
- `4b57450` — feat: Item 49a — tombol "000" pindah ke baris bawah, setelah "00"
- `46531a9` — docs: catat rencana batch besar (Item 49) ke PLAN.md — keypad, struk 3-baris, catatan cetak, tab Pengeluaran, jump-to-edit satuan, retur/edit in-place nota lunas, filter audit trail
- `d3d9403` — feat: cache keranjang katalog HTML (order_page_service) ke localStorage keyed per versi katalog + TTL 1 hari, tombol "Kosongkan" baru — refresh browser tak lagi hilangkan pilihan pelanggan

## 2026-07-19

- `fa79eb0` — feat: Catatan di Struk (receipt_note) benar-benar terpakai di struk share/cetak/gabungan (sebelumnya disimpan tapi tak pernah dibaca) + kartu KPI baru "Selisih Kas Operasional" (Omzet - Pengeluaran) di tab Ringkasan
- `c094014` — fix: Laporan Ringkasan basi pasca-sync — rebuildStaleSummariesInRange (self-heal cache daily_summaries yang tak ikut ter-rebuild saat merge) dipanggil di provider Ringkasan & ekspor laporan sebelum baca cache
- `d291e5d` — fix: usulan ubah harga (sync antar-role) — applyProductProposals replace penuh price_tiers/alt_prices per satuan yg di-approve (hapus tier lama sebelum insert) supaya harga owner benar berubah & tidak me-revert harga asisten saat sync balik
- `37fe379` — feat: pembatas "----- Tambahan HH:MM -----" (Gaya A) di struk share (_ReceiptPaper) & cetak thermal (printer_service), sebelumnya cuma di struk in-app
- `daca3a6` — fix: hapus aksen warna kartu "Device Ini" di Pengaturan (netral kembali; Toko hijau & Perangkat teal tetap)
- `f7de38a` — revert: batalkan total eksperimen ikon peach (emoji bawaan tak sempat diverifikasi), kembali ke Icons.shopping_basket_rounded default
- `05c4413` — feat: ikon toolbar kasir jadi emoji peach bawaan (Text('🍑'), bukan custom painter — akurat via font emoji sistem, tanpa dependency/lisensi baru)
- `c371bd0` — revert: batalkan ikon peach toolbar kasir (user: "tidak mirip sama sekali"), kembalikan ke Icons.shopping_basket_rounded
- `b0e16d2` — refine: ikon peach toolbar kasir menyerupai referensi (tangkai + daun + garis lekuk) — kemudian direvert di c371bd0
- `772e492` — feat: batch redesign UI — keypad tunai berwarna (1-9 hijau, 0/00/000 biru bertahap), tombol Bayar struk hijau solid (payGreen) samakan checkout, HAPUS debounce stepper (bikin lemot multi-tap), aksen seksi Pengaturan (Device=biru/Toko=hijau/Perangkat=teal baru), ikon keranjang toolbar → peach custom (_PeachGlyph), mode gelap: angka lingkaran hijau stepper jadi gelap + Bayar Nanti merah solid
- `9e52eb6` — fix: revisi UI keranjang/stepper/struk (qty di kiri keranjang jadi teks biasa, stepper minus tak berkedip saat + ditekan berulang, qty+satuan struk in-app dibold tapi tak lebih tebal dari nama produk)
- `df48d8f` — feat: select-all teks lama saat kolom cari kasir dapat fokus ulang (ketik langsung menimpa, tak perlu jangkau tombol x) — _KasirTopbarState._onFocusChange via post-frame

## 2026-07-18

- `3cbbb54` — docs: HANDOFF/PLAN/CHANGELOG/PATCHNOTES batch Item 42-46 + pisah test flaky low-stock
- `9eabb9b` — feat: Item 46 — banner stok menipis di kasir setelah checkout (lowStockAlertsForProducts + stockBreakdownText "100 Biji (5 Pak, 1 Dos)", pendingLowStockAlertsProvider, RouteAware.didPopNext + fallback post-frame build)
- `e8f7b87` — feat: Item 42 — filter periode (Hari/Minggu/Bulan/Custom) di tab Pengeluaran (getNetProfitExpenseTotal Laporan sengaja tak diubah)
- `7f5012e` — feat: fix satuan dasar ganda (45), stepper qty berpindah sisi (43), qty di kiri item keranjang (44)
- `98ab0df` — fix: stepper (AddControl) tetap besar setelah tap sampai tap area lain/scroll (bukan cuma sesaat selagi ditahan) — AddControl.activeStepper (ValueNotifier statis) + StepperActiveScope di kasir_screen.dart & cart_sheet.dart
- `3c1525e` — feat: aksen warna soft per fungsi di kartu Ringkasan/Laporan/Pengaturan (Varian B dari mockup, dipilih user) — hijau=Uang&Kas, amber=Stok, merah=kritis, biru=Produk&Data, ungu=Sinkronisasi
- `58faf98` — feat: stepper feedback taktil (AnimatedScale saat ditekan), bulk add/remove kategori produk, opsi Bagikan langsung utk backup (BPOP2/BPOT1)
- `da2aa8e` — fix: kartu Usulan Harga/Produk overflow di HP sempit (tombol Tinjau ListTile trailing lebar-penuh meremas title/subtitle jadi 1 karakter/baris) — tambah LanSyncService.debugAddProposal/debugClearProposals (seam test-only, hindari real socket di test render)
- `d2b4c4d` — fix: eksekusi P1/P2 audit Item 41 — rekonsiliasi stok pasca-sync (rebuildStockAfterForUnits), UTC timestamp sync, satu slot antrian/IP, hemat memori BytesBuilder, HMAC respons + verifikasi klien, allowlist tabel + guard identifier, layar /kunci-hilang (keystore gagal), BackupException konsisten, parseValue anti-overflow, potong crash log, password ekspor min 8, prune lockout, cache/mmap SQLCipher diturunkan, manifest BT legacy maxSdkVersion=30 — 510 test hijau + bukti revert-merah
- `3e9d2e1` — docs: hasil verifikasi test nyata (Flutter 3.24.5: analyze 0 issue, 498 test hijau) + temuan D.5 gagal kompilasi di SDK 3.44.6
- `b00d8bc` — docs: perbarui CHANGELOG/HANDOFF untuk sesi audit kode (Item 41)
- `5944593` — docs: audit kode menyeluruh — temuan lengkap ke PLAN.md Item 41 (bug/silent bug, keamanan, performa/daya, kompatibilitas, clean code; prioritas P1-P3)
- `120ead6` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk Item 40 (usulan harga/produk)
- `fcadcb1` — feat: Item 40 — usulan harga/produk dari device non-owner via sync LAN (kolom products.locally_modified, dumpLocalProposals/applyProductProposals, queue _pendingProposals terpisah dari _pendingQueue, layar ProductProposalReviewScreen) — schemaVersion 15 -> 16
- `3f3a4c0` — fix: struk cetak/gambar tampilkan kembalian pembayaran TERAKHIR (bukan tx.changeAmount akumulasi) — printer_service.dart + _ReceiptPaper, konsisten dgn Ringkasan on-screen & nota gabungan
- `5c244da` — feat: Item 39 — sync LAN lebih andal (deteksi IP dual-strategi + tombol Refresh IP + profil timeout dipilih user + pesan error dipertajam + logging CrashLogService)

## 2026-07-17

- `a1c2776` — fix: timeout total (bukan idle) memutus transfer sync besar yang masih aktif (babak ke-4 laporan asisten stok minus)
- `939048a` — fix: sync LAN tanpa timeout HTTP → infinite loading di klien (babak ke-3 laporan asisten stok minus)
- `d21889f` — fix: asisten tidak bisa override stok minus walau sudah digrant izin ("Jadi Host" sync khusus owner — master data cuma boleh mengalir dari owner sbg host)
- `7f37d64` — fix: barcode produk/varian yang dinonaktifkan/dihapus terkunci permanen (lepas via mutasi nilai di `product_barcodes`, sync-safe tanpa ubah protokol)
- `5c9de7f` — feat: Item 36 (stock opname hitung fisik BUTA + riwayat sesi) + Item 37 (publish katalog ke Cloudflare Pages otomatis, nama project deterministik slug+hash)
- `b69d538` — fix: varian produk dgn barcode bentrok gagal-diam tanpa pesan error (tangkap exception di `_addVariant`/`_editVariant`)
- `886db53` — feat: Tutup Buku tanggal custom (bukan selalu 1 Januari), sekali per tahun (Item 31)
- `fa3e496` — feat: opsi sinkron harga via barcode saja (Item 35 opsional)
- `dd4bad3` — feat: kontrol stok owner — katalog auto-habis (29) + layar Cek Stok + tab audit Laporan (30)
- `db60a4b` — fix: sinkron harga antar-toko salah cocok karena SKU non-unik (pengaman tabrakan kode + satuan wajib cocok + fix `_findOrCreateProduct`)

## 2026-07-16

- `c805907` — feat: aksen warna soft per-fungsi tombol toolbar kasir (scan/antrian/riwayat/tempel pesanan) — Varian C
- `21e58c1` — fix: riwayat transaksi tampilkan nama generik "Pelanggan" utk pelanggan yang sudah dihapus, alih-alih nama aslinya
- `839a29c` — fix: turunkan debounce scanner eksternal 300ms→150ms agar scan dobel cepat yang disengaja tidak ke-drop
- `1d09200` — fix: 2 bug ditemukan saat testing device asli Alihkan Owner (redirect loop router + nama/kode device tidak lagi warisi punya lama)
- `99de7ea` — feat: fitur "Alihkan Owner" (transfer data + identitas toko via file terenkripsi BPOT1) + opsi "Pulihkan dari File" di welcome screen
- `e565430` — fix: poin loyalitas nyangkut di pelanggan lama saat transaksi diubah balik ke Umum/pelanggan lain
- `fc991d2` — fix: device yang di-revoke bisa "membuka diri sendiri" via kode aktivasi yang sama
- `2ade5b5` — feat: boleh naikkan qty item sama di edit sheet nota tempo yang belum ada pembayaran
- `32d017e` — fix: poin loyalitas tidak bertambah kumulatif saat Tambah Belanjaan
- `f098fa4` — fix: alamat pelanggan tidak tampil di dropdown picker cart bar
- `87b8c42` — fix: teks nama produk di baris item struk in-app dibuat bold
- `eb7da72` — feat: redesign header struk — status Lunas/Tempo jadi watermark stempel
- `feaf7d2` — docs: perbarui catatan Item 29 — clearance stempel vs baris item sudah diverifikasi di mockup
- `e57dcb0` — docs: simpan spesifikasi final redesign header struk (stempel) ke PLAN.md Item 29

## 2026-07-15

- `79b94e6` — docs: tambah rencana "Alihkan Owner" (transfer sesi) & lanjutkan pesanan lintas device ke PLAN.md
- `99ca815` — feat: batch perbaikan modal checkout & struk (label, layout, warna, poin, alamat)
- `791e021` — feat: bundle font lokal (Hanken Grotesk, Newsreader, Roboto Mono) — offline-first
- `3b55d1c` — feat: tampilkan sisa waktu lisensi di Pengaturan
- `8f0c958` — feat: toggle direct WhatsApp vs share generik untuk katalog HTML
- `d7c257d` — fix: qty desimal (0.25) tidak tampil proper di stepper + tambah debounce anti-missclick
- `a23c48e` — fix: struk gabungan banyak item jadi blur saat dibagikan — kirim sbg PDF

## 2026-07-14

- `3591396` — feat: sakelar darurat "lockAll" di Lapis 3 + durasi kustom (menit) di generator
- `0d1efe2` — feat: aktifkan gerbang lisensi — tanam public key developer
- `d4a8e71` — perf: katalog HTML update satu baris produk, bukan render ulang grid
- `45ac0c5` — fix: poin loyalitas tempo tidak muncul + tap luar tutup panel antrian
- `3200c0e` — redesign: satukan kartu antrian "Pesanan Ditahan" pakai chip status
- `102399d` — docs: tambah gotcha CLAUDE.md — Clipboard.getData() hang di widget test
- `458fc77` — feat: tombol "Salin Teks Pesanan" di bawah QR handoff pegawai
- `69abb77` — fix: teks "N pilihan" katalog HTML under-count saat varian punya >1 satuan
- `7c65b78` — fix: katalog HTML tidak menampilkan satuan lain (mis. Dus) produk
- `67414e1` — feat: samakan gaya badge jumlah item di struk & keranjang dgn cart bar
- `2d4467a` — fix: sync LAN gagal total kalau device penerima tertinggal 1 kolom skema

## 2026-07-13

- `310960f` — feat: tampilkan jumlah item di struk (baris Tandai Semua) & keranjang kasir
- `3a48d4e` — revert: "fix: samakan gaya stepper keranjang katalog HTML dengan AddControl app kasir"
- `e12c290` — revert: "docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk fix stepper katalog HTML"
- `36ceff7` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk fix stepper katalog HTML (di-revert, lihat `3a48d4e`/`e12c290` — desain sudah ditangani di branch lain, `24097ec`)
- `beaf395` — fix: samakan gaya stepper keranjang katalog HTML dengan AddControl app kasir (di-revert, lihat di atas)
- `b047372` — docs: tambah gotcha CLAUDE.md — tombol lebar-penuh dalam Row di AlertDialog
- `74a1aaf` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF — fix susulan tombol Tambah Bayar + gotcha baru
- `9633e7d` — fix: tombol Uang Pas/Bayar hilang di modal Tambah Bayar layar sempit + judul jadi "Bayar"
- `2090d40` — feat: checklist verifikasi + stepper senada di keranjang kasir
- `9fec89e` — fix: tombol modal Tambah Bayar tidak sejajar (overflow ke kolom)
- `442ee22` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk follow-up round batch 18-item
- `24097ec` — feat: katalog HTML — kontrol +/- lingkaran spt app kasir, harga read-only, font lebih besar
- `16b94b9` — fix: scan pesanan pegawai via scanner HID tertentu salah rute ke Tempel Pesanan
- `83e01dd` — fix: tombol Batalkan Pembayaran tidak muncul untuk nota lunas seketika
- `6564852` — feat: katalog HTML — modal tap-item ganti dropdown varian (pilih satuan/harga custom/catatan)
- `955ea34` — feat: bersihkan file share sementara (struk/katalog) yang menumpuk di temp dir
- `acaf2b5` — feat: modal Tambah Bayar Uang Pas pindah kiri + gate kosong, stepper lebih besar, harga produk reaktif
- `5ff92a4` — feat: struk — Bayar+Tambah Belanjaan sejajar, batalkan pembayaran, edit item, fix nota gabungan
- `a8c94ad` — feat: skema v15 — checklist struk persisten, batalkan pembayaran, edit item nota belum lunas
- `174cad7` — feat: gerbang aktivasi/lisensi offline (Item 25c) — public key developer masih placeholder, gerbang nonaktif total
- `fb8ba80` — fix: build APK utk armeabi-v7a + arm64-v8a — akar masalah crash Infinix Smart 8 TERKONFIRMASI
- `f47e67b` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF — crash Infinix Smart 8 masih belum selesai
- `2c5ddf9` — fix: pindahkan crash log ke folder Downloads publik (Android/data terblokir File Manager) + jaring lebih awal
- `c48ec4b` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk fix tap-to-scan race + HID #PSN:
- `2ee8068` — fix: deteksi basi tap-to-scan + kode #PSN: pecah jadi beberapa scan di HID eksternal
- `26ce99c` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk fix crash Infinix Smart 8
- `e3a7b7d` — fix: cegah force-close diam-diam di HP tertentu (mis. Infinix Smart 8) + jaring pengaman crash log
- `1b6d275` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk bugfix tap-to-scan + atribusi antrian
- `c146695` — fix: scan tap-to-scan mengulang barang lama + atribusi pelanggan/pegawai tertukar di antrian
- `386b275` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF/PLAN untuk Item 24b
- `b04e064` — feat: sheet Verifikasi Pesanan sebelum lanjut bayar antrian handoff (Item 24b)
- `610d8b6` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF/PLAN untuk Item 24d-core
- `1f18000` — feat: gerbang pembayaran Pegawai via QR + antrian handoff (Item 24d)
- `7fa7907` — feat: catatan per-produk katalog HTML + tata letak kalkulator bayar (Item 26a/26b/26c)
- `8fa05d8` — docs: masukkan Item 26 — catatan per-produk HTML, posisi Uang Pas/keypad
- `5d65188` — docs: putuskan mekanisme kirim pesanan pegawai — QR gabung scanner kasir
- `9f9cb18` — docs: catat 2 opsi desain mekanisme kirim pesanan pegawai (Item 24d)

## 2026-07-12

- `4317c33` — feat: rename kosmetik "Kasir"→"Pegawai" di UI + izin Terima Pembayaran (Item 24d, bagian 1)
- `5a18301` — feat: tap-to-scan + redesign kapsul melayang scanner kasir (Item 24e+24f), + badge kosmetik "Habis" di kartu kasir (Item 25a — bagian kedua)
- `d9e1f2e` — feat: tanda "Stok Habis" cepat dari modal kasir (Item 25a) — inti
- `29d7400` — feat: hapus produk via swipe di tab Produk (Item 25b)
- `6285481` — feat: katalog HTML default terang + font Hanken Grotesk/Newsreader (Item 24c)
- `37ca76e` — feat: chip Uang Pas di modal Tambah Bayar/Lunasi (Item 24a)
- `a2ad03d` — fix: field harga produk tak bisa diketik setelah tap "Edit produk" dari keranjang (cart sheet salah kebuka lagi di belakang ProdukFormScreen, HID handler menelan input digit)
- `7950176` — docs: perbarui CHANGELOG/PATCHNOTES/HANDOFF untuk poin 2+3 (harga dasar & per-qty)
- `d703c0b` — feat: tampilkan harga per-qty di baris item keranjang kasir (mis. "Karung · Rp 65.000")
- `b1141f6` — feat: tampilkan harga dasar di bawah nama produk (tab Produk)
- `cd382ed` — fix: kalkulator tampilkan Kembalian palsu saat ada sisa tagihan lama (preview _change/_shortfall belum ikut existingShortfall)
- `88c8deb` — fix: hitungkan "Total yang perlu ditagih" di kalkulator, bukan kasir yang jumlah manual
- `765734e` — feat: info "+ Sisa tagihan sebelumnya" di kalkulator Tambah Belanjaan (kasir tahu Total kalkulator ≠ total yang perlu ditagih)
- `87cdaf0` — fix: "Dibayar" di Ringkasan struk tidak konsisten dgn Sisa Tagihan (Total != Dibayar+Sisa)
- `19e679d` — fix: Sisa Tagihan understated saat kembalian dipakai ulang sbg pembayaran baru (double-count di `paid`)

## 2026-07-11

- `c3e975a` — fix: centang "Pakai kembalian" di kalkulator bayar tidak merespons tap (state beku di sheet showModalBottomSheet)
- `0323d3f` — feat: cabut flag Eksperimental dari Tempel Pesanan
- `d77e81e` — feat: info kembalian terakhir + centang di kalkulator bayar Tambah Belanjaan (+ highlight nominal Total)
- `6173b57` — feat: Buku Hutang tampilkan daftar nota belum lunas per pelanggan (+ fix 2 overflow lama)
- `5759c18` — feat: Riwayat Pembayaran tampilkan kembalian per baris + centang per baris
- `399a742` — feat: kembalian per-baris pembayaran (schemaVersion 13)
- `cb87507` — feat: pindah toggle "Izinkan Stok Minus" ke halaman utama Pengaturan (dari dalam Izin Kasir)
- `9e52f61` — fix: owner selalu bisa override stok minus (sebelumnya ikut ke-block sama seperti kasir tanpa bypass khusus)
- `c8a79f1` — fix: tombol "Harga lain" di modal item kasir tampilkan nama opsi terpilih (mis. "Eceran"), bukan cuma hitungan generik
- `e4baa92` — fix: produk hasil import CSV hilang dari katalog HTML (isBaseUnit tidak pernah ditandai true, OrderPageService mensyaratkan itu tanpa fallback)
- `07fee39` — feat: pindah flag Eksperimental dari Katalog Pesanan (HTML, jadi native) ke menu baru "Import dari Griyo POS" (CsvImportScreen griyoMode)
- `63d0f2d` — fix: import CSV format Griyo POS (pemisah ";", header & satuan/grup legacy) — parser hanya kenal ",", alias kolom tidak cocok header asli Griyo, kolom Satuan/Grup berisi ID legacy mentah bukan nama teks

## 2026-07-10

- `15c50b8` — feat: Tutup Kasir harian — rekap kas sistem vs fisik + selisih + riwayat (tabel cash_closings, schemaVersion 12) — Item 15
- `56d42f1` — feat: pengingat backup (cek saat app dibuka, kartu status + toggle interval) — Item 13
- `33ecd4f` — feat: peringatan stok menipis (kolom min_stock, schemaVersion 11) — badge + filter di Produk — Item 11
- `9af9cb6` — feat: Harga Lain & tier grosir jadi dropdown menempel di field Harga (bukan chip menumpuk) — Item 19
- `4bd4d97` — fix: atribusi varian per-satuan (parentProductUnitId) + tombol minus tak menebak saat >1 satuan — Item 16
- `b48f7c2` — feat: beralih antar pesanan tertahan auto-hold keranjang aktif (tanpa dialog, tanpa kehilangan) — Item 18
- `320a0dc` — feat: Buku Hutang terpusat (tab Laporan, urut umur menunggak, lunasi langsung) — Item 12
- `b5ebaff` — feat: pencatatan pengeluaran + Laba Bersih di laporan (ExpensesScreen, unhide izin input_pengeluaran) — Item 9
- `eaa5ea6` — feat: edit metode pembayaran (reuse sheet) + hapus via swipe bila nonaktif — Item 14
- `dbdc779` — feat: tombol edit produk di modal kasir (owner/asisten saja) — Item 20
- `fd4ed1e` — feat: pilih metode bayar saat pelunasan/tambah bayar hutang (dialog reusable, ganti hardcode tunai)
- `f8f65e9` — fix: warna chip terpilih (tema, sistemik) + banner sukses hijau/gagal merah light & dark
- `b949268` — feat: reorder "Harga Lain" via drag-handle di form Produk (schemaVersion 10)
- `c1a9efe` — perf: optimasi halaman HTML Katalog Pesanan untuk HP low-end
- `3bff1b6` — fix: kunci dedup importer CSV ikut barcode/kode produk (silent data loss)
- `ea6e952` — fix: dropdown pelanggan scroll sungguhan, hapus pemotongan .take(N)
- `6f1fbc4` — fix: urutan qty/satuan di struk in-app (1 pcs x, bukan pcs 1 x)

## 2026-07-08

- `50752cd` — fix: rapikan layout topbar kasir + kecualikan tap produk dari collapse cari
- `632a836` — feat: checkbox kembalian sudah diambil, animasi expand kolom cari kasir
- `6dedc80` — feat: tombol Bayar Nanti terpisah, harga alternatif berlabel, poles Katalog Pesanan
- `ef9ab12` — feat(eksperimental): parser & UI Tempel Pesanan sisi kasir (Katalog Pesanan Fase 2)

## 2026-07-07

- `dc9c3ef` — docs: catat fitur eksperimental Katalog Pesanan (commit e422639)
- `e422639` — feat(eksperimental): katalog pesanan HTML self-contained tanpa hosting
- `1993b80` — chore: naikkan versi ke 2.1.1+3 untuk rilis perbaikan audit
- `b6fefbe` — fix: audit code review — consolidate payment logic & archive filtering (PR #2, squash dari `7ed9692`)
- `c1bafd7` — fix: audit ulang — konsolidasi pelunasan ke addPaymentToTransaction + filter arsip per-tahun
- `998a475` — docs: catat hasil audit kode — 14 bug fix + cleanup (commit 7d1fc6f, 81f1af6)
- `81f1af6` — chore: hapus kode mati hasil audit + sembunyikan izin fitur yang belum ada
- `7d1fc6f` — fix: perbaiki 12 temuan bug audit kode (sync arsip, retur multi-bank, CSV, kembalian, dll)
- `dd6f729` — docs: tambahkan metode test wajib sebelum rilis ke CLAUDE.md
- `eeb5ea1` — Rilis production v2.1.0 — deep debug, hardening, retur hutang, backup/restore fix, test suite lengkap
- `58b54bb` — docs: catat version bump 2.1.0+2 di changelog
- `3b7c305` — chore: naikkan versi ke 2.1.0+2 untuk rilis production pertama pasca deep-debug

## 2026-07-06

- `1eec864` — docs: catat Riwayat Transaksi Opsi C, optimasi pencarian, sync watermark (commit d9340b2)
- `d9340b2` — feat: Riwayat Transaksi Opsi C (auto-refresh saat sheet dibuka), optimasi pencarian produk (lepas dari volume riwayat), incremental sync watermark (arah host→klien)
- `b97ffcb` — fix(backup): perbaiki 2 bug restore (cross-device gagal password + StreamProvider tidak ter-notify)
- `a0c4c6c` — test(widget): buktikan overflow nama kasir panjang di struk sudah aman
- `5a8a49b` — docs: catat fitur Sisa/Kembali Riwayat Transaksi + feedback device Tier 4 user
- `79aa836` — feat(kasir): tampilkan sisa hutang/kembalian langsung di baris Riwayat Transaksi + fix overflow header Riwayat Transaksi

## 2026-07-05

- `f2f7829` — docs: catat harness widget-test & 2 overflow fix (changelog, patchnotes, hand-off)
- `7307740` — test(widget): bangun harness widget-test pertama + fix 2 overflow layout nyata di receipt_screen.dart
- `9991519` — refactor(chart): ekstrak clamp tinggi bar jadi pure function + test (Tier 3)
- `5a4ee57` — refactor(kasir): ekstrak alokasi diskon jadi pure function + test (Tier 3)
- `3a7ce6b` — test: Tier 2 — resolvePrice, mergeRows master-data, restoreFromDump, generateUniqueLocalId
- `9b9b3cc` — test: siklus hidup transaksi paling kritis (Tier 1) — saveTransaction, voidTransaction, addReturnTransaction, settleMergedDebt
- `0dff97e` — feat(kasir): retur nota belum lunas kini mengurangi hutang langsung

## 2026-07-02

- `61c7455` — perf(db): indeks transaction_payments(transaction_id) — cegah O(n^2) di startup (schema v7)
- `2d3dc37` — docs: catat hasil sesi deep debug (changelog, patchnotes, hand-off)
- `16ad934` — fix: deep debug — perbaikan bug lintas modul (stok, sync, backup, struk, chart, QRIS)

## 2026-07-01

- `9e16f22` — docs: add project memory files (CLAUDE.md, changelog, patchnotes, hand-off)
- `178d16a` — docs: archive original project reference files

## 2026-06-30

- `702212c` — feat(kasir): pulse animation on scan line for successful scans
- `f2d8b94` — fix(kasir,laporan): 5-item polish batch
- `a6868ce` — Katalog: fitur edit katalog tersimpan
- `e6039ff` — Laporan: ekspor per-kategori dengan grafik sesuai aplikasi + perbaiki ekspor
- `81bfe84` — Kasir: tab meta membentang penuh — hilangkan ruang kosong di samping Tahan
- `57b41c4` — Fitur katalog: buat & bagikan daftar harga sebagai gambar
- `1b292eb` — Settings, kasir, laporan & PDF export improvements

## 2026-06-29

- `7fdb65f` — Docs: revisi proposal pertimbangan Barokah Order
- `99112f9` — Docs: proposal lengkap sistem order pelanggan (HTML + WA + Paste Parser)

## 2026-06-28

- `65197cf` — Fix: scroll keranjang ke bawah — pindahkan trigger ke dalam builder
- `0d9f701` — Fix: keranjang langsung scroll ke bawah saat dibuka dari scan eksternal
- `051357b` — Kasir: debounce scanner eksternal 300ms + auto-scroll keranjang ke bawah

## 2026-06-27

- `939c07b` — Fix: field harga tidak bisa diketik — useRootNavigator membuat HID handler menelan input
- `d4911a8` — Fix: edit harga dari keranjang — tutup sheet dulu sebelum buka editor
- `e6728cd` — Fix: field harga tak bisa diketik (IME desync akibat pemisah ribuan)
- `76bcacf` — Debug: panel diagnostik field harga di modal entri item (sementara)
- `9aed569` — Fix: input harga tak terbaca saat modal item dibuka dari keranjang
- `8feaef7` — Fix: haptik scan tidak muncul + harga tak bisa diedit di modal keranjang
- `98c7ea6` — Kasir: haptik saat scan, scan eksternal buka keranjang, redesign cart bar
- `1f59836` — Sync harga satu arah, approve per kategori, izin stok minus asisten
- `b798ba8` — Kasir: cari SKU, modal edit item dari keranjang, catatan format quote

## 2026-06-26

- `1917ef8` — Fix sync mergeRows: handle local_id collision for append-only tables
- `b261027` — Fix tombol Setuju sync + pindah export katalog harga ke tab produk
- `b22c2ae` — Fix sync error Variable<Object> dan tombol Setuju tidak terlihat
- `f307ad7` — Tambah export CSV produk dan katalog sinkron harga di pengaturan
- `32b057a` — Fix mapping unit types sesuai data lama + merge ID 7,8 ke 12

## 2026-06-25

- `f4c2683` — Tambah 5 satuan baru: Ons, Rek, Paket, Box, Karton

## 2026-06-21

- `8e86e96` — Fix duplikat price tier yang menyebabkan sync harga gagal
- `4eb5a48` — Tambah logging sync harga & diagnostic duplikat tier di Pengaturan
- `033b8e2` — Fix layout antrian sync & terjemahkan nama tabel ke Indonesia
- `165b076` — Cetak tebal nama produk di label item terakhir cart bar

## 2026-06-20

- `bd2f0d6` — Fix logika sync harga: unit-aware match, varian, harga 0, layout
- `003666d` — Fix QR scan sync: strip port dari IP agar tidak dobel
- `9ddb5a9` — Fix sync error: product_units tidak punya kolom updated_at
- `ef3f769` — Penyesuaian UI catatan & laba: blockquote, toggle, riwayat
- `4c49ffb` — Laba inline di struk, catatan nota, pemisah hari riwayat, filter produk detail
- `baf0c8e` — Pelanggan/pegawai di cart bar + tahan pesanan inline
- `ff3b63d` — Tambah QR code untuk sync data dan sync harga

## 2026-06-19

- `9489b29` — Fix tambah belanjaan kedua kali tidak masuk ke struk
- `f8eb105` — Fitur tambah belanjaan: keranjang per-slot + alur bayar selisih
- `2d6a3ca` — Scanner torch + overlay panduan, fondasi tambah belanjaan

## 2026-06-18

- `9caf1c2` — Fitur sinkron harga antar toko: WiFi langsung + CSV

## 2026-06-17

- `b7916d8` — Fitur pegawai toko: dicatat per nota, tampil di struk
- `549709f` — Nota gabungan: id nota tidak bold, footer total/sisa pakai layout struk biasa
- `6d415ca` — Fix nota gabungan: hapus "Struk Gabungan", tambah alamat, perbaiki subtotal
- `266d103` — Struk: jam di samping tanggal, kode nota cukup nomor urut, jarak nama toko
- `f66117b` — Ukuran teks: pengaturan global + auto-fit layar
- `eefe8c0` — Poin loyalitas: aturan konfigurable + poin editable; + induk varian
- `c6ba690` — Kasir: perbaiki minus list view + dropdown varian inline (eksperimen)
- `56d5fba` — Fix: tombol minus, nama+alamat pelanggan di struk, catatan item
- `567037f` — docs: tulis README komprehensif
- `6ace6e7` — Kasir: tambah tombol minus di kartu produk, perbesar lingkaran qty
- `5416439` — Struk: sesuaikan format footer — total/kembali wide, bayar normal
- `979e9a1` — CI: APK langsung download tanpa zip via GitHub Release

## 2026-06-16

- `ebc7314` — Struk: perbesar footer & nama pelanggan, scanner eksternal, edit varian
- `a8c6ac0` — fix: sync izin kasir dari owner ke HP kasir
- `33bfc30` — fix: warna system navigation bar Android mengikuti dark/light mode
- `da6fe2a` — refactor: konsolidasi stok ke satuan dasar (schema v4)
- `8f9619c` — feat: penyesuaian stok manual dari detail produk
- `8fd0aa2` — fix: sync crash transaction_items, harga asli di struk in-app

## 2026-06-15

- `ef77bee` — fix: laba di struk in-app, warna pelanggan umum vs terdaftar
- `10b4bb4` — fix: donut chart contrast, profit di detail transaksi, timestamp semantik
- `6d75d13` — Gabung nota + timeline pembayaran di struk
- `a3e8799` — ci: fall back to debug signing when release keystore is absent
- `5c80c97` — ci: inject signing keystore from GitHub Secrets at build time
- `1685b85` — feat: receipt header redesign, fix customer edit UX, price padding
- `ddc9ddc` — feat: customizable receipt header (WhatsApp, Telegram, free header text)
- `3f928ae` — fix: receipt printed two timestamps
- `85a561c` — feat: inline edit buyer name on receipt screen
- `f825f74` — fix: catalog '+' uses green (not primary) when in cart
- `75edf4a` — UX: auto-select fields, clear confirm, accent color, edit customer in history
- `23cb63c` — fix: undo session variants on discard + inline banner for held orders

## 2026-06-14

- `8b74cc6` — fix: item note clearing + preserve parent base qty when mixing variants
- `a8a9f69` — fix: 8 bugs — variant/parent cart logic, transaction save, history filter, controller leaks, badge qty, CSV price parsing, COGS rounding, archive close
- `63064b1` — revert: 2 fixes that conflicted with project design intent
- `cb3ddd9` — fix: 7 bugs across kasir, produk, pengaturan, and database layers
- `8a539b5` — fix: paired devices inherited owner's device code
- `6431692` — fix: sync token length + archive read-only crash
- `6a9ad2e` — fix: sync timestamp unit mismatch + defensive customer name access
- `63abc4d` — fix: revert misguided B-5/A-5, fix C-5 non-stock false positive
- `27a8c34` — fix(A-5,C-4,A-12,C-5,B-3,B-4,B-5,B-6): apply changes to existing files
- `34dac77` — fix(A-5,C-4,A-12,C-5,B-3,B-4,B-5,B-6): resolve all deferred audit items

## 2026-06-13

- `8046596` — fix: audit P0–P3 — transaction integrity, security hardening, data integrity
- `c8e83ad` — fix: parent/variant flow, inline banner redesign, tutup buku button, printer logs
- `647035f` — fix: constrain trailing FilledButton in tutup buku ListTile
- `663d641` — feat: product group management + unsaved-changes guard on produk form
- `0872c5d` — feat: bold product names on thermal receipt, drop checkmark on print
- `5fe3c9c` — feat: add InlineBannerStateMixin and convert produk/printer screens
- `b721eda` — feat: replace all remaining SnackBars with InlineBanner in pengaturan screens
- `1ab7c7e` — fix: parent/variant cart logic — dua bug kritis
- `e0459fa` — Add InlineBanner widget + timestamp labels on charts
- `20a7ab7` — feat: variant auto-offset in cart + barcode scanner in product form
- `2c96cf5` — feat: redesign receipt format + paper size + format settings
- `7bcee82` — feat: bypass print_bluetooth_thermal with native Kotlin RFCOMM channel
- `903177d` — fix: printer writeBytes — 600ms stabilisasi RFCOMM + warm-up ESC@ sebelum data nyata
- `d928caf` — fix: printer ESC/POS — sanitasi ASCII semua string, em-dash dan non-ASCII tidak lagi crash
- `f2306fe` — feat: debug log panel printer — log setiap langkah koneksi+print dengan timing & warna

## 2026-06-12

- `180d8ba` — fix: teks vertikal di layar printer — override minimumSize FilledButton.tonal di ListTile trailing
- `26e283a` — feat: redesain keypad bayar (slide-up + ✓), warna semantik konsisten, perbaikan layar printer & toast dark mode
- `1a944df` — feat: varian produk (bersarang) + perbaikan tombol "+" katalog
- `aec8589` — fix: printer bluetooth, sticky keypad, delete pelanggan, sort A-Z, bayar nanti, kembalian
- `74d361e` — feat: tutup buku tahunan + arsip read-only
- `1286237` — feat: app icon lebih besar + format backup portable BPOSP
- `e8e953e` — Phase 3: UX + bisnis + fondasi performa database
- `1365b47` — ci: trigger Build APK on claude/** branches
- `c0aeb98` — fix: apply security & bug audit fixes across all layers
- `5f763af` — feat(produk): support multiple price tiers per unit (harga grosir)
- `34615e7` — feat: kasir item entry modal, price in catalog, counter button + fixes
- `5641cd1` — feat: add Slop unit type + seed existing DBs via beforeOpen

## 2026-06-11

- `353b80b` — design: fresh UI — Hanken Grotesk + Newsreader, warm palette, kasir topbar
- `46288de` — ci: build single arm64-v8a APK instead of split-per-abi
- `d672ca7` — fix: use named top-level function for SQLCipher isolateSetup
- `a996c43` — fix: load SQLCipher in background isolate — crash libsqlite3.so not found
- `8809788` — fix ci: pin Flutter 3.24.5 to match dev environment
- `87ae1bf` — feat: kasir UX from mockup — hold orders, tx history, keypad, share struk
- `773774f` — fix: upgrade AGP 8.1→8.3 and Kotlin 1.8→1.9 for file_picker compat
- `c406ad5` — add phase 6 HTML preview (WiFi sync, printer, backup, CSV, export, izin kasir)
- `371e583` — add GitHub Actions build workflow + peach emoji app icon
- `d186289` — enforce input_stok permission for kasir on produk form/list
- `4c5a212` — feat: implement WiFi sync, Bluetooth printer, PDF/XLSX export, backup, CSV import, kasir permissions
- `13882bd` — chore: track Flutter .metadata file
- `2a6a61d` — feat: Phase 5 — Polish: nama produk di struk + barcode screen
- `1adefac` — feat: Phase 4 — Pengaturan screens fungsional
- `15529f1` — feat: Phase 3 — Ringkasan dashboard + Laporan 4-tab
- `c60a678` — feat: Phase 2 — Kasir, Produk, Pelanggan CRUD + pembayaran
- `02f087a` — feat: Phase 1 — Flutter foundation + full DB schema + HTML preview
